---
phase: 2.4
slug: advanced-attack-tiers
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-22
---

# Phase 2.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (already installed in `rag-security` conda env) |
| **Config file** | `tests/` directory (no pytest.ini; uses defaults) |
| **Quick run command** | `conda run -n rag-security python -m pytest tests/ -v -m "not integration" --timeout=30` |
| **Full suite command** | `conda run -n rag-security python -m pytest tests/ -v --timeout=60` |
| **Estimated runtime** | ~30 seconds (unit tests only; integration tests excluded by default) |

---

## Sampling Rate

- **After every task commit:** Run `conda run -n rag-security python -m pytest tests/ -v -m "not integration" --timeout=30`
- **After every plan wave:** Run full suite `conda run -n rag-security python -m pytest tests/ -v --timeout=60`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 2.4-01-01 | 01 | 1 | ATK-06 | — | Tier 3 passage IDs 20100–20149 present in corpus_poisoned.jsonl | unit | `conda run -n rag-security python -m pytest tests/test_corpus.py -k "tier3_ids" -x` | ❌ W0 | ⬜ pending |
| 2.4-01-02 | 01 | 1 | ATK-06 | — | Anchor `t3.evilcorp.io` present in all Tier 3 passage texts | unit | `conda run -n rag-security python -m pytest tests/test_corpus.py -k "tier3_anchor" -x` | ❌ W0 | ⬜ pending |
| 2.4-01-03 | 01 | 1 | ATK-07 | — | Tier 4 passage IDs 20200–20299 present; exactly 3 fragments per attack set | unit | `conda run -n rag-security python -m pytest tests/test_corpus.py -k "tier4" -x` | ❌ W0 | ⬜ pending |
| 2.4-02-01 | 02 | 2 | D-15 | — | Tier-detection predicate: ID 20100 → tier3, ID 20200 → tier4, ID 20099 → tier2 (not tier3) | unit | `conda run -n rag-security python -m pytest tests/test_pipeline.py -k "tier_detection" -x` | ❌ W0 | ⬜ pending |
| 2.4-02-02 | 02 | 2 | EVAL-V2-02 | — | Judge output parsing: "YES" and "NO" parsed correctly; unknown/malformed → None | unit | `conda run -n rag-security python -m pytest tests/test_generator.py -k "judge" -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_corpus.py` — add test stubs: `test_tier3_ids_in_range`, `test_tier3_anchor_present`, `test_tier4_ids_in_range`, `test_tier4_three_fragments_per_set`
- [ ] `tests/test_pipeline.py` — add test stub: `test_tier_detection_predicate` (unit-tests the fixed ID-range predicate logic without touching Ollama)
- [ ] `tests/test_generator.py` — add test stub: `test_judge_output_parsing` (unit-tests YES/NO/fallback parsing)

*Wave 0 must be complete before any Wave 1 tasks begin.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| kimi-k2.5:cloud generates factual prose (not imperative/injection-style) | ATK-06 / D-01 | Requires `ollama login` and live cloud model call; non-deterministic output | Review 5 random Tier 3 passages from corpus_poisoned.jsonl: confirm no "IGNORE", "SYSTEM:", or imperative overrides; confirm `t3.evilcorp.io` appears naturally embedded |
| Tier 4 fragments co-retrieved for target query (all 3 in top-5 hits) | ATK-07 / D-08 | Co-retrieval rate is empirical, not deterministic | Run `conda run -n rag-security python scripts/run_eval.py --collection nq_poisoned_v4 --queries data/test_queries.json --defense off --output logs/eval_t4_coretrieval_check.json` and inspect per-query hits for Tier 4 sets |
| LLM-as-judge agreement rate with substring detection | EVAL-V2-02 | Requires live gpt-oss:20b-cloud calls | After running judge script, review agreement_rate field in output JSON; report as research finding regardless of value |
| Cross-model eval (llama vs mistral 4-tier ASR table) | EVAL-V2-01 prereq | Requires running two full eval passes with separate Ollama models | Run both eval passes, confirm result files exist with valid JSON and non-null aggregate fields for all 4 tiers |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
