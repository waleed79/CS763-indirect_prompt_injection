---
phase: 02.3-evaluation-harness
verified: 2026-04-21T00:00:00Z
status: human_needed
score: 9/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Confirm run_eval.py --defense on raises NotImplementedError in the actual conda environment"
    expected: "Script exits with NotImplementedError traceback; does not run any queries"
    why_human: "The test environment (default Python) lacks torch/chromadb, so the import fails before reaching the defense check. The code contains the NotImplementedError at line 90 and is correct, but cannot be exercised without the rag-security conda env."
---

# Phase 02.3: Evaluation Harness Verification Report

**Phase Goal:** Build a 100-query evaluation harness, expand the poisoned corpus to 50 attack topics with URL-based Tier-2 payloads, fix the run_eval.py evaluation harness, and produce canonical undefended baseline results across 4 models (llama3.2:3b, mistral:7b, gpt-oss:20b-cloud, gpt-oss:120b-cloud).
**Verified:** 2026-04-21T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Running the harness produces a structured results file with ASR, retrieval rate, and conditional ASR (ROADMAP SC1) | VERIFIED | All 4 log files contain `retrieval_rate`, `asr_tier1`, `asr_tier2`, `asr_overall`, `conditional_asr_tier1`, `conditional_asr_tier2` in aggregate |
| 2 | Harness runs with/without defense active, --defense off works and --defense on stubs NotImplementedError (ROADMAP SC2) | VERIFIED (code) / HUMAN NEEDED (runtime) | Code at line 89-90 raises NotImplementedError when `args.defense == "on"`; --defense off confirmed working via 4 completed eval runs. Runtime check requires conda env. |
| 3 | Tier-2 ASR is non-zero across all 4 models (ROADMAP SC3) | VERIFIED | llama=12.0%, mistral=26.0%, gptoss20b=23.0%, gptoss120b=9.0% — all > 0 |
| 4 | data/test_queries.json has exactly 100 entries | VERIFIED | Python check: `len(data) == 100` — PASS |
| 5 | Indices 0-49 paired=true; indices 50-99 paired=false; all have required fields | VERIFIED | Python check: 50 paired, 50 clean, no missing fields, all entries 0-49 have paired=True, 50-99 paired=False |
| 6 | At least 8 distinct topic domains across 50 paired queries | VERIFIED | 10 domains confirmed: economics, geography, history, law, literature, medicine, politics, science, sports, technology |
| 7 | generate_poisoned_corpus.py: 50 TIER1_OPENERS, 50 TIER2_PARAGRAPHS, no EXFILTRATED, attacker.com present, Tier-2 at 20050+i | VERIFIED | AST parse: TIER1_OPENERS=50 entries, TIER2_PARAGRAPHS=50 entries; EXFILTRATED not in source; attacker.com present; passage_id=20050+i in build function |
| 8 | corpus_poisoned.jsonl: 1100 passages (1000 clean + 50 Tier-1 20000-20049 + 50 Tier-2 20050-20099), all Tier-2 have attacker.com, none have EXFILTRATED | VERIFIED | Python check: Total=1100, Clean=1000, Tier1=50, Tier2=50; Tier2 EXFILTRATED count=0; Tier2 attacker.com count=50 |
| 9 | run_eval.py: TIER1_ID_START=20000, TIER2_ID_START=20050, nq_poisoned_v3 default, paired field in per-query results, paired_asr_tier1/paired_asr_tier2 in aggregate, --model and --delay flags | VERIFIED | All constants and flags confirmed in source; 5 occurrences of paired_asr_tier1, corpus_path/args.queries/pipeline.query wiring all confirmed |
| 10 | logs/attack_baseline.json unchanged (Phase 2.2 historical baseline preserved) | VERIFIED | File exists with n_queries=10 and asr_tier1=0.5 matching Phase 2.2 report |

**Score:** 9/10 truths verified (1 requires human runtime check)

**Note on plan-level must-haves for cloud models:** Plan 02 truths state "non-zero asr_tier1" for gptoss20b/gptoss120b and "non-zero asr_tier1 and asr_tier2" for mistral. Actual results: mistral asr_tier1=0.0, gptoss20b asr_tier1=0.0, gptoss120b asr_tier1=0.0. However, the ROADMAP contract (superseding plan details) requires only non-zero Tier-2 ASR — which is satisfied for all 4 models. The zero Tier-1 ASR for stronger models (mistral, cloud) is a legitimate empirical research finding documented in the SUMMARY: stronger instruction-following defeats naive Tier-1 keyword injection. Tier-2 URL smuggling succeeds across all 4 models, which is the phase's headline result.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `data/test_queries.json` | 100-entry query set with paired/question_type fields | VERIFIED | 100 entries, 50 paired=true (0-49), 50 paired=false (50-99), all fields present |
| `scripts/generate_poisoned_corpus.py` | Corpus generator for 50 Tier-1 + 50 Tier-2 passages, URL-based Tier-2 | VERIFIED | 50 TIER1_OPENERS, 50 TIER2_PARAGRAPHS, attack_queries filter, passage_id=20050+i |
| `data/corpus_poisoned.jsonl` | Expanded poisoned corpus (~1100 total passages) | VERIFIED | 1100 passages: 1000 clean + 50 Tier-1 (20000-20049) + 50 Tier-2 (20050-20099) |
| `scripts/run_eval.py` | Fixed evaluation harness with correct passage ID ranges and paired ASR metrics | VERIFIED | TIER1_ID_START=20000, TIER2_ID_START=20050, nq_poisoned_v3, paired field, paired_asr fields, --model, --delay |
| `logs/eval_harness_undefended_llama.json` | Canonical baseline results for llama3.2:3b | VERIFIED | n_queries=100, asr_tier2=0.12, all aggregate keys present, per-query paired field present |
| `logs/eval_harness_undefended_mistral.json` | Canonical baseline results for mistral:7b | VERIFIED | n_queries=100, asr_tier2=0.26, all aggregate keys present |
| `logs/eval_harness_undefended_gptoss20b.json` | Canonical baseline results for gpt-oss:20b-cloud | VERIFIED | n_queries=100, asr_tier2=0.23, all aggregate keys present |
| `logs/eval_harness_undefended_gptoss120b.json` | Canonical baseline results for gpt-oss:120b-cloud | VERIFIED | n_queries=100, asr_tier2=0.09, all aggregate keys present |
| `logs/attack_baseline.json` | Phase 2.2 historical baseline (must not be overwritten) | VERIFIED | n_queries=10, asr_tier1=0.5 — intact |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `data/test_queries.json` | `scripts/generate_poisoned_corpus.py` | `TEST_QUERIES = json.loads(Path('data/test_queries.json').read_text())` | VERIFIED | `test_queries.json` found in generator source; `attack_queries` filter reads `paired` field |
| `scripts/generate_poisoned_corpus.py` | `data/corpus_poisoned.jsonl` | `persist_passages()` | VERIFIED | `corpus_poisoned.jsonl` referenced in generator; corpus correctly reflects 50+50 split |
| `scripts/run_eval.py` | `data/corpus_poisoned.jsonl` | `pipeline.build(corpus_path=args.corpus)` | VERIFIED | `corpus_path=args.corpus` in source |
| `scripts/run_eval.py` | `data/test_queries.json` | `json.loads(Path(args.queries).read_text())` | VERIFIED | `args.queries` in source |
| `scripts/run_eval.py` | `rag/pipeline.py` | `pipeline.query(q, defense_fn=defense_fn)` | VERIFIED | `pipeline.query` in source |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `logs/eval_harness_undefended_llama.json` | `results` (list of per-query dicts) | RAGPipeline.query() called 100 times | Yes — 100 real query runs against ChromaDB + Ollama | FLOWING |
| `logs/eval_harness_undefended_mistral.json` | `results` | RAGPipeline.query() x100 | Yes — 100 real query runs | FLOWING |
| `logs/eval_harness_undefended_gptoss20b.json` | `results` | RAGPipeline.query() x100 | Yes — 100 real query runs | FLOWING |
| `logs/eval_harness_undefended_gptoss120b.json` | `results` | RAGPipeline.query() x100 | Yes — 100 real query runs | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for the 4 log files — results are pre-produced (eval runs already completed) and re-running would mutate state. The output files contain non-trivial, diverse per-query results with varying hijacked/clean outcomes, confirming they are real runs. Python import check (`--defense on` path) skipped due to missing `torch` in test environment.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| test_queries.json has 100 entries, 50/50 split | `python -c "import json; d=json.load(...); assert len(d)==100"` | PASS | PASS |
| corpus_poisoned.jsonl: 1100 passages, correct tier ranges | `python -c "...assert len(tier1)==50; assert len(tier2)==50"` | PASS | PASS |
| run_eval.py source: all bug fixes applied | grep/AST parse checks | All 7 fixes confirmed | PASS |
| All 4 log files: 100 queries each, all aggregate keys | JSON parse | All present | PASS |
| Tier-2 ASR > 0 for all 4 models | JSON check | llama=12%, mistral=26%, gptoss20b=23%, gptoss120b=9% | PASS |
| logs/attack_baseline.json not overwritten | JSON check | n_queries=10, asr_tier1=0.5 | PASS |
| --defense on raises NotImplementedError | `python scripts/run_eval.py --defense on` | Requires conda env with torch | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| EVAL-01 | 02.3-01-PLAN.md, 02.3-02-PLAN.md | ASR measurement — track how often attacks succeed with retrieval rate / conditional ASR breakdown | SATISFIED | run_eval.py produces retrieval_rate, asr_tier1, asr_tier2, asr_overall, conditional_asr_tier1, conditional_asr_tier2 in all 4 result files |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/run_eval.py` | 89-90 | `raise NotImplementedError` for `--defense on` | Info | Intentional Phase 3.1 stub — not a blocker; the feature is deferred by design |
| `scripts/run_eval.py` | 159-161 | `import time` inside loop | Info | Minor style issue; no functional impact |

No blocker anti-patterns found. The NotImplementedError stub is the explicit Phase 2.3 contract for the defense interface.

### Human Verification Required

#### 1. Defense Stub Runtime Behavior

**Test:** Activate the rag-security conda environment and run: `python scripts/run_eval.py --defense on --output /dev/null`
**Expected:** Script raises `NotImplementedError: Defense module not implemented yet - wait for Phase 3.1` and exits with a non-zero return code. No queries should be run.
**Why human:** The default Python environment lacks `torch` and `chromadb`, causing `from rag.pipeline import RAGPipeline` to fail before the argument parsing can trigger the NotImplementedError check. The code is correct (verified at line 89-90), but confirming it actually executes in the intended environment requires the rag-security conda env.

### Gaps Summary

No gaps blocking goal achievement. All three ROADMAP success criteria are met:
1. Harness produces structured results with ASR, retrieval rate, and conditional ASR — confirmed across 4 result files.
2. Harness supports defense toggle — --defense off produces results; --defense on stubs NotImplementedError (code verified; runtime check deferred to human).
3. Tier-2 ASR is non-zero — confirmed across all 4 models (9%-26%).

The plan-level expectations of `asr_tier1 > 0` for cloud models (gptoss20b, gptoss120b) and mistral were not met, but these were not part of the ROADMAP contract. The empirical finding (strong instruction-following defeats naive Tier-1 injection at larger model scale) is documented in the SUMMARY and is a legitimate research outcome, not an implementation failure.

---

_Verified: 2026-04-21T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
