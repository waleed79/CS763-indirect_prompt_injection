# Phase 02.3: Evaluation Harness - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-21
**Phase:** 02.3-evaluation-harness
**Areas discussed:** Tier 3/4 ID convention, Model for canonical eval, Remaining scope, Multi-model naming, Query set expansion

---

## Tier 3/4 ID Convention

| Option | Description | Selected |
|--------|-------------|----------|
| T3: 10020-10029, T4: 10030-10039 | Contiguous ranges, easy to extend | |
| T3: 11000-11999, T4: 12000-12999 | Widely spaced, visually distinct | |
| Let Claude decide in Phase 2.4 | Don't lock it now | ✓ |

**User's choice:** Deferred to Phase 2.4  
**Notes:** Phase 2.4 owns Tier 3/4 entirely — no need to reserve ID ranges now.

---

## Model for Canonical Eval

| Option | Description | Selected |
|--------|-------------|----------|
| mistral:7b | Matches current config.toml | |
| llama3.2:3b | Matches attack_baseline.json for direct comparison | |
| Both — run eval on both models | Produces two baseline files, one per model | ✓ |

**User's choice:** Both models  
**Notes:** Phase 2.4 requires a cross-model ASR table (Tier 1–4, both LLMs). Phase 2.3 establishes the Tier 1/2 rows. Canonical filenames: `logs/eval_harness_undefended_llama.json` and `logs/eval_harness_undefended_mistral.json`.

---

## Remaining Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Corpus fix + eval run only | Regenerate corpus + run eval | ✓ |
| Also add --model flag | Cleaner for multi-model runs | |
| Also extend harness for Tier 3/4 stubs | Placeholder detection logic | |

**User's choice:** Corpus fix + eval run only (harness code changes limited to bug fixes)  
**Notes:** During codebase scout, discovered two bugs in run_eval.py that were not previously known:
(1) `TIER1_ID_START = 10000` but corpus uses 20000; (2) collection default is `nq_poisoned_v1` but Phase 2.2 used `nq_poisoned_v2`. These need fixing alongside the Tier 2 payload fix.

---

## Multi-Model File Naming

| Option | Description | Selected |
|--------|-------------|----------|
| 02.3 defines canonical names | logs/eval_harness_undefended_llama.json + _mistral.json | ✓ |
| 02.3 uses single file, 02.4 reruns both | Simpler separation | |
| Let Claude decide naming | Doesn't matter much | |

**User's choice:** 02.3 defines canonical names  
**Notes:** Phase 2.4 adds Tier 3/4 columns alongside these Tier 1/2 baselines.

---

## Query Set Expansion

**User's input:** "I want to increase the data and the number of poisoned queries and their variety"

| Question | Options | Selected |
|----------|---------|----------|
| Query count | 20, 50, 100 | 100 |
| Variety | More topics, question type variety, clean queries with no poisoned doc, adversarial queries | All four |
| Attack-paired split | 50/50, 70/30, all 100 | 50 attack-paired + 50 clean |

**Notes:** 
- 50 attack-paired queries → 100 poisoned docs (50 Tier 1 + 50 Tier 2)
- Tier 1 passage_ids: 20000–20049; Tier 2: 20050–20099
- New collection: `nq_poisoned_v3`
- Schema update: add `"paired": bool` and `"question_type": str` fields
- Clean queries (no poisoned doc) serve as the FPR baseline for Phase 3.1 defense evaluation

---

## Claude's Discretion

- Whether to add `--model` CLI flag to `run_eval.py`
- Exact Tier 2 URL payload phrasing (must contain "attacker.com")
- Exact JSON field names for paired vs raw ASR stats
- Templating approach for 50-topic corpus generation

## Deferred Ideas

- Tier 3/4 ID ranges — Phase 2.4
- Poisoning ratio sweep — Phase 3.3
- LLM-as-judge scoring — Phase 2.4 bootstraps
- Multi-seed aggregation — Phase 3.2
