---
phase: 03.3-quick-evaluation-additions
plan: "03"
subsystem: attack
tags: [atk-02, poisoning-ratio-sweep, corpus-generation, poisonedrag]

dependency_graph:
  requires:
    - phase: 03.3-01
      provides: tests/test_ratio_sweep.py — ATK-02 ratio sweep math contract (RATIOS, math.ceil, 5 output files)
    - phase: 02.2-attack-module
      provides: scripts/generate_poisoned_corpus.py::build_poisoned_passages() — Tier-1 passage pool
  provides:
    - scripts/run_ratio_sweep.py — ATK-02 corpus sweep driver with RATIOS constant and math.ceil rounding
    - data/corpus_ratio_0005.jsonl — 1005-line corpus (1000 clean + 5 Tier-1 poisoned at 0.5%)
    - data/corpus_ratio_0010.jsonl — 1010-line corpus (1000 clean + 10 Tier-1 poisoned at 1%)
    - data/corpus_ratio_0020.jsonl — 1020-line corpus (1000 clean + 20 Tier-1 poisoned at 2%)
    - data/corpus_ratio_0050.jsonl — 1050-line corpus (1000 clean + 50 Tier-1 poisoned at 5%)
    - data/corpus_ratio_0100.jsonl — 1100-line corpus (1000 clean + 100 Tier-1 poisoned at 10%)
  affects:
    - 03.3-07 (EVAL-V2-01 matrix driver) — ratio-sweep corpora available for eval experiments
    - Phase 3.4 writeup — ASR-vs-poisoning-ratio curve data

tech-stack:
  added: []
  patterns:
    - "ATK02_SWEEP_ID_START (21000-21049) reserved band for cycle-1 Tier-1 pool extension — avoids ID collision with T2 (20050-20099)"
    - "p._replace(passage_id=new_id) for NamedTuple field update — Passage is NamedTuple not dataclass; dataclasses.replace() raises TypeError"
    - "math.ceil(ratio * n_clean) adversary-favoring rounding — PoisonedRAG convention, never round()"
    - "n_clean = len(clean) dynamic — never hardcoded 500 or 1000"

key-files:
  created:
    - scripts/run_ratio_sweep.py
    - data/corpus_ratio_0005.jsonl
    - data/corpus_ratio_0010.jsonl
    - data/corpus_ratio_0020.jsonl
    - data/corpus_ratio_0050.jsonl
    - data/corpus_ratio_0100.jsonl
  modified: []

key-decisions:
  - "Tier-1 pool cycling uses ATK02_SWEEP_ID_START (21000-21049) for cycle-1 replicas — NOT p.passage_id + 50 which lands in 20050-20099 (Tier-2 range); threat T-3.3-03-03 mitigated"
  - "p._replace() used for NamedTuple field update instead of dataclasses.replace() — Passage is NamedTuple, dc_replace raises TypeError (Rule 1 auto-fix)"
  - "n_poison per ratio verified: 5/10/20/50/100 (math.ceil on 1000-passage base per CONTEXT D-10)"

patterns-established:
  - "ATK02_SWEEP_ID_START pattern: cycle extra pool docs into reserved band above all existing tiers"

requirements-completed: [ATK-02]

duration: 5min
completed: "2026-04-25"
---

# Phase 03.3 Plan 03: ATK-02 Poisoning Ratio Sweep Summary

**ATK-02 ratio sweep driver generating 5 poisoned corpora at 0.5%/1%/2%/5%/10% of 1000-passage baseline, with Tier-1 pool cycling into ATK02_SWEEP_ID_START band (21000-21049) for the 100-doc 10% case.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T06:59:51Z
- **Completed:** 2026-04-25T07:04:46Z
- **Tasks:** 2
- **Files created:** 6 (1 script + 5 JSONL corpora)

## Accomplishments

- `scripts/run_ratio_sweep.py` created with `RATIOS = [0.005, 0.01, 0.02, 0.05, 0.10]`, `CLEAN_PATH`, `math.ceil(ratio * n_clean)` rounding, and safe pool-cycling logic
- 5 poisoned corpus JSONL files generated at 0.5%, 1%, 2%, 5%, 10% poisoning ratios with correct Tier-1 doc counts: 5/10/20/50/100
- Cycle-1 IDs use ATK02_SWEEP_ID_START (21000-21049) avoiding any collision with T2 range (20050-20099)
- TestRatioSweepMath: 6/6 passed; full suite: 147 passed 29 skipped 0 failed (no regressions)

## Task Commits

1. **Task 1: Create scripts/run_ratio_sweep.py** - `7d03788` (feat)
2. **Task 2: Run sweep driver to generate 5 corpus JSONL files** - `2b316dd` (feat)

## Files Created/Modified

- `scripts/run_ratio_sweep.py` — ATK-02 corpus sweep driver with RATIOS constant, math.ceil arithmetic, _tier1_only() pool cycling
- `data/corpus_ratio_0005.jsonl` — 1005 lines: 1000 clean + 5 Tier-1 poisoned (0.5%)
- `data/corpus_ratio_0010.jsonl` — 1010 lines: 1000 clean + 10 Tier-1 poisoned (1%)
- `data/corpus_ratio_0020.jsonl` — 1020 lines: 1000 clean + 20 Tier-1 poisoned (2%)
- `data/corpus_ratio_0050.jsonl` — 1050 lines: 1000 clean + 50 Tier-1 poisoned (5%)
- `data/corpus_ratio_0100.jsonl` — 1100 lines: 1000 clean + 100 Tier-1 poisoned (10%)

## Decisions Made

- Tier-1 pool cycling uses `ATK02_SWEEP_ID_START` (21000-21049) for cycle-1 replicas per threat T-3.3-03-03 mitigation — the "simple" formula `p.passage_id + 50` would land cycle-1 IDs in 20050-20099 (Tier-2 range) corrupting ATK-02 retrieval metrics
- `p._replace(passage_id=new_id)` used instead of `dataclasses.replace()` — `Passage` is a `NamedTuple` not a dataclass; `dc_replace` raises `TypeError: replace() should be called on dataclass instances`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed NamedTuple vs dataclass replace() call**
- **Found during:** Task 2 (running sweep driver)
- **Issue:** Plan's `_tier1_only()` template used `dc_replace(p, passage_id=new_id)` but `Passage` is a `NamedTuple`; `dataclasses.replace()` raises `TypeError`
- **Fix:** Replaced `dc_replace(p, passage_id=new_id)` with `p._replace(passage_id=new_id)` (NamedTuple's native replace method); removed unused `dataclasses.replace` import
- **Files modified:** `scripts/run_ratio_sweep.py`
- **Verification:** `python scripts/run_ratio_sweep.py` exits 0, all 5 files generated
- **Committed in:** `2b316dd` (Task 2 commit)

**2. [Rule 1 - Bug] Fixed persist_passages() return value used as line count**
- **Found during:** Task 2 (reviewing persist_passages API)
- **Issue:** Plan's print statement used `n_written = persist_passages(...)` as integer, but `persist_passages()` returns `Path` not `int`
- **Fix:** Changed to `persist_passages(combined, out_path)` then `n_written = len(combined)` for the print
- **Files modified:** `scripts/run_ratio_sweep.py`
- **Verification:** Print output shows correct counts (1005/1010/1020/1050/1100)
- **Committed in:** `2b316dd` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — bugs in plan's code template)
**Impact on plan:** Both fixes necessary for correct execution. No scope creep. Threat T-3.3-03-03 mitigated as specified.

## Issues Encountered

None beyond the two auto-fixed bugs above.

## Known Stubs

None. All 5 corpus files are fully populated and verified.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. The ratio sweep driver reads from a hardcoded local path (`data/corpus_clean.jsonl`) and writes to 5 fixed output paths — T-3.3-03-01 (math.ceil convention) and T-3.3-03-03 (ID collision prevention) both mitigated as designed.

## Next Phase Readiness

- 5 ratio-sweep corpora available for eval experiments in Phase 3.4 (ASR-vs-poisoning-ratio curve)
- `run_ratio_sweep.py` importable; `RATIOS` constant usable by downstream eval scripts
- Full test suite clean (147 passed 29 skipped)

---

*Phase: 03.3-quick-evaluation-additions*
*Completed: 2026-04-25*

## Self-Check: PASSED

Files verified:
- scripts/run_ratio_sweep.py: FOUND
- data/corpus_ratio_0005.jsonl: FOUND
- data/corpus_ratio_0010.jsonl: FOUND
- data/corpus_ratio_0020.jsonl: FOUND
- data/corpus_ratio_0050.jsonl: FOUND
- data/corpus_ratio_0100.jsonl: FOUND

Commits verified:
- 7d03788: feat(03.3-03): add ATK-02 ratio sweep driver
- 2b316dd: feat(03.3-03): generate 5 ATK-02 ratio-sweep corpus JSONL files
