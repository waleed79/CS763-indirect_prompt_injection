---
phase: 03.3-quick-evaluation-additions
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/xss_ssrf_taxonomy.md
autonomous: true
requirements: [EVAL-08]

must_haves:
  truths:
    - "docs/xss_ssrf_taxonomy.md exists as a valid Markdown file"
    - "The taxonomy table contains at minimum these three rows: (1) Stored XSS mapped to Corpus Poisoning, (2) SSRF mapped to Tool-Call Injection, (3) CSP mapped to Context Sanitization (per D-18)"
    - "Each of the three required rows contains all 5 columns populated: Web Attack Class, IPI Analog, Mechanism, Defensive Control, Citation (per D-18)"
    - "Each row cites at least one specific concrete example (e.g., actual CVE, OWASP section, or project phase reference) — no empty citation cells"
    - "The file includes a section header or preamble that explicitly frames this as a mapping between classical web-security vulnerability classes and IPI (Indirect Prompt Injection) attack classes"
    - "grep -c '^|.*|.*|.*|.*|.*|' docs/xss_ssrf_taxonomy.md returns at least 4 (header row + separator + 3 data rows minimum)"
  artifacts:
    - path: "docs/xss_ssrf_taxonomy.md"
      provides: "IPI-to-web-security taxonomy mapping table for Phase 3.4 paper inclusion"
      contains: "Stored XSS"
  key_links:
    - from: "docs/xss_ssrf_taxonomy.md::table"
      to: "IPI research artifacts (Phase 2.2 corpus poisoning, Phase 3.1 context sanitization)"
      via: "citation column references project phases and/or external sources"
      pattern: "Phase [0-9]"
---

<objective>
Implement EVAL-08: produce a formal Markdown taxonomy table that maps classical web-security
vulnerability classes to Indirect Prompt Injection (IPI) attack classes. This is a pure
writing/analysis deliverable — no code, no experiments.

Purpose: Provide the Phase-3.4 paper with a defensible conceptual framing that situates IPI
in RAG systems within the established web-security literature. Demonstrates that IPI attack
classes (corpus poisoning, tool-call injection) have mechanistic analogs in well-understood
web vulnerabilities (stored XSS, SSRF), and that defensive primitives (context sanitization)
are analogous to long-standing web defenses (CSP). Required rows per CONTEXT D-18:
stored XSS ↔ corpus poisoning, SSRF ↔ tool-call injection, CSP ↔ context sanitization.

Output: `docs/xss_ssrf_taxonomy.md` with a Markdown table containing at minimum the three
required mapping rows, each with all five columns populated (attack class, IPI analog,
mechanism, defensive control, citation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.3-quick-evaluation-additions/03.3-CONTEXT.md
@.planning/phases/03.3-quick-evaluation-additions/03.3-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/xss_ssrf_taxonomy.md with required mapping table</name>
  <files>docs/xss_ssrf_taxonomy.md</files>
  <action>
Create the file `docs/xss_ssrf_taxonomy.md`. The file MUST be valid Markdown and MUST contain
a section that begins with a level-1 or level-2 header framing the document as an "IPI ↔
Web-Security Taxonomy" (or equivalent).

The file MUST contain a Markdown table with these exact 5 columns (column header text is
author's discretion so long as intent is preserved):

  | Web Attack Class | IPI Analog | Mechanism | Defensive Control | Citation |

The table MUST include, at minimum, the following three required rows (per D-18). Each
row's cells must be non-empty and substantive (one full sentence or citation is the minimum
bar — a single word like "yes" or "TBD" is NOT acceptable):

  Row 1 (Stored XSS ↔ Corpus Poisoning):
    - Web Attack Class: "Stored (Persistent) Cross-Site Scripting (XSS)"
    - IPI Analog: "Corpus Poisoning / Indirect Prompt Injection via Retrieved Documents"
    - Mechanism: Must describe the parallel — attacker plants content in a trusted data
      store (HTML page / RAG corpus) that is later fetched and rendered/executed by the
      victim (browser / LLM) in an elevated trust context
    - Defensive Control: Must name a specific web defense (e.g., output encoding,
      Content-Security-Policy, sanitization) and its IPI analog (context sanitization
      classifier from Phase 3.1, substring-based anchor detection, etc.)
    - Citation: At least one concrete reference — e.g., "OWASP Top 10 A03:2021 Injection",
      "CWE-79", "Phase 2.2 corpus poisoning results (03.3-02-PLAN)", or an external paper

  Row 2 (SSRF ↔ Tool-Call Injection):
    - Web Attack Class: "Server-Side Request Forgery (SSRF)"
    - IPI Analog: "Tool-Call Injection / Agentic RAG Tool Hijack"
    - Mechanism: Must describe the parallel — attacker forces the server/agent to issue
      outbound requests it would not normally make, exploiting the server's elevated
      network position (internal services / tool-call APIs)
    - Defensive Control: Must name a specific web defense (e.g., allowlist, metadata
      service blocking, network segmentation) and its IPI analog (tool-call allowlist,
      capability scoping, output filtering on tool arguments)
    - Citation: At least one concrete reference — e.g., "OWASP API Security Top 10 API10:2023",
      "CWE-918", Capital One 2019 incident, or a Phase reference

  Row 3 (CSP ↔ Context Sanitization):
    - Web Attack Class: "Content-Security-Policy (CSP) — a defensive control, framed as
      the dual of stored XSS"
    - IPI Analog: "Context Sanitization Layer (BERT-based classifier from Phase 3.1)"
    - Mechanism: Must describe the parallel — both are declarative, centralized policy
      layers that constrain what downstream interpreter (browser / LLM) will execute
      when given untrusted content from a trusted source
    - Defensive Control: This row IS about the defense — note the Phase 3.1 FusedDefense
      classifier as the IPI analog and describe what it filters (injection-bearing chunks
      before LLM prompt assembly)
    - Citation: At least one concrete reference — e.g., "W3C CSP Level 3 specification",
      "OWASP CSP Cheat Sheet", "Phase 3.1 FusedDefense results (03.1-SUMMARY.md)"

The author MAY add additional taxonomy rows at their discretion (e.g., "SQL Injection ↔
Prompt Template Injection", "CSRF ↔ Cross-Agent Request"), but the three above are
REQUIRED and must each carry a non-empty citation. Do NOT use placeholders like "TBD",
"?", or empty cells for any of the five columns in the three required rows.

Keep total length between 400 and 1500 words (enough to explain the mappings, short enough
to embed directly into the Phase 3.4 writeup as a single section).
  </action>
  <verify>
    <automated>pytest tests/test_eval08_taxonomy.py -x 2>/dev/null || (test -f docs/xss_ssrf_taxonomy.md && grep -q "Stored" docs/xss_ssrf_taxonomy.md && grep -q "SSRF" docs/xss_ssrf_taxonomy.md && grep -q -i "content.security.policy\|CSP" docs/xss_ssrf_taxonomy.md && grep -q -i "corpus poisoning" docs/xss_ssrf_taxonomy.md && grep -q -i "tool.call\|tool-call" docs/xss_ssrf_taxonomy.md && grep -q -i "context sanitization" docs/xss_ssrf_taxonomy.md)</automated>
  </verify>
  <done>
    docs/xss_ssrf_taxonomy.md exists, Markdown-valid, contains the three required mapping
    rows with all five columns populated and non-empty citations, and includes at least
    one reference to Phase 2.2 / 3.1 project artifacts or a named external source
    (OWASP / CWE / CVE / published paper) per row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → repo | Author (Claude executor) writes Markdown file into the project repo. No runtime code path; the file is a static artifact consumed by human readers and paper-writing tools. |
| reader (LLM paper-writer) → file | Downstream phase (3.4 writeup) may ingest the file as context. Adversarial content here would be self-inflicted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.3-06-01 | Tampering | docs/xss_ssrf_taxonomy.md | accept | File is a static project artifact tracked by git; any tampering would be visible in diff review. No runtime execution path touches this file. |
| T-3.3-06-02 | Information Disclosure | docs/xss_ssrf_taxonomy.md | mitigate | Ensure the taxonomy does NOT include secret project internals (API keys, internal file paths outside `.planning/` / `scripts/` / `rag/`). Only reference public-facing project phase names and standard external sources (OWASP/CWE/CVE). |
| T-3.3-06-03 | Repudiation | citation column | mitigate | Every citation must be concrete (OWASP section #, CWE #, CVE #, or explicit Phase-N-SUMMARY reference) — no vague "as discussed earlier" phrasing. This is enforced by the acceptance-criteria check that citations are non-empty. |
</threat_model>

<verification>
After completion, run:

  grep -c '^|' docs/xss_ssrf_taxonomy.md

Must return at least 5 (header row + separator + 3 data rows = 5 lines with leading `|`).

Sanity-check that each of the three required rows contains a Phase reference or OWASP/CWE
reference (no placeholder citations).
</verification>

<success_criteria>
- docs/xss_ssrf_taxonomy.md exists and is valid Markdown
- Taxonomy table contains all three required D-18 mapping rows
- Each required row has all 5 columns populated with substantive content
- Each required row contains at least one concrete external citation or project-phase reference
- File length is between 400 and 1500 words
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-quick-evaluation-additions/03.3-06-SUMMARY.md`.
</output>
