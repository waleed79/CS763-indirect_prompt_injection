---
phase: 04-final-presentation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - tests/test_phase4_assets.py
  - .planning/phases/04-final-presentation/04-WAVE0-NOTES.md
autonomous: true
requirements: [PRES-02, PRES-03]
must_haves:
  truths:
    - "qrcode[pil]==8.2 is importable in the rag-security conda env"
    - "tests/test_phase4_assets.py exists and pytest can collect it (skips OK pre-implementation)"
    - "All 5 existing PNGs (figures/fig1..fig5) are confirmed present"
    - "Verified GitHub repo URL is captured for QR encoding (Wave 1 input)"
  artifacts:
    - path: "requirements.txt"
      provides: "qrcode[pil]==8.2 dependency line for RAG-05 reproducibility"
      contains: "qrcode[pil]==8.2"
    - path: "tests/test_phase4_assets.py"
      provides: "Phase 4 asset smoke-test stubs (TestMakeDiagramsSmoke, TestMakeQrSmoke, TestPhase4AssetsOnDisk)"
      min_lines: 80
    - path: ".planning/phases/04-final-presentation/04-WAVE0-NOTES.md"
      provides: "Captured GitHub repo URL + figure inventory + course-template check result"
      contains: "https://github.com/waleed79/CS763-indirect_prompt_injection"
  key_links:
    - from: "tests/test_phase4_assets.py"
      to: "scripts/make_diagrams.py / scripts/make_qr.py"
      via: "importlib.util.spec_from_file_location with try/except guard"
      pattern: "spec_from_file_location"
---

<objective>
Wave 0 setup for Phase 4 (Final Presentation).

Purpose: Install the one new pip dep (qrcode[pil]==8.2), create the test scaffold so Wave 1 scripts can land RED→GREEN, verify the 5 prior-phase figure assets are still on disk, capture the verified GitHub URL for Wave 1 QR encoding, and check Canvas/TA for any course-specific poster template (RESEARCH §Open Questions Q1).

Output: requirements.txt updated, tests/test_phase4_assets.py committed (tests SKIP cleanly until Wave 1 scripts exist), and 04-WAVE0-NOTES.md committed with verified URL, figure inventory, and template-check result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-final-presentation/04-CONTEXT.md
@.planning/phases/04-final-presentation/04-RESEARCH.md
@.planning/phases/04-final-presentation/04-PATTERNS.md
@.planning/phases/04-final-presentation/04-VALIDATION.md
@tests/test_make_figures.py
@requirements.txt
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Install qrcode[pil]==8.2 and append to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (find the right place to append; mirror existing pin format `pkg==X.Y.Z`)
    - .planning/phases/04-final-presentation/04-RESEARCH.md (§Standard Stack — qrcode 8.2 verified PyPI latest 2026-05-02; §Runtime State Inventory line 318 — RAG-05 reproducibility)
    - .planning/phases/04-final-presentation/04-PATTERNS.md (§requirements.txt analog)
    - CLAUDE.md (conda env: rag-security; conda run -n rag-security python ... pattern)
  </read_first>
  <action>
    1. Run `conda run -n rag-security pip install "qrcode[pil]==8.2"` (Bash). Confirm exit 0 and that pip resolves Pillow (already 12.2.0 — no upgrade expected).
    2. Verify the import works: `conda run -n rag-security python -c "import qrcode; print(qrcode.__version__)"` must print `8.2`.
    3. Append exactly one line `qrcode[pil]==8.2` to requirements.txt (use Edit tool — keep alphabetical or append-to-end consistent with existing file style; do NOT reorder other lines).
    4. Confirm with `grep -n "qrcode" requirements.txt` that the line landed once (not duplicated).
    Per RESEARCH §Don't Hand-Roll: never write a custom QR encoder; use the qrcode lib.
  </action>
  <verify>
    <automated>conda run -n rag-security python -c "import qrcode; assert qrcode.__version__ == '8.2'" && grep -c "^qrcode\[pil\]==8.2$" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n rag-security python -c "import qrcode; print(qrcode.__version__)"` prints `8.2`
    - `grep -c "^qrcode\[pil\]==8.2$" requirements.txt` returns `1`
    - No other lines in requirements.txt were reordered or removed (diff shows +1 line only)
  </acceptance_criteria>
  <done>qrcode 8.2 importable in rag-security env; requirements.txt has the new pin appended once.</done>
</task>

<task type="auto">
  <name>Task 1.2: Create tests/test_phase4_assets.py — Wave 0 stubs (3 test classes, importlib guards)</name>
  <files>tests/test_phase4_assets.py</files>
  <read_first>
    - tests/test_make_figures.py (canonical Wave-0 importlib stub pattern, lines 1-26)
    - .planning/phases/04-final-presentation/04-PATTERNS.md (§tests/test_phase4_assets.py — full skeleton, 3 classes split, naming conventions)
    - .planning/phases/04-final-presentation/04-RESEARCH.md (§Validation Architecture lines 555-599 — recommended test bodies including pyzbar optional skip)
    - .planning/phases/04-final-presentation/04-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - pytest.ini (Test* / test_* conventions, 60s timeout)
  </read_first>
  <action>
    Create tests/test_phase4_assets.py with this exact structure:

    Header docstring: "Wave 0 stubs for Phase 4 assets (scripts/make_diagrams.py, scripts/make_qr.py + on-disk artifacts). Skips until Wave 1 lands."

    Imports: `from __future__ import annotations`; `importlib.util`; `sys`; `pathlib.Path`; `pytest`.

    Module-level loader (mirror tests/test_make_figures.py:12-26 pattern):
    ```python
    _SCRIPTS_DIR = Path(__file__).parent.parent / "scripts"
    def _try_load(modname: str):
        try:
            spec = importlib.util.spec_from_file_location(modname, _SCRIPTS_DIR / f"{modname}.py")
            mod = importlib.util.module_from_spec(spec)
            spec.loader.exec_module(mod)
            return mod
        except (AttributeError, FileNotFoundError, Exception):
            return None
    _diag = _try_load("make_diagrams")
    _qr   = _try_load("make_qr")
    _DIAG_OK = _diag is not None
    _QR_OK   = _qr   is not None
    FIG = Path("figures")
    ```

    Class 1 — `TestMakeDiagramsSmoke`:
      - Class const: `EXPECTED_PNGS = ["diagram_a_rag_pipeline.png", "diagram_b_defense_pipeline.png"]`
      - `@pytest.mark.skipif(not _DIAG_OK, reason="scripts/make_diagrams.py not yet implemented (Wave 1)")` on the two run-tests
      - `test_main_runs_with_no_args(self, tmp_path)` — calls `_diag.main(["--output-dir", str(tmp_path)])`, asserts rc == 0
      - `test_both_diagrams_emitted(self, tmp_path)` — calls main, then for each name in EXPECTED_PNGS asserts (tmp_path/name).exists() and stat().st_size > 1024
      - `test_agg_backend_used(self)` — `import matplotlib; assert matplotlib.get_backend().lower() == "agg"` (no skipif — backend should be Agg even if make_diagrams.py absent, because make_figures.py sets it; if neither has been imported yet this test will detect that and SHOULD also be guarded by `@pytest.mark.skipif(not _DIAG_OK, reason="diagram script not loaded")`)

    Class 2 — `TestMakeQrSmoke`:
      - `@pytest.mark.skipif(not _QR_OK, reason="scripts/make_qr.py not yet implemented (Wave 1)")` on both run-tests
      - `test_main_runs_with_default_args(self, tmp_path)` — calls `_qr.main(["--output", str(tmp_path / "qr_github.png")])`, asserts rc == 0
      - `test_qr_decodes_to_repo_url(self, tmp_path)` — calls main, then attempts `from pyzbar.pyzbar import decode`; if ImportError → `pytest.skip("pyzbar not installed; manual phone-scan substitutes")`; else opens (tmp_path/"qr_github.png") with PIL.Image, decodes, asserts substring `github.com/waleed79/CS763-indirect_prompt_injection` is in decoded[0].data.decode()

    Class 3 — `TestPhase4AssetsOnDisk` (asset-existence checks for FINAL artifacts in figures/ — these will SKIP until Wave 1 / Wave 1 demo are committed; mark each with `@pytest.mark.skipif(not (FIG / "<filename>").exists(), reason="...not yet produced (Wave 1)")` so they SKIP rather than FAIL during Wave 0):
      - `test_minimum_two_figures_present` — `pngs = list(FIG.glob("*.png")); assert len(pngs) >= 2` (this should PASS at Wave 0 because fig1..fig5 already exist; do NOT skipif this one)
      - `test_diagram_a_present` — assert `(FIG / "diagram_a_rag_pipeline.png").exists()` (skipif until exists)
      - `test_diagram_b_present` — assert `(FIG / "diagram_b_defense_pipeline.png").exists()` (skipif until exists)
      - `test_qr_present_and_nonempty` — assert exists and size > 0 (skipif until exists)
      - `test_demo_gif_present_and_animated` — assert exists, size > 50_000, size < 2_000_000, and `PIL.Image.open(...).n_frames > 1` (skipif until exists)

    Run `conda run -n rag-security python -m pytest tests/test_phase4_assets.py --collect-only -q` to confirm pytest collects every test cleanly (no import errors). Then run `conda run -n rag-security python -m pytest tests/test_phase4_assets.py -x --quiet` and confirm: `test_minimum_two_figures_present` PASSES; all others SKIP.

    Naming convention applied uniformly (PATTERNS.md flagged conflict): `diagram_a_rag_pipeline.png`, `diagram_b_defense_pipeline.png`, `qr_github.png`, `demo_tier2_mistral.gif`. This is the SINGLE convention used across all Phase 4 plans (scripts, tests, slide embeds).
  </action>
  <verify>
    <automated>conda run -n rag-security python -m pytest tests/test_phase4_assets.py --collect-only -q 2>&1 | tail -5 && conda run -n rag-security python -m pytest tests/test_phase4_assets.py -x --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_phase4_assets.py` exits 0 and reports >= 8 collected tests
    - `pytest tests/test_phase4_assets.py -x --quiet` shows `test_minimum_two_figures_present` PASSED
    - All other tests show SKIPPED (not FAILED, not ERROR)
    - `grep -c "diagram_a_rag_pipeline.png" tests/test_phase4_assets.py` >= 1 (consistent naming)
    - `grep -c "diagram_b_defense_pipeline.png" tests/test_phase4_assets.py` >= 1
    - `grep -c "qr_github.png" tests/test_phase4_assets.py` >= 1
    - `grep -c "demo_tier2_mistral.gif" tests/test_phase4_assets.py` >= 1
    - File contains 3 class definitions: `TestMakeDiagramsSmoke`, `TestMakeQrSmoke`, `TestPhase4AssetsOnDisk` (grep `^class Test` returns 3 lines)
  </acceptance_criteria>
  <done>Test scaffold committed; pytest collects clean; only the truly-already-satisfied test passes, the rest SKIP via importlib/exists guards.</done>
</task>

<task type="auto">
  <name>Task 1.3: Capture verified URL + figure inventory + course template check (04-WAVE0-NOTES.md)</name>
  <files>.planning/phases/04-final-presentation/04-WAVE0-NOTES.md</files>
  <read_first>
    - .planning/phases/04-final-presentation/04-RESEARCH.md (§Open Questions Q1 course poster template; §Pitfall 5 verify URL with `git remote get-url origin`; §Security Domain V4 — make repo PUBLIC before printing)
    - .planning/phases/04-final-presentation/04-CONTEXT.md (§Specifics — verify actual URL before generating)
  </read_first>
  <action>
    1. Run `git remote get-url origin` and capture exact output.
    2. Strip `.git` suffix to derive the QR-encoded URL (the URL a phone visits).
    3. Run `ls figures/*.png` and count; confirm the 5 prior-phase PNGs (fig1_arms_race.png, fig2_utility_security.png, fig3_loo_causal.png, fig4_ratio_sweep.png, fig5_cross_model_heatmap.png) are all present and >0 bytes.
    4. Run `gh repo view waleed79/CS763-indirect_prompt_injection --json visibility -q .visibility` (or `gh repo view --json visibility`) and capture the visibility (PUBLIC required before printing per RESEARCH §Pitfall 5 / §Security V4). If gh CLI not authenticated, capture the manual fallback step ("verify on github.com that repo is Public; if Private, run `gh repo edit --visibility public`").
    5. Course poster template check (5-min task, RESEARCH §Open Questions Q1): browse the existing repo for any pre-existing course template hint. Run `ls -la "Final Project Details.pptx" 2>/dev/null` to check if there's a course-supplied PowerPoint template, and `grep -ri "poster.template\|poster.size\|36x48" --include="*.md" .planning/ 2>/dev/null | head -5` for any prior mention. If nothing found, document fallback decision: "no course template found, defaulting to standard 36×48" landscape per CONTEXT D-14".

    Write `.planning/phases/04-final-presentation/04-WAVE0-NOTES.md` with sections:
      - `# Phase 4 Wave 0 Notes` (top-level header)
      - `## Verified GitHub Repo URL` — exact `git remote get-url origin` output AND the stripped QR-encoded URL (`https://github.com/waleed79/CS763-indirect_prompt_injection`); note "captured 2026-05-02"
      - `## Repo Visibility` — `PUBLIC` or `PRIVATE` from `gh repo view`; if PRIVATE, add **WARNING: must be made PUBLIC before printing poster QR (RESEARCH §Pitfall 5)** in bold; note any manual remediation step
      - `## Existing Figures Inventory` — bulleted list of the 5 PNGs with `ls -la` size/mtime per file
      - `## Course Poster Template Check` — search results; final decision (use course template or default to 36×48" custom size per D-14); cite the exact files / commands used in the search
      - `## Wave 1 Inputs Locked` — single-paragraph summary listing the URL Wave 1's make_qr.py must encode and the naming convention (`diagram_a_rag_pipeline.png`, `diagram_b_defense_pipeline.png`, `qr_github.png`, `demo_tier2_mistral.gif`)
  </action>
  <verify>
    <automated>test -f .planning/phases/04-final-presentation/04-WAVE0-NOTES.md && grep -c "github.com/waleed79/CS763-indirect_prompt_injection" .planning/phases/04-final-presentation/04-WAVE0-NOTES.md && ls figures/fig1_arms_race.png figures/fig2_utility_security.png figures/fig3_loo_causal.png figures/fig4_ratio_sweep.png figures/fig5_cross_model_heatmap.png 2>&1 | grep -c "^figures/fig"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-final-presentation/04-WAVE0-NOTES.md` exists and is >800 bytes
    - Contains the literal URL `https://github.com/waleed79/CS763-indirect_prompt_injection` at least twice (raw + QR-encoded sections)
    - Contains the heading `## Repo Visibility` and a value (`PUBLIC` or `PRIVATE` with WARNING)
    - Contains the heading `## Existing Figures Inventory` with all 5 figure filenames listed
    - Contains the heading `## Course Poster Template Check` with explicit decision (use template / use 36×48 default)
    - All 5 prior-phase PNGs (fig1..fig5) confirmed present in figures/
  </acceptance_criteria>
  <done>Verified URL captured, repo visibility known, figure inventory recorded, template question resolved — Wave 1 has every input it needs.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- `conda run -n rag-security python -c "import qrcode; print(qrcode.__version__)"` prints `8.2`
- `conda run -n rag-security python -m pytest tests/test_phase4_assets.py -x --quiet` shows 1 PASSED + N SKIPPED, 0 FAILED, 0 ERROR
- `cat .planning/phases/04-final-presentation/04-WAVE0-NOTES.md` shows verified URL + visibility + inventory + template decision
</verification>

<success_criteria>
- New dep installed and pinned (RAG-05 reproducibility)
- Test scaffold lands committed and clean (RED→GREEN ready for Wave 1)
- Verified GitHub URL captured for QR encoding (un-blocks Plan 03 in Wave 1)
- Course poster template question definitively resolved (un-blocks Plan 06/07 layout decisions)
- Naming convention chosen: `diagram_a_rag_pipeline.png`, `diagram_b_defense_pipeline.png`, `qr_github.png`, `demo_tier2_mistral.gif` — applied uniformly across this plan and all downstream Phase 4 plans
</success_criteria>

<output>
Create `.planning/phases/04-final-presentation/04-01-SUMMARY.md` with:
- qrcode install confirmation + import test output
- pytest collect-only output + pytest run output
- Full content of 04-WAVE0-NOTES.md
- Repo visibility status (PUBLIC/PRIVATE) and any remediation needed before Plan 06 prints the poster
</output>
