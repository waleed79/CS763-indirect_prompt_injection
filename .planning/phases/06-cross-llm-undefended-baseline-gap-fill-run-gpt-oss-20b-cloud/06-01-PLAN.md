---
phase: 06
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_phase6_eval.py
  - tests/test_make_results_v6.py
autonomous: true
requirements:
  - P6-PRE
  - P6-RUN-20b-UND
  - P6-RUN-120b-UND
  - P6-PRO
  - P6-DEF
  - P6-MTX
  - P6-RES-PR
  - P6-MD
  - P6-CSV
  - P6-D12-FUSED
  - P6-D12-UND
  - P6-D03
  - P6-AUTH
  - P6-ERR
  - P6-ENC
requirements_addressed:
  - P6-PRE
  - P6-RUN-20b-UND
  - P6-RUN-120b-UND
  - P6-PRO
  - P6-DEF
  - P6-MTX
  - P6-RES-PR
  - P6-MD
  - P6-CSV
  - P6-D12-FUSED
  - P6-D12-UND
  - P6-D03
  - P6-AUTH
  - P6-ERR
  - P6-ENC

must_haves:
  truths:
    - "pytest --collect-only tests/test_phase6_eval.py succeeds without ImportError"
    - "pytest --collect-only tests/test_make_results_v6.py succeeds without ImportError"
    - "Every Wave 1+ requirement (P6-* IDs above) has at least one stub test class that maps to it"
    - "All test methods initially skip via pytestmark.skipif(not _AVAILABLE, ...) when production code does not yet exist"
  artifacts:
    - path: "tests/test_phase6_eval.py"
      provides: "Stub classes: TestSanityAssert, TestUndefendedRuns20b, TestUndefendedRuns120b, TestProvenanceFields, TestDefendedRuns, TestMatrixSummary, TestD12FusedV6, TestD12UndefendedV6, TestD03V6, TestPreflightCloudAuth, TestErrorPolicy, TestEncodingExplicit"
      contains: "_AVAILABLE flag, importlib.util.spec_from_file_location, pytestmark.skipif"
    - path: "tests/test_make_results_v6.py"
      provides: "Stub classes: TestPathResolver, TestMarkdownDisclosure, TestCsvCompanions"
      contains: "_AVAILABLE flag pattern"
  key_links:
    - from: "tests/test_phase6_eval.py"
      to: "scripts/run_phase6_eval.py"
      via: "importlib.util.spec_from_file_location guarded by try/except FileNotFoundError"
      pattern: "spec_from_file_location.*run_phase6_eval"
    - from: "tests/test_make_results_v6.py"
      to: "scripts/make_results.py"
      via: "importlib.util.spec_from_file_location"
      pattern: "spec_from_file_location.*make_results"
---

<objective>
Wave 0 test stub creation. Create pytest skeletons for Phase 6 production code that does not yet exist (Wave 1+). Stubs must be collectable by `pytest --collect-only` (no ImportError) and skip cleanly until the modules they target land.

Purpose: Lock the verification contract before implementation. Every P6-* requirement gets a test class with a clear name that ties to a CONTEXT.md decision. Wave 1 implementers can then run `pytest -k <test_name>` to validate their work as they go.

Output: Two new test files, both pytest-collectable, all tests currently SKIP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-CONTEXT.md
@.planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-PATTERNS.md
@.planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-VALIDATION.md
@tests/test_make_results.py
@tests/test_phase4_assets.py
@tests/test_judge_fpr.py

<interfaces>
<!-- Pattern from tests/test_make_results.py:1-26 (importlib stub) -->
<!-- Pattern from tests/test_phase4_assets.py (multi-class smoke + on-disk PNG checks) -->

```python
# Established stub pattern (REUSE VERBATIM):
import importlib.util
import sys
from pathlib import Path
import pytest

_SCRIPTS_DIR = Path(__file__).parent.parent / "scripts"
try:
    _spec = importlib.util.spec_from_file_location(
        "<module_name>", _SCRIPTS_DIR / "<module_file>.py"
    )
    _mod = importlib.util.module_from_spec(_spec)
    _spec.loader.exec_module(_mod)
    main = _mod.main  # also import any module-level helpers tests need
    _AVAILABLE = True
except (AttributeError, FileNotFoundError):
    _AVAILABLE = False
except Exception:
    import traceback; traceback.print_exc()
    _AVAILABLE = False

pytestmark = pytest.mark.skipif(
    not _AVAILABLE, reason="<scripts/X.py> not yet implemented (Wave 1)"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_phase6_eval.py with all 12 stub classes</name>
  <read_first>
    - tests/test_make_results.py (full read — copy importlib stub pattern from lines 1-26)
    - tests/test_phase4_assets.py (full read — copy multi-class TestSmoke + on-disk file existence pattern)
    - tests/test_judge_fpr.py (full read — copy extended `_AVAILABLE` pattern with traceback printing for non-FileNotFoundError exceptions)
    - .planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-PATTERNS.md (full read — sections "tests/test_phase6_eval.py" lines 171-251, "Cross-cutting: importlib stub with `_AVAILABLE` flag" lines 789-808)
    - .planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-VALIDATION.md (full read — Per-Task Verification Map for exact test class names)
  </read_first>
  <behavior>
    - pytest --collect-only tests/test_phase6_eval.py exits 0 (no ImportError)
    - All test methods skip with reason "scripts/run_phase6_eval.py not yet implemented (Wave 1)" because the production module does not exist yet
    - Test class names match VALIDATION.md exactly: TestSanityAssert, TestUndefendedRuns20b, TestUndefendedRuns120b, TestProvenanceFields, TestDefendedRuns, TestMatrixSummary, TestD12FusedV6, TestD12UndefendedV6, TestD03V6, TestPreflightCloudAuth, TestErrorPolicy, TestEncodingExplicit
    - Each test class has at least one test method (sufficient to be collected)
    - File header docstring identifies it as "Wave 0 stub for Phase 6"
  </behavior>
  <action>
Create file `tests/test_phase6_eval.py` with the EXACT importlib stub pattern from `tests/test_make_results.py:1-26` extended with the traceback-printing fallback from `tests/test_judge_fpr.py:8-51`. Concrete content:

```python
"""Wave 0 stub for scripts/run_phase6_eval.py (Wave 1) and Phase 6 figure renderers.

Skips until run_phase6_eval.py and make_figures.py v6 renderers are implemented.
Maps 1:1 onto P6-* requirements in 06-VALIDATION.md.
"""
from __future__ import annotations
import importlib.util
import json
import sys
from pathlib import Path

import pytest

_SCRIPTS_DIR = Path(__file__).parent.parent / "scripts"
try:
    _spec = importlib.util.spec_from_file_location(
        "run_phase6_eval", _SCRIPTS_DIR / "run_phase6_eval.py"
    )
    _mod = importlib.util.module_from_spec(_spec)
    _spec.loader.exec_module(_mod)
    main = _mod.main
    _AVAILABLE = True
except (AttributeError, FileNotFoundError):
    _AVAILABLE = False
except Exception:
    import traceback
    traceback.print_exc()
    _AVAILABLE = False

pytestmark = pytest.mark.skipif(
    not _AVAILABLE,
    reason="scripts/run_phase6_eval.py not yet implemented (Wave 1)",
)


# --- P6-PRE: D-01a sanity assert -----------------------------------------
class TestSanityAssert:
    """P6-PRE: assert_collection_has_all_tiers raises on stale collection."""
    def test_missing_tier_range_raises(self):
        pytest.skip("Wave 1: implement assert_collection_has_all_tiers + mock chromadb")


# --- P6-RUN-20b-UND, P6-RUN-120b-UND -------------------------------------
class TestUndefendedRuns20b:
    """P6-RUN-20b-UND: Driver emits logs/eval_harness_undefended_gptoss20b_v6.json."""
    @pytest.mark.skipif(
        not Path("logs/eval_harness_undefended_gptoss20b_v6.json").exists(),
        reason="Wave 2 cloud run not yet executed",
    )
    def test_undefended_20b_log_exists(self):
        path = Path("logs/eval_harness_undefended_gptoss20b_v6.json")
        data = json.loads(path.read_text(encoding="utf-8"))
        assert "aggregate" in data
        for k in ["asr_tier1", "asr_tier1b", "asr_tier2", "asr_tier3", "asr_tier4"]:
            assert k in data["aggregate"], f"missing aggregate key: {k}"


class TestUndefendedRuns120b:
    """P6-RUN-120b-UND: Driver emits logs/eval_harness_undefended_gptoss120b_v6.json."""
    @pytest.mark.skipif(
        not Path("logs/eval_harness_undefended_gptoss120b_v6.json").exists(),
        reason="Wave 2 cloud run not yet executed",
    )
    def test_undefended_120b_log_exists(self):
        path = Path("logs/eval_harness_undefended_gptoss120b_v6.json")
        data = json.loads(path.read_text(encoding="utf-8"))
        assert "aggregate" in data
        for k in ["asr_tier1", "asr_tier1b", "asr_tier2", "asr_tier3", "asr_tier4"]:
            assert k in data["aggregate"], f"missing aggregate key: {k}"


# --- P6-PRO: provenance fields -------------------------------------------
class TestProvenanceFields:
    """P6-PRO: each _v6.json carries phase=06, supersedes_phase_02_3, error_count."""
    @pytest.mark.skipif(
        not Path("logs/eval_harness_undefended_gptoss20b_v6.json").exists(),
        reason="Wave 2 cloud run not yet executed",
    )
    def test_provenance_fields_present_20b(self):
        data = json.loads(
            Path("logs/eval_harness_undefended_gptoss20b_v6.json")
            .read_text(encoding="utf-8")
        )
        assert data.get("phase") == "06"
        assert data.get("supersedes_phase_02_3") is True
        assert data.get("collection") == "nq_poisoned_v4"
        assert data.get("corpus") == "data/corpus_poisoned.jsonl"
        assert isinstance(data.get("aggregate", {}).get("error_count"), int)


# --- P6-DEF: defended runs -----------------------------------------------
class TestDefendedRuns:
    """P6-DEF: 4 defended _v6 cells under logs/eval_matrix/."""
    @pytest.mark.skipif(
        not Path("logs/eval_matrix/gptoss20b_cloud__fused__all_tiers_v6.json").exists(),
        reason="Wave 2 defended runs not yet executed",
    )
    def test_all_four_defended_cells_exist(self):
        for model_key in ["gptoss20b_cloud", "gptoss120b_cloud"]:
            for defense in ["fused", "def02"]:
                path = Path(f"logs/eval_matrix/{model_key}__{defense}__all_tiers_v6.json")
                assert path.exists(), f"missing defended cell: {path}"


# --- P6-MTX: _summary_v6.json composition --------------------------------
class TestMatrixSummary:
    """P6-MTX: _summary_v6.json contains exactly 75 cells."""
    @pytest.mark.skipif(
        not Path("logs/eval_matrix/_summary_v6.json").exists(),
        reason="Wave 2 summary composition not yet executed",
    )
    def test_summary_has_exactly_75_cells(self):
        rows = json.loads(
            Path("logs/eval_matrix/_summary_v6.json").read_text(encoding="utf-8")
        )
        assert isinstance(rows, list)
        assert len(rows) == 75, f"expected 75 cells, got {len(rows)}"
        for cell in rows:
            for k in ["model", "defense", "tier", "asr_overall", "asr_tier_native",
                      "fpr", "retrieval_rate"]:
                assert k in cell, f"cell missing key {k}: {cell}"


# --- P6-D12-FUSED: 5x5 fused heatmap -------------------------------------
class TestD12FusedV6:
    """P6-D12-FUSED: figures/d12_cross_model_heatmap_v6.png exists, >1KB, shape (5,5) enforced."""
    @pytest.mark.skipif(
        not Path("figures/d12_cross_model_heatmap_v6.png").exists(),
        reason="Wave 4 figure not yet rendered",
    )
    def test_d12_fused_v6_exists_and_sized(self):
        path = Path("figures/d12_cross_model_heatmap_v6.png")
        assert path.exists()
        assert path.stat().st_size > 1024


# --- P6-D12-UND: 5x4 undefended heatmap ----------------------------------
class TestD12UndefendedV6:
    """P6-D12-UND: figures/d12_undefended_v6.png exists, >1KB, shape (5,4) enforced."""
    @pytest.mark.skipif(
        not Path("figures/d12_undefended_v6.png").exists(),
        reason="Wave 4 figure not yet rendered",
    )
    def test_d12_undefended_v6_exists_and_sized(self):
        path = Path("figures/d12_undefended_v6.png")
        assert path.exists()
        assert path.stat().st_size > 1024


# --- P6-D03: arms-race v6 ------------------------------------------------
class TestD03V6:
    """P6-D03: figures/d03_arms_race_v6.png exists and is larger (gpt-oss bars added)."""
    @pytest.mark.skipif(
        not Path("figures/d03_arms_race_v6.png").exists(),
        reason="Wave 4 figure not yet rendered",
    )
    def test_d03_v6_exists_and_sized(self):
        path = Path("figures/d03_arms_race_v6.png")
        assert path.exists()
        assert path.stat().st_size > 1024


# --- P6-AUTH: ollama list pre-flight -------------------------------------
class TestPreflightCloudAuth:
    """P6-AUTH: ollama list shows both gpt-oss models."""
    def test_ollama_list_contains_gptoss_models(self):
        pytest.skip("Wave 2: invoked at run-time by driver, not by unit test")


# --- P6-ERR: error policy ------------------------------------------------
class TestErrorPolicy:
    """P6-ERR: single-query failure does not abort run; error_count incremented."""
    def test_error_policy_increments_count(self):
        pytest.skip("Wave 1: implement post-run mutation + simulate failure path")


# --- P6-ENC: utf-8 encoding everywhere -----------------------------------
class TestEncodingExplicit:
    """P6-ENC: every open()/read_text/write_text in run_phase6_eval.py specifies encoding=utf-8."""
    def test_run_phase6_eval_uses_utf8_explicit(self):
        path = _SCRIPTS_DIR / "run_phase6_eval.py"
        if not path.exists():
            pytest.skip("Wave 1: production file not yet created")
        src = path.read_text(encoding="utf-8")
        # Every read_text / write_text must include encoding="utf-8"
        for needle in ["read_text(", "write_text("]:
            offset = 0
            while True:
                i = src.find(needle, offset)
                if i == -1:
                    break
                # Find matching close paren
                depth = 1
                j = i + len(needle)
                while j < len(src) and depth > 0:
                    if src[j] == "(":
                        depth += 1
                    elif src[j] == ")":
                        depth -= 1
                    j += 1
                call = src[i:j]
                assert "encoding=" in call and "utf-8" in call, (
                    f"P6-ENC violation: {needle}...) without encoding='utf-8' at "
                    f"offset {i}: {call!r}"
                )
                offset = j
```

Use the Write tool to create the file at `tests/test_phase6_eval.py`. Do NOT use heredoc.
  </action>
  <verify>
    <automated>pytest --collect-only tests/test_phase6_eval.py 2>&1 | grep -E "(collected|error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_phase6_eval.py`
    - `pytest --collect-only tests/test_phase6_eval.py` exits 0
    - All 12 test class names present: `grep -c "^class Test" tests/test_phase6_eval.py` returns at least 12
    - Required class names present: `grep -E "class (TestSanityAssert|TestUndefendedRuns20b|TestUndefendedRuns120b|TestProvenanceFields|TestDefendedRuns|TestMatrixSummary|TestD12FusedV6|TestD12UndefendedV6|TestD03V6|TestPreflightCloudAuth|TestErrorPolicy|TestEncodingExplicit)" tests/test_phase6_eval.py | wc -l` returns 12
    - importlib pattern present: `grep -n "spec_from_file_location" tests/test_phase6_eval.py` returns at least 1 hit
    - pytestmark skip present: `grep -n "pytestmark = pytest.mark.skipif" tests/test_phase6_eval.py` returns at least 1 hit
    - All test methods skip when `scripts/run_phase6_eval.py` does not exist (verified by collect-only succeeding without ImportError, since module is not yet present in repo)
  </acceptance_criteria>
  <done>tests/test_phase6_eval.py exists, is collectable by pytest, contains all 12 named TestX classes from VALIDATION.md, uses the importlib _AVAILABLE pattern, and all tests skip cleanly because run_phase6_eval.py does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_make_results_v6.py with 3 stub classes</name>
  <read_first>
    - tests/test_make_results.py (full read — copy `tmp_path` + main(["--output-dir", str(tmp_path)]) pattern from lines 30-65; copy substring assertions from `test_three_source_aggregation` and `test_arms_race_table_emitted`)
    - .planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-PATTERNS.md (lines 255-313 — exact stub patterns for TestPathResolver, TestMarkdownDisclosure, TestCsvCompanions)
    - .planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-VALIDATION.md (lines for P6-RES-PR, P6-MD, P6-CSV)
  </read_first>
  <behavior>
    - pytest --collect-only tests/test_make_results_v6.py exits 0
    - 3 test class names match VALIDATION.md exactly: TestPathResolver, TestMarkdownDisclosure, TestCsvCompanions
    - Tests skip until either (a) Wave 3 make_results.py edits land, or (b) the regenerated docs/results/*.md files exist
    - Existing tests/test_make_results.py is NOT modified — Phase 6 v6 tests live in a separate file (no risk to green regression baseline)
  </behavior>
  <action>
Create file `tests/test_make_results_v6.py`. The module imports `make_results` via importlib (same pattern as test_make_results.py:1-26) so it can call `main(["--logs-dir", str(tmp_path / "logs"), "--output-dir", str(tmp_path / "out")])` against a tmp_path. Concrete content:

```python
"""Wave 0 stub for Phase 6 make_results.py extensions.

Tests path resolver (_v6.json preferred), disclosure header injection,
and _v6.csv companion emission. Skips until Wave 3 edits land.
"""
from __future__ import annotations
import importlib.util
import json
import sys
from pathlib import Path

import pytest

_SCRIPTS_DIR = Path(__file__).parent.parent / "scripts"
try:
    _spec = importlib.util.spec_from_file_location(
        "make_results", _SCRIPTS_DIR / "make_results.py"
    )
    _mod = importlib.util.module_from_spec(_spec)
    _spec.loader.exec_module(_mod)
    main = _mod.main
    _AVAILABLE = True
except (AttributeError, FileNotFoundError):
    _AVAILABLE = False
except Exception:
    import traceback
    traceback.print_exc()
    _AVAILABLE = False

pytestmark = pytest.mark.skipif(
    not _AVAILABLE,
    reason="scripts/make_results.py not importable",
)


# Phase-6 v6 disclosure header literal — should also appear as module
# constant inside scripts/make_results.py once Wave 3 lands. Test asserts on substring.
PHASE6_DISCLOSURE_SUBSTR = "Updated 2026-05-04: Phase 6 cross-LLM gap fill"


def _stage_minimal_inputs(tmp_path: Path) -> tuple[Path, Path]:
    """Stage minimum logs/ and ablation/matrix files so main() runs end-to-end."""
    logs_dir = tmp_path / "logs"
    logs_dir.mkdir(parents=True, exist_ok=True)
    out_dir = tmp_path / "out"
    out_dir.mkdir(parents=True, exist_ok=True)
    # Minimal ablation
    ablation = {"no_defense": {"asr_tier1": 0.05, "asr_tier2": 0.10}}
    (logs_dir / "ablation_table.json").write_text(
        json.dumps(ablation), encoding="utf-8"
    )
    # Minimal matrix
    eval_matrix_dir = logs_dir / "eval_matrix"
    eval_matrix_dir.mkdir(exist_ok=True)
    (eval_matrix_dir / "_summary.json").write_text(
        json.dumps([{"model": "llama3.2_3b", "defense": "no_defense", "tier": "tier1",
                     "asr_overall": 0.0, "asr_tier_native": 0.0,
                     "fpr": 0.0, "retrieval_rate": 0.0}]),
        encoding="utf-8",
    )
    return logs_dir, out_dir


# --- P6-RES-PR: path resolver --------------------------------------------
class TestPathResolver:
    """P6-RES-PR: prefer _v6.json over un-versioned when present."""
    def test_v6_preferred_when_present(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        # Stage BOTH versioned and un-versioned for gptoss20b
        (logs_dir / "eval_harness_undefended_gptoss20b.json").write_text(
            json.dumps({
                "phase": "02.3", "llm_model": "gpt-oss:20b-cloud",
                "n_queries": 100, "aggregate": {
                    "asr_tier1": 0.0, "asr_tier2": 0.5,
                    "retrieval_rate": 0.86,
                }
            }),
            encoding="utf-8",
        )
        (logs_dir / "eval_harness_undefended_gptoss20b_v6.json").write_text(
            json.dumps({
                "phase": "06", "llm_model": "gpt-oss:20b-cloud",
                "supersedes_phase_02_3": True, "n_queries": 100,
                "collection": "nq_poisoned_v4",
                "aggregate": {
                    "asr_tier1": 0.10, "asr_tier1b": 0.20,
                    "asr_tier2": 0.30, "asr_tier3": 0.40,
                    "asr_tier4": 0.50, "retrieval_rate": 0.88,
                    "error_count": 0,
                },
            }),
            encoding="utf-8",
        )
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        md = (out_dir / "undefended_baseline.md").read_text(encoding="utf-8")
        # _v6 number 0.40 (asr_tier3) MUST appear; un-versioned would have shown nothing
        assert "0.40" in md, "v6 numbers not surfaced — resolver did not prefer _v6.json"

    def test_v6_summary_preferred_when_present(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        # Stage BOTH _summary.json and _summary_v6.json
        (logs_dir / "eval_matrix" / "_summary_v6.json").write_text(
            json.dumps([
                {"model": "gpt-oss_20b-cloud", "defense": "fused", "tier": "tier1",
                 "asr_overall": 0.99, "asr_tier_native": 0.99,
                 "fpr": 0.0, "retrieval_rate": 1.0},
            ]),
            encoding="utf-8",
        )
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        md = (out_dir / "cross_model_matrix.md").read_text(encoding="utf-8")
        # 0.99 only appears in _summary_v6.json — assert resolver picked it up
        assert "0.99" in md, "v6 summary not surfaced — resolver did not prefer _summary_v6.json"


# --- P6-MD: disclosure header --------------------------------------------
class TestMarkdownDisclosure:
    """P6-MD: regenerated .md files include the dated disclosure header."""
    def test_undefended_baseline_disclosure_present(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        md = (out_dir / "undefended_baseline.md").read_text(encoding="utf-8")
        assert PHASE6_DISCLOSURE_SUBSTR in md, (
            f"missing disclosure header. md head: {md[:200]!r}"
        )

    def test_arms_race_table_disclosure_present(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        md = (out_dir / "arms_race_table.md").read_text(encoding="utf-8")
        assert PHASE6_DISCLOSURE_SUBSTR in md, (
            f"missing disclosure header. md head: {md[:200]!r}"
        )


# --- P6-CSV: csv companions ----------------------------------------------
class TestCsvCompanions:
    """P6-CSV: undefended_baseline_v6.csv and arms_race_table_v6.csv emitted."""
    def test_undefended_baseline_v6_csv_exists(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        csv_path = out_dir / "undefended_baseline_v6.csv"
        assert csv_path.exists(), "undefended_baseline_v6.csv missing"
        header = csv_path.read_text(encoding="utf-8").splitlines()[0]
        # Post-Phase-6 schema includes T1b/T3/T4 columns
        for col in ["asr_tier1b", "asr_tier3", "asr_tier4"]:
            assert col in header, f"missing column {col} in v6 csv header: {header!r}"

    def test_arms_race_table_v6_csv_exists(self, tmp_path):
        logs_dir, out_dir = _stage_minimal_inputs(tmp_path)
        rc = main(["--logs-dir", str(logs_dir), "--output-dir", str(out_dir)])
        assert rc == 0
        csv_path = out_dir / "arms_race_table_v6.csv"
        assert csv_path.exists(), "arms_race_table_v6.csv missing"
```

Use the Write tool. Do NOT modify existing tests/test_make_results.py.
  </action>
  <verify>
    <automated>pytest --collect-only tests/test_make_results_v6.py 2>&1 | grep -E "(collected|error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_make_results_v6.py`
    - `pytest --collect-only tests/test_make_results_v6.py` exits 0
    - 3 required class names present: `grep -E "class (TestPathResolver|TestMarkdownDisclosure|TestCsvCompanions)" tests/test_make_results_v6.py | wc -l` returns 3
    - Disclosure literal substring present: `grep -c "Updated 2026-05-04: Phase 6 cross-LLM gap fill" tests/test_make_results_v6.py` returns at least 1
    - importlib pattern present: `grep -c "spec_from_file_location" tests/test_make_results_v6.py` returns at least 1
    - Existing tests/test_make_results.py untouched: `git diff tests/test_make_results.py` returns no output
    - tests/test_make_results.py still green: `pytest tests/test_make_results.py -x` exits 0 (regression guard)
  </acceptance_criteria>
  <done>tests/test_make_results_v6.py exists with 3 test classes (TestPathResolver, TestMarkdownDisclosure, TestCsvCompanions), pytest --collect-only succeeds, the disclosure literal "Updated 2026-05-04: Phase 6 cross-LLM gap fill" is referenced as PHASE6_DISCLOSURE_SUBSTR, and existing tests/test_make_results.py is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → test runner | Test stub files run on developer machine via pytest. No external inputs. |
| test runner → filesystem | tmp_path fixture isolates writes to pytest's temporary tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W0-01 | T (Tampering) | tests/test_phase6_eval.py importing scripts/run_phase6_eval.py | mitigate | Use try/except FileNotFoundError pattern; never crash collection if module absent |
| T-06-W0-02 | I (Information disclosure) | Test fixtures writing real query payloads to tmp_path | accept | Phase-6 stubs use minimal stub JSON, no payloads — test_phase6_eval.py only asserts on log files produced by Wave 2 cloud runs |
| T-06-W0-03 | D (Denial of service) | pytest --collect-only hangs on broken stub | mitigate | Wrap import in try/except; pytestmark.skipif gates all tests; no module-level side-effects beyond importlib.util |
</threat_model>

<verification>
After both tasks complete:

```bash
pytest --collect-only tests/test_phase6_eval.py tests/test_make_results_v6.py 2>&1 | tail -10
pytest tests/test_phase6_eval.py tests/test_make_results_v6.py -v 2>&1 | tail -30
pytest tests/test_make_results.py -x  # regression guard
```

Expected:
- collect-only succeeds (no ImportError)
- All Phase 6 tests SKIP (because Wave 1+ production code not yet present)
- test_make_results.py existing tests stay green
</verification>

<success_criteria>
- tests/test_phase6_eval.py exists and is collectable by pytest with all 12 named test classes
- tests/test_make_results_v6.py exists and is collectable with 3 named test classes
- All 12 + 3 = 15 stub tests skip cleanly (no FAILED) when Wave 1 production code is absent
- Existing tests/test_make_results.py still passes — no regression
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-llm-undefended-baseline-gap-fill-run-gpt-oss-20b-cloud/06-01-SUMMARY.md` documenting the stub-test scaffolding and which P6-* requirement each test class maps to.
</output>
