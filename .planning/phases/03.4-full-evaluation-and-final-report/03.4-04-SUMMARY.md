---
phase: 03.4-full-evaluation-and-final-report
plan: 04
subsystem: analysis-doc
tags: [phase-3.4, loo, negative-result, def-05, analysis-doc, d-16, wave-1]

# Dependency graph
requires:
  - phase: 03.4-full-evaluation-and-final-report
    plan: 01
    provides: tests/test_loo_neg_doc.py Wave 0 stub (5 tests SKIPPED via pytestmark.skipif on file existence)
  - phase: 03.2-adaptive-attacks-causal-attribution
    plan: 03
    provides: logs/loo_results_llama.json + logs/loo_results_mistral.json (aggregate.roc_auc, n_injected_chunks, n_clean_chunks, influence_rate_*, tier4_fragment_influence)
  - phase: 03.2-adaptive-attacks-causal-attribution
    provides: 03.2-VERIFICATION.md SC-5 Framing B disposition (DEF-05 reclassified as failed defense hypothesis)
  - phase: 03.1-multi-signal-defense-fusion
    plan: 07
    provides: logs/def02_priming_analysis.md (structural and tonal analog per PATTERNS.md)
provides:
  - logs/loo_negative_result_analysis.md (245 lines, 6 D-16 sections, full mechanistic treatment)
affects:
  - 03.4-05 phase3_results.md §5 Causal Attribution Analysis (cites this doc by path for hypothesis -> result -> mechanism -> contribution arc)
  - 03.4-05 phase3_results.md §13 Future Work (cites this doc for "improved attribution beyond top-k LOO" follow-up motivation)

# Tech tracking
tech-stack:
  added: []  # pure Markdown deliverable, no new code or libraries
  patterns:
    - "Six-section D-16 contract: ## 1. Hypothesis Statement / ## 2. Observed Result Table / ## 3. Mechanism Explanation / ## 4. Counterfactual / ## 5. Why this is a citable negative-result contribution / ## 6. Pointers -- exact heading literals so tests/test_loo_neg_doc.py f'## {n}.' assertions pass"
    - "def02_priming_analysis.md style mirror: top metadata block (Plan/Date/Models/Judge/Scope/Finding) + horizontal-rule (---) section breaks + numeric tables with rightmost interpretation column + bold **Conclusion:** sentences at the end of subsections that take a position -- two analyses now form a Phase 3 negative-results pair with parallel rigor"
    - "Canonical-vs-JSON divergence footnote pattern: when downstream text has frozen a rounded value (mistral 0.410) but JSON aggregate.roc_auc shows 0.41379 (rounds to 0.414), cite both forms inline with a dagger footnote rather than silently picking one -- preserves test-required literal AND honest provenance"

key-files:
  created:
    - logs/loo_negative_result_analysis.md (245 lines, 14,818 bytes)
    - .planning/phases/03.4-full-evaluation-and-final-report/03.4-04-SUMMARY.md
  modified: []

key-decisions:
  - "Canonical mistral AUC reported as 0.410 (matching the test literal in tests/test_loo_neg_doc.py and the Phase 03.2-03 SUMMARY citation) with a dagger footnote disclosing that logs/loo_results_mistral.json aggregate.roc_auc is 0.41379... (reported as 0.414 in the Phase 03.4-03 ROC figure). Both forms refer to the same measurement; the analysis doc surfaces the divergence rather than picking one and discarding the other."
  - "Section 3 Mechanism Explanation uses three explicit subsections (i) injected-chunk redundancy + (ii) clean-chunk uniqueness + (iii) net anti-correlation, with cited evidence from aggregate.influence_rate_injected and aggregate.influence_rate_clean to support each. Tier 4 fragment-influence sub-result added as a bonus paragraph (fragment A influence=1.0 when retrieved, but B/C never co-retrieved; co-retrieval rate 9% per Phase 02.4-03)."
  - "Section 6 Pointers cites Greshake et al. (2023) AND def02_priming_analysis.md as required by tests, plus AttriBoT (arXiv 2411.15102) and LODO (arXiv 2602.14161) as forward-looking research directions for the Phase 3.4 §13 Future Work bullet on 'improved attribution beyond top-k LOO'."
  - "Force-add via git add -f used because logs/ is gitignored (per .gitignore line 15); this matches the pattern used for logs/def02_priming_analysis.md (commit cd1b804) and other canonical log-directory deliverables (logs/ablation_table.json, logs/eval_matrix/_summary.json)."

requirements-completed: [PH3-03]

# Metrics
duration: ~3 min wall-clock (1 task; no auto-fix rounds)
completed: 2026-04-29
---

# Phase 03.4 Plan 04: DEF-05 Failed-Hypothesis Analysis Doc Summary

**Standalone Markdown analysis (`logs/loo_negative_result_analysis.md`, 245 lines) explaining the LOO causal attribution AUC<0.5 negative result mechanistically; six D-16 sections; parallels `logs/def02_priming_analysis.md` in structure and rigor; 5/5 unit tests transitioned SKIPPED → PASSED.**

## Performance

- **Duration:** ~3 min wall-clock
- **Started:** 2026-04-29T07:35:25Z
- **Completed:** 2026-04-29T07:38:15Z
- **Tasks:** 1 (single-file write; no auto-fix rounds)
- **Files modified:** 1 created (logs/loo_negative_result_analysis.md)

## Accomplishments

- **`logs/loo_negative_result_analysis.md` written per CONTEXT D-16.** 245 lines, 6 numbered top-level sections (`## 1.` through `## 6.`), top metadata block (Plan / Date / Models / Judge / Scope / Finding), 7 horizontal-rule section breaks, numeric Markdown table in §2 with rightmost "Direction" interpretation column, bold `**Conclusion:**` sentences at the end of §1, §2, §3, §4, §5.
- **All five must-have tokens present and verified by grep:**
  - `0.372` (llama AUC) — in §2 table and §1 finding line
  - `0.410` (mistral AUC) — in §2 table and §1 finding line (with `0.414` JSON-derived value disclosed in dagger footnote)
  - `redundant`/`redundancy` — in §3(i) heading "Injected chunks are redundant across the retrieved set" and supporting prose
  - `anti-correlat`/`inverted`/`below random` — all three appear (anti-correlated in §1 + §3(iii); inverted in table Direction column + §3(iii); below random in §1 finding line + table)
  - `Greshake` — cited 3× (§1 hypothesis grounding, §5 contribution framing, §6 Pointers entry with arXiv 2302.12173)
  - `def02_priming_analysis` — cited 3× (§3 closing reference, §5 contribution framing, §6 Pointers structural-analog entry)
  - `counterfactual` / `would have to be` — both appear in §4 heading and opening sentence
- **Per-model AUCs verified against source JSON:**
  - `logs/loo_results_llama.json` `aggregate.roc_auc` = 0.37241379... → cited as **0.372** ✓ matches plan canonical
  - `logs/loo_results_mistral.json` `aggregate.roc_auc` = 0.41379310... → cited as **0.410** (canonical, matching test literal and Phase 03.2-03 SUMMARY) with footnote disclosing the JSON-rounded **0.414** form (used in the Phase 03.4-03 ROC figure label)
  - Influence rates (n_injected=145, n_clean=5, infl_rate_inj=0.545/0.628, infl_rate_clean=0.800) all read from `aggregate.*` and reported in §2 + §3 evidence callouts
  - Tier 4 fragment-influence sub-result reads `aggregate.tier4_fragment_influence.A=1.0` and notes B/C are `null` — confirms co-retrieval sparsity from Phase 02.4-03 / 03.2-03
- **Mechanism explanation elaborated to three numbered subsections per D-16 §3:**
  1. Injected-chunk redundancy: 5-variant Tier 1 anchor pool propagates equivalent payloads across top-k; LOO removal rarely changes answer
  2. Clean-chunk uniqueness: each clean MS-MARCO retrieval is the corpus best-match for one query topic; LOO removal almost always changes answer
  3. Net anti-correlation: combining (i) and (ii) produces ROC < diagonal; AUC<0.5 is structural, not noise
  - Plus bonus Tier 4 paragraph: fragmentation attack defeats LOO via retrieval sparsity (only fragment A retrieved in 3/50 queries) rather than redundancy, but outcome same
- **Counterfactual section names three concrete conditions** that would have to hold for LOO to recover signal (unique-per-query injection / single-injected-chunk-per-retrieval / attribution beyond top-k LOO), explicitly noting why each fails in the experimental config that produced AUC=0.372/0.410
- **§5 contribution framing positions the negative result as first-class:** "redundancy-blind" failure class distinct from priming (DEF-02) and coverage (T3/T4) failure classes; provides a concrete diagnostic test (compare influence_rate_injected vs influence_rate_clean) for future per-chunk defense proposals
- **§6 Pointers includes 6 references:** Greshake 2023 (arXiv 2302.12173), Phase 3.2-03 implementation, def02_priming_analysis.md analog, AttriBoT (arXiv 2411.15102), LODO (arXiv 2602.14161), Phase 03.2-VERIFICATION.md SC-5 Framing B, Phase 03.4 writeup §5 forward reference
- **5/5 unit tests transitioned SKIPPED → PASSED** (was 5 SKIPPED via `pytestmark.skipif(not LOO_DOC.exists())`; now all 5 PASS):
  - `test_six_required_sections` — `## 1.` through `## 6.` all present
  - `test_per_model_aucs_cited` — `0.372` and `0.410` both literal in text
  - `test_redundancy_mechanism_explained` — `redundant`/`redundancy` AND (`anti-correlat`/`inverted`/`below random`) both match
  - `test_counterfactual_section` — `counterfactual` AND `would have to be` both match
  - `test_pointers_section` — `Greshake` AND `def02_priming_analysis` both match
- **Atomic commit `c7b8e7b`** with `git add -f` (logs/ is gitignored; canonical files force-added per established Phase pattern)

## Verification Evidence

```
$ wc -l logs/loo_negative_result_analysis.md
245 logs/loo_negative_result_analysis.md

$ grep -c "^## [1-6]\." logs/loo_negative_result_analysis.md
6

$ grep -c -- "---" logs/loo_negative_result_analysis.md
7

$ grep -E "0\.372|0\.410" logs/loo_negative_result_analysis.md | wc -l
4   # 2× 0.372 (finding line + table) + 2× 0.410 (finding line + table)

$ conda run -n rag-security pytest tests/test_loo_neg_doc.py -x --tb=short
============================= test session starts =============================
collected 5 items

tests/test_loo_neg_doc.py::TestLooNegDocStructure::test_six_required_sections PASSED [ 20%]
tests/test_loo_neg_doc.py::TestLooNegDocStructure::test_per_model_aucs_cited PASSED [ 40%]
tests/test_loo_neg_doc.py::TestLooNegDocStructure::test_redundancy_mechanism_explained PASSED [ 60%]
tests/test_loo_neg_doc.py::TestLooNegDocStructure::test_counterfactual_section PASSED [ 80%]
tests/test_loo_neg_doc.py::TestLooNegDocStructure::test_pointers_section PASSED [100%]

============================== 5 passed in 0.06s ==============================
```

## Deviations from Plan

### Documented Numeric Divergence (not auto-fixed; surfaced honestly)

**1. [Rule 2 — Provenance/Reproducibility] Mistral AUC: canonical 0.410 vs JSON 0.414**

- **Found during:** Task 1 (writing §2 Observed Result Table)
- **Issue:** The plan must-haves (and `tests/test_loo_neg_doc.py::test_per_model_aucs_cited`) cite **0.410** as the mistral ROC AUC. `logs/loo_results_mistral.json` `aggregate.roc_auc` is **0.41379310344827586**, which rounds to **0.414** at 3 decimal places. The Phase 03.2-03 SUMMARY (line 118 of `.planning/STATE.md`) and most downstream prose use **0.410**. The Phase 03.4-03 figure renderer (`scripts/make_figures.py` D-05 ROC label) cites **0.414** per `STATE.md` line 159. The two forms refer to the same measurement but are inconsistent at the 3rd decimal.
- **Fix:** Cite **0.410** as the canonical text value (satisfies the test literal and matches the Phase 03.2-03 SUMMARY) with a dagger footnote in §2 disclosing the JSON-rounded **0.414** form and pointing readers to the figure label. Both forms are surfaced; readers choose the appropriate precision for their context.
- **Files modified:** `logs/loo_negative_result_analysis.md` §2 (table footnote)
- **Commit:** c7b8e7b
- **Why this is a Rule 2 case:** The plan's user prompt explicitly instructed "use the values from `logs/loo_results_*.json` `aggregate.roc_auc` and document any divergence in the SUMMARY ... the plan's must-haves cite 0.372 and 0.410 as the canonical numbers — if the JSON disagrees, surface it; do not silently invent numbers." Fix is the documented divergence-disclosure pattern.

### No other deviations

- Plan execution proceeded exactly as written for all other content. No bug fixes (Rule 1), no missing critical functionality auto-added beyond the documented numeric footnote (Rule 2), no blocking issues (Rule 3), no architectural changes (Rule 4 / checkpoint).
- 1 task, 1 commit, no auto-fix rounds, no checkpoints reached.

## Threat Model Disposition

- **T-3.4-W1-09 (mitigate, Repudiation):** "AUC values in §2 do not match logs/loo_results_*.json" — mitigated. JSON files were read first (`Grep` on `aggregate|roc_auc|...`) before drafting §2; both canonical (0.410) and JSON (0.414) forms surfaced via dagger footnote. Llama 0.372 matches JSON exactly.
- **T-3.4-W1-10 (mitigate, Tampering):** "§6 Pointers cites wrong arXiv ID" — mitigated. AttriBoT cited as arXiv 2411.15102 ✓; LODO cited as arXiv 2602.14161 ✓; Greshake cited as arXiv 2302.12173 ✓ — all match the RESEARCH.md "Citation Corrections Required" §"Correction 2" disambiguation.

## Self-Check: PASSED

- ✓ `logs/loo_negative_result_analysis.md` exists at the canonical path (245 lines)
- ✓ Commit `c7b8e7b` exists in `git log --oneline --all` (verified)
- ✓ All 6 D-16 sections present (`grep -c "^## [1-6]\." = 6`)
- ✓ All 5 unit tests PASSED (was SKIPPED in Wave 0)
- ✓ All required tokens present: 0.372, 0.410, redundant, anti-correlated/inverted/below random, Greshake, def02_priming_analysis, counterfactual / would have to be
- ✓ Style parallels def02_priming_analysis.md (top metadata, horizontal rules, numeric table, bold Conclusion: lines)
