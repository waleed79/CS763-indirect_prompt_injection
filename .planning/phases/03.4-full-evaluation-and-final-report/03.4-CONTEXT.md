# Phase 3.4: Full Evaluation and Final Report - Context

**Gathered:** 2026-04-24 (Round 1)
**Updated:** 2026-04-28 (Round 2)
**Status:** Ready for planning

<update_log>
## Update Log

**2026-04-28 — Round 2 update.** Following three deltas since Round 1:

1. **SC-5 / DEF-05 reclassified as negative result** (commit c4aff8b, Framing B):
   `03.2-VERIFICATION.md` now records DEF-05 as a failed defense hypothesis
   with citable mechanistic explanation; the negative result is the contribution.
2. **Phase 3.3-07 EVAL-V2-01 matrix complete** (commit 3848fc7): 45 per-cell
   logs in `logs/eval_matrix/` and aggregated `_summary.json`; produced two
   findings not present in Round 1 — gemma4:31b-cloud 0%-everywhere and
   Tier 1b homoglyph DEF-02 bypass (22%→35% on llama).
3. **EVAL-08 taxonomy delivered** (commit d203b33): `docs/xss_ssrf_taxonomy.md`
   exists but was not slotted into the writeup structure in Round 1.

Round 2 captured 12 new decisions across 4 areas:

- **D-03, D-04, D-05, D-09 amended in place** — figure specs, writeup section
  list, and section count changed.
- **D-11..D-16 added** — review checkpoint, 5th figure, EVAL-08 placement,
  single-seed caveat, hero-finding elevation, new analysis doc.
- Section count moved from 12 to 13 (new §12 Web Security Mapping;
  Future Work shifted from §12 to §13).
- Total figure count moved from 4 to 5; D-04 and D-05 are now two-panel.

Original DISCUSSION-LOG.md is preserved; a "Round 2 delta" section is appended.

</update_log>

<domain>
## Phase Boundary

Collect and consolidate all experiment results from Phases 3.1, 3.2, and 3.3 into a
complete arms race result matrix; generate all required figures; and write a comprehensive
Phase 3 results section to be added to the existing Phase 1 Google Doc, submitted by
Apr 30.

**Execution constraint**: Phase 3.4 does NOT start until Phase 3.2 and Phase 3.3 are both
complete. Both phases are now complete (3.2 verified 2026-04-24 with SC-5 resolution
2026-04-28 Framing B; 3.3 plan 07 complete 2026-04-27). The full result set (adaptive
attacks, causal attribution as documented negative result, retriever transferability,
extended model matrix) is available — no fallback to partial results needed.

No new attack variants, no new defense components, no new eval harness changes.
Phase 3.4 is purely synthesis, analysis, and writing.

</domain>

<decisions>
## Implementation Decisions

### Writing Strategy

- **D-01:** **Results-first, then write in a sprint.** Execute Phases 3.2 and 3.3 to
  completion before Phase 3.4 begins. Both are now complete. Phase 3.4 then writes the
  full report in a focused sprint using the complete result set. No partial-results
  fallback scenario — 3.4 plans can assume all upstream log files exist.

- **D-11 (NEW 2026-04-28):** **Apr 29 internal review checkpoint.** Plan targets all
  five figures + writeup draft + Markdown source `docs/phase3_results.md` complete by
  EOD Apr 29. Apr 30 is reserved for: Google-Doc paste, format check, link validation,
  final read-through, and submission. Reduces last-day risk on a 2-day sprint.

### Results Consolidation Scripts

- **D-02:** **Script-based results assembly.** Phase 3.4 delivers:
  - `scripts/make_results.py` — reads all `logs/*.json`, `logs/ablation_table.json`,
    and `logs/eval_matrix/_summary.json`, assembles the arms race table, the
    cross-model defense matrix table, and per-experiment result tables, outputs
    Markdown and CSV for pasting into the Google Doc and reuse in the presentation.
  - `scripts/make_figures.py` — generates all five required figures (some two-panel)
    from the log data, saves to `figures/` directory as PNG files.
  Both scripts must be runnable from scratch by a grader (`python scripts/make_results.py`)
  and produce deterministic output. No manual number-copying into the doc.

### Required Figures (now five, with two two-panel)

- **D-03 AMENDED 2026-04-28:** **Arms race bar chart** — x-axis now has SIX tier
  categories (T1, T1b, T2, T3, T4, Adaptive), bars grouped by FIVE defense levels
  (No Defense, BERT alone, Fused, DEF-02, Adaptive ATK-08/09 vs Fused). 6 × 5 = 30
  bars total. Drawn from `ablation_table.json` + `eval_matrix/_summary.json`
  (T1b column source) + Phase 3.2 adaptive rows. T1b inclusion makes the homoglyph
  DEF-02 bypass (22% → 35% on llama, see D-15b) visible in the headline figure rather
  than matrix-data-only. Adaptive bar group makes the arms race progression explicit.
  Primary escalation narrative visualization (satisfies SC-2). **Round 1 spec was
  5 tier categories × 4 bars; superseded.**

- **D-04 AMENDED 2026-04-28:** **ASR-vs-retrieval-rate tradeoff plot** — TWO-PANEL:
  - (a) **llama3.2:3b 7-defense ablation** from `logs/ablation_table.json`
    `retrieval_rate` and `fpr` columns. Explicitly shows the fused defense's
    88% → 50% poisoned retrieval drop and its 76% clean-query FPR cost (satisfies SC-4).
  - (b) **3-LLM × 3-defense from EVAL-V2-01 matrix** (`logs/eval_matrix/_summary.json`):
    cross-architecture sample of the same tradeoff. Tests whether the utility-security
    curve generalizes across LLMs.

  Two-panel rather than overlay because mixing 7-defense ablation markers with
  3-defense matrix markers in one plot creates legend confusion.

- **D-05 AMENDED 2026-04-28:** **LOO causal attribution figure** — TWO-PANEL,
  reframed for negative-result presentation:
  - (a) **ROC curve** labeled "Inverted AUC = 0.372 (llama) / 0.410 (mistral) —
    systematic clean-bias, not noise."
  - (b) **Influence-vs-cleanliness scatter** showing the inversion directly:
    x = LOO influence score, y = ground-truth label (clean vs injected),
    visualizes that injected chunks fall in the LOW-influence region while
    clean chunks fall in the HIGH-influence region (the opposite of what a
    successful defense would show).

  The negative-result evidence is presented visually, not just stated. Drawn from
  Phase 3.2 DEF-05 LOO output (`logs/loo_*.json`). **Round 1 spec was a single ROC
  panel framed as "the metric's ability to distinguish injected from clean chunks";
  superseded — that framing is wrong for AUC < 0.5.**

- **D-06:** **ASR-vs-poisoning-ratio curve** — from Phase 3.3 ATK-02 sweep results
  (5 ratios: 0.5%, 1%, 2%, 5%, 10%). Shows how attack success scales with corpus
  contamination fraction. Standard figure in RAG attack literature; directly comparable
  to PoisonedRAG's analogous figure. (unchanged from Round 1)

- **D-12 (NEW 2026-04-28):** **Figure 5 — EVAL-V2-01 cross-model heatmap.** 5×3
  heatmap: rows = 5 attack tiers (T1, T1b, T2, T3, T4), columns = 3 LLMs
  (llama3.2:3b, mistral:7b, gemma4:31b-cloud), cells = paired ASR under fused
  defense. Drawn from `logs/eval_matrix/_summary.json`. Visually anchors the
  cross-architecture-generalization story: gemma4 0%-everywhere (cloud-scale
  robustness, see D-15a), mistral mid-tier, llama high. Suggested colormap:
  viridis_r (0% = light, higher ASR = darker).

### PoisonedRAG/BadRAG Literature Comparison

- **D-07:** **Methodology table + their published ASR numbers with caveats.**
  Produce a two-part comparison:
  1. A methodology differences table with columns: [Our Work | PoisonedRAG | BadRAG]
     and rows covering: corpus type, corpus size, embedding model, attack tiers
     implemented, defense type, eval protocol (paired vs. random).
  2. A numeric ASR comparison citing their published numbers alongside our Tier-1 naive
     injection ASR on the undefended pipeline, with an explicit caveat footnote that
     direct comparison is approximate (different corpora, models, and eval protocols).
  The comparison satisfies SC-6 (PH3-05). Do NOT fabricate their numbers — cite directly
  from their papers. If a number cannot be found for a specific configuration, leave the
  cell blank with "N/A — config not reported."

### Google Doc Structure (now 13 sections)

- **D-08:** **Extension of the existing Phase 1 Google Doc.** Do NOT create a new
  document. Add a "Phase 3 Results" major section to the existing doc.

- **D-09 AMENDED 2026-04-28:** **Renumbered to 13 subsections.** The Phase 3 results
  section must include all of the following, in order:

  1. **Methods Overview** — concise recap of the multi-signal defense (4 signals + LR
     meta-classifier), adaptive attack variants (ATK-08/09), and LOO causal attribution
     (mentioned NEUTRALLY as "third candidate defense"; AUC<0.5 reveal saved for §5
     per Round-2 framing decision). Self-contained for readers who haven't read
     earlier phases. Opens with brief web-security framing (1-2 sentences) introducing
     the XSS/SSRF/CSP analogs that motivate the multi-signal classifier as a CSP-analog
     (sets up §12 threading per D-13).
  2. **Arms Race Results Table** — complete matrix:
     - **(a)** Phase 2.3 cross-LLM undefended baseline (4 LLMs × T1/T2,
       `eval_harness_undefended_*.json`),
     - **(b)** Phase 3.3 EVAL-V2-01 cross-model defense matrix (3 LLMs × 3 defenses ×
       5 tiers, `logs/eval_matrix/_summary.json`),
     - **(c)** Phase 3.1 single-LLM defense ablation (`ablation_table.json`).

     **STANDING SINGLE-SEED CAVEAT** in §2 preamble (per D-14a): "All EVAL-V2-01
     results are single-seed; per-cell variance unmeasured. Reported numbers are
     point estimates."

     One-sentence preview of the gemma4 cloud-scale finding: "Of note, gemma4:31b-cloud
     scores 0% paired ASR across all 5 tiers × 3 defenses — see §8(e) for cross-
     architecture discussion." (per D-15a)

  3. **Arms Race Bar Chart** — D-03 figure (now 6 tier × 5 defense = 30 bars).
  4. **Utility-Security Tradeoff** — D-04 two-panel figure; per-mode FPR table from
     `ablation_table.json`. Honest discussion of the 76% FPR cost. Cite the **CSP
     strict/permissive analog** when discussing the cost (threading from §12 EVAL-08
     framing per D-13): just as `default-src 'none'` breaks applications without
     careful allowlisting, an FPR of 76% on clean queries breaks utility without
     careful threshold tuning.
  5. **Causal Attribution Analysis** — title kept ("Causal Attribution Analysis";
     not renamed). **Failed-hypothesis arc** per Round-2 framing decision:
     hypothesis (LOO can distinguish injected from clean) → result (AUC = 0.372 /
     0.410, both below random — present D-05 two-panel figure here) → mechanism
     (injected chunks are redundant across the retrieved set; removing one does not
     restore clean model behavior; clean chunks contain unique factual content,
     so LOO divergence is anti-correlated with injection status) → why this is the
     contribution (failed defense hypothesis with mechanistic explanation).
     Cite `logs/loo_negative_result_analysis.md` (new deliverable per D-16).
     Tier 4 fragment influence breakdown stays here (each fragment scores low
     individually; only fragment A retrieved in 3/50 queries; co-retrieval
     insufficient for the fragmentation attack).
  6. **Poisoning Ratio Sweep** — D-06 figure with brief discussion.
  7. **Retriever Transferability** — EVAL-06 per-model ASR table (from Phase 3.3).
     Brief discussion of whether poisoned docs maintain retrieval advantage across
     embedding architectures.
  8. **Per-Finding Narrative Analysis** — SIX bullets (was four):
     - **(a)** DEF-02 priming effect (counter-productive finding with mechanism
       explanation per `def02_priming_analysis.md`)
     - **(b)** T3/T4 zero-baseline puzzle (0% undefended ASR on llama3.2:3b — explain
       why this is correct not a bug)
     - **(c)** BERT classifier 100% detection → anchor-token memorization
     - **(d)** Adaptive attack ASR demonstrating the arms race dynamic
     - **(e) NEW (D-15a):** **Cloud-scale instruction-following robustness.**
       gemma4:31b-cloud is 0% paired ASR across all 5 tiers × 3 defenses
       (single-seed observation per D-14b). Counterpoint to the small-model arms
       race story. Future Work §13 expands the "scale boundary" question.
     - **(f) NEW (D-15b):** **Substring-keyed defenses bypassed by Unicode
       homoglyphs.** Tier 1b on llama under DEF-02: 22% no-defense → 35% under
       DEF-02 (DEF-02 substring detector keyed on ASCII "HACKED" is bypassed by
       Cyrillic "НАСКЕД" payload). Distinct from DEF-02 priming bullet (a) because
       the mechanism differs: priming = prompt-engineering side-effect on
       instruction following; homoglyph bypass = ASCII-only substring matching
       broken by Unicode. Cite **XSS Unicode-bypass precedent** as direct web-
       security analog (threading from §12 EVAL-08 framing per D-13). Single-seed
       observation per D-14b. Future Work §13 expands the "Unicode-aware DEF-02"
       follow-up.
  9. **Limitations** — at minimum the three ROADMAP-required items:
     - **(a)** T3/T4 zero-baseline puzzle.
     - **(b)** DEF-02 counter-productive finding (priming mechanism per
       `def02_priming_analysis.md`).
     - **(c)** Fundamental limits of per-chunk defenses vs. cross-chunk-aware
       approaches. **CITE THE MECHANISM, NOT THE DEFENSE NAME**: "Per-chunk
       defenses cannot recover global poisoning context — injected chunks are
       redundant across the retrieved set, so leave-one-out attribution shows
       inverted distinguishability." DEF-05 is named in §5 results; §9 stays
       general per Round-2 framing decision.

     Plus standing items: 76% clean-query FPR, small corpus scale (1000 passages
     vs. production RAG systems), single-domain evaluation (MS-MARCO web passages).
  10. **Failure Case Analysis (EVAL-04)** — when does the fused defense fail?
      Drawn from false-negative cases in defense eval logs.
  11. **Comparison to Related Work — RAG-specific literature.** Methodology table +
      numeric comparison per D-07. Position vs. AttriBoT (arXiv 2602.14161) for the
      LODO evaluation methodology. **§11 stays focused on RAG/LLM security
      literature** (PoisonedRAG, BadRAG, AttriBoT). Web-security framing lives in §12.
  12. **NEW (D-13): Web Security Mapping (EVAL-08).** Full 3-row taxonomy table
      inline from `docs/xss_ssrf_taxonomy.md` (Stored XSS ↔ Corpus Poisoning,
      SSRF ↔ Tool-Call Injection, CSP ↔ Context Sanitization). Implications
      paragraph closes the section. Threaded callbacks: §1 framing, §4 (CSP
      strict/permissive cost analog), §8(f) (XSS Unicode-bypass analog for T1b).
  13. **Future Work** — covers all descoped items + two NEW additions per Round 2:
      - **NEW (D-15a follow-up):** Scale boundary for instruction-following
        robustness. At what LLM scale does this property emerge? Phase 3.4 measures
        the binary outcome (small models susceptible, gemma4:31b cloud-scale robust);
        a future study sweeping intermediate scales (e.g., 7B, 13B, 70B) would
        characterize the threshold.
      - **NEW (D-15b follow-up):** Unicode-aware DEF-02 variant. Substring matcher
        with NFKC Unicode normalization preprocessing. Patches the ASCII-only
        weakness exposed by Tier 1b homoglyphs.

      Plus existing list: minimax-m2.5:cloud hard-target test, EVAL-07 human
      stealthiness study, Signal 5+ (semantic similarity), gradient boosting
      meta-classifier, cross-model DEF-02 priming test (mistral + gpt-oss:20b-cloud).

- **D-13 (NEW 2026-04-28):** **EVAL-08 taxonomy is FIRST-CLASS in the writeup.**
  - Full 3-row taxonomy table inlined at §12 (not link-only, not summary).
  - Implications paragraph closes the section.
  - Threaded into §1 (framing), §4 (CSP↔Fused for 76% FPR cost discussion),
    §8(f) (XSS Unicode-bypass↔T1b homoglyph). Threading is explicit cross-references,
    not duplicated content.
  - **Section split decision:** §11 stays RAG-specific lit (PoisonedRAG, BadRAG,
    AttriBoT); §12 is the broader web-security mapping. Two distinct positioning
    moves; not merged.

- **D-14 (NEW 2026-04-28):** **Single-seed caveat for EVAL-V2-01 matrix appears in
  TWO places**:
  - **(a) Standing caveat in §2 preamble**: covers the entire matrix once; reader
    sees it before any number is presented.
  - **(b) Per-finding qualifier in §8 bullets (e) and (f)**: each bullet that
    surfaces a EVAL-V2-01-derived finding cites "(single-seed observation)" in-line.
    Reader-of-bullet sees the caveat without flipping back to §2. Suggested wording:
    "(single-seed observation; full per-cell variance not measured)."

- **D-15 (NEW 2026-04-28):** **Two hero findings elevated to §8 narrative bullets**:
  - **(a) gemma4:31b-cloud 0%-everywhere → §8 bullet (e) + §2 sentence.** Mention
    briefly in §2 ("see §8(e)"); full bullet in §8.
  - **(b) Tier 1b homoglyph DEF-02 bypass → standalone §8 bullet (f).** Distinct
    from DEF-02 priming bullet (a); the mechanisms are different (priming vs
    Unicode-substring-bypass). Cite XSS Unicode-bypass precedent for web-security
    grounding (§12 thread).

- **D-16 (NEW 2026-04-28):** **New deliverable: `logs/loo_negative_result_analysis.md`.**
  Standalone analysis document for DEF-05 failed-hypothesis result, parallel to
  `logs/def02_priming_analysis.md` in structure and rigor. Required contents:
  1. Hypothesis statement (LOO influence/relevance ratio anomaly metric should
     distinguish injected from clean chunks, AUC > 0.5).
  2. Observed result table (per-model AUC: llama 0.372, mistral 0.410; both below
     random; cite `logs/loo_*.json`).
  3. Mechanism explanation: (i) injected chunks are redundant — same payload
     repeated across the retrieved top-k, so removing one does not restore clean
     model behavior; (ii) clean chunks contain unique factual content — removing
     one DOES change model output, registering as high LOO influence; net effect
     is anti-correlation between LOO influence and injection status.
  4. Counterfactual: what would have to be true for LOO to work (e.g., injected
     chunks are unique-per-query, OR retrieved set has only one injected chunk,
     OR LOO operates on something other than top-k removal).
  5. Why this is a citable negative-result contribution: per-chunk defense
     literature has assumed local reasoning suffices; this is empirical evidence
     that one specific local-reasoning approach (LOO) is dominated by retrieval
     redundancy.
  6. Pointers: Greshake et al. (2023), Phase 3.2 LOO implementation, def02 analog.

  §5 cites this document; §13 Future Work cites it for the "improved attribution
  beyond top-k LOO" follow-up direction.

### Markdown Draft → Google Doc

- **D-10:** **Write in `docs/phase3_results.md` first**, then manually format and paste
  into the Google Doc. The Markdown file is committed to the repo as the version-controlled
  source of truth for the writeup. Tables from `make_results.py` paste directly as Markdown
  tables. Figures are embedded as image uploads in the Google Doc.

### Claude's Discretion

- Exact Python libraries for figure generation (matplotlib + seaborn recommended)
- Output format for `make_results.py` (Markdown tables + CSV — both acceptable)
- Figure color scheme and palette (consistent across all 5 figures; suggest
  matplotlib colorblind cycle for D-03 bars and viridis_r for Figure 5 heatmap)
- Whether `make_results.py` and `make_figures.py` are one combined script or two
- Section numbering in the Google Doc (continue from Phase 1 numbering or restart at 1)
- Exact footnote wording for the PoisonedRAG numeric comparison caveats
- §12 section title (e.g., "Web Security Mapping" vs "IPI in the Web-Security Tradition")
- Whether D-04 two-panel uses shared y-axis or independent y-axes
- Whether D-05 two-panel has shared legend or per-panel legends
- Exact wording of the §8(e)/(f) single-seed qualifier (e.g., "(single-seed observation)"
  vs "(single seed; full variance not measured)")
- Whether Figure 5 heatmap annotates each cell with the numeric ASR value
- §1 EVAL-08 framing length (one sentence vs short paragraph)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Prior Phase Results (read first)

- `logs/ablation_table.json` — Phase 3.1 ablation results; contains per-defense-mode
  ASR, retrieval_rate, and fpr columns. Core input for `make_results.py` and the
  utility-security tradeoff figure (D-04 panel a). **READ THIS FIRST.**
- `logs/eval_matrix/_summary.json` — **(NEW reference 2026-04-28)** Phase 3.3-07
  EVAL-V2-01 cross-model 45-cell matrix (3 LLMs × 3 defenses × 5 tiers). Source for
  Figure 5 heatmap (D-12) and §2 cross-model results table (D-09 §2). 45 per-cell
  log files in `logs/eval_matrix/<model>__<defense>__<tier>.log`.
- `logs/def02_priming_analysis.md` — Full priming mechanism analysis for DEF-02
  counter-productive finding. The §8 bullet (a) and §9 bullet (b) must accurately
  represent this analysis.
- `logs/loo_*.json` — Phase 3.2-03 LOO causal attribution per-model output. Source
  for D-05 two-panel figure (ROC + scatter) and `loo_negative_result_analysis.md`
  (new D-16 deliverable).
- `logs/attack_baseline.json` — Phase 2.2 frozen 10-query reference for llama3.2:3b
  Tier-1/2 undefended. ROADMAP SC-1 specifies this row alongside the Phase 2.3
  100-query canonical runs.
- `logs/eval_harness_undefended_llama.json`, `_mistral.json`, `_gptoss20b.json`,
  `_gptoss120b.json` — Phase 2.3 canonical undefended baselines for all 4 LLMs.
- `logs/adaptive_*.json` — Phase 3.2 adaptive attack runs (ATK-08/09 vs Fused).
  Source for D-03 arms race chart "Adaptive vs Fused" bar group and §8 bullet (d).

### Documentation Deliverables

- `docs/xss_ssrf_taxonomy.md` — **(NEW reference 2026-04-28)** EVAL-08 deliverable.
  Source for §12 Web Security Mapping inline taxonomy table (D-13). 39-line file
  with 3 mapped pairs (Stored XSS ↔ Corpus Poisoning, SSRF ↔ Tool-Call Injection,
  CSP ↔ Context Sanitization) plus Implications paragraph.

### Prior Phase Context (planning decisions)

- `.planning/phases/03.2-adaptive-attacks-causal-attribution/03.2-VERIFICATION.md` —
  **(UPDATED 2026-04-28)** SC-5 resolution document. Records Framing B disposition:
  DEF-05 reclassified as failed defense hypothesis with mechanistic explanation.
  §5 of the writeup builds on the resolution note; D-16 standalone analysis doc
  expands the framing.
- `.planning/phases/03.2-adaptive-attacks-causal-attribution/03.2-CONTEXT.md` —
  arms race summary table structure (D-17 rows), LOO attribution scope
  (50 attack-paired queries), EVAL-05 3-seed aggregation format, FPR threshold
  sweep results.
- `.planning/phases/03.3-quick-evaluation-additions/03.3-CONTEXT.md` —
  EVAL-06 embedding model names, ATK-02 ratio sweep file naming, EVAL-V2-01
  matrix scope (5 tiers × 3 defense levels × 3 models = 45 runs).
- `.planning/phases/03.3-quick-evaluation-additions/03.3-07-SUMMARY.md` —
  "On the third defense column" framing for `def02` (rather than runtime causal)
  in the EVAL-V2-01 matrix.
- `.planning/phases/03.1-multi-signal-defense-fusion/03.1-CONTEXT.md` —
  ablation table schema, DEF-02 system prompt text, BERT tier-stratified hold-out
  split, fused defense threshold strategy.

### Existing Code (must read before planning)

- `scripts/run_eval.py` — TIER_ID_START constants, HIJACK_STR constants,
  DEFENSE_CHOICES. `make_results.py` must read the same log files this produces.
- `scripts/run_eval_matrix.py` — **(NEW reference 2026-04-28)** Phase 3.3-07 driver
  that wrote `logs/eval_matrix/`. Output schema reference for the 45-cell matrix.
- `logs/` directory listing — all log files that exist at Phase 3.4 execution time.
  Planner should enumerate expected log files from Phases 3.1/3.2/3.3 plans.

### Academic References (required citations in writeup)

- Greshake et al. (2023) — original indirect prompt injection paper
- PoisonedRAG (Zou et al. 2024) — corpus poisoning attack paper; comparison ASR source
- BadRAG (2024) — RAG backdoor attack; methodology comparison source
- Alon & Kamfonas (2023) — GPT-2 perplexity filter citation (Signal 2 justification)
- OWASP LLM01:2025 Prompt Injection — DEF-02 role-separation citable standard
- arXiv 2602.14161 (LODO evaluation methodology) — justifies tier-stratified hold-out
- W3C Content Security Policy Level 3 — §12 CSP↔Fused mapping anchor
- OWASP Top 10 A03:2021 Injection / CWE-79 — §12 Stored XSS↔Corpus Poisoning anchor
- OWASP API Security Top 10 API10:2023 / CWE-918 — §12 SSRF↔Tool-Call Injection anchor
- Capital One 2019 AWS breach — §12 SSRF case-study reference

### Project Files

- `.planning/PROJECT.md` — development environment (conda env `rag-security`)
- `.planning/REQUIREMENTS.md` — EVAL-02, EVAL-03, EVAL-04, PH3-01 through PH3-05
  (UPDATED 2026-04-28: DEF-05 entry reflects "implementation complete; SC re-classified")

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `logs/ablation_table.json` — structured JSON with defense mode rows; primary
  input for the arms race table and D-04 panel (a) utility figure.
- `logs/eval_matrix/_summary.json` — **(NEW)** aggregated cross-model matrix;
  primary input for D-12 Figure 5 heatmap and D-04 panel (b).
- `docs/xss_ssrf_taxonomy.md` — **(NEW)** primary source for §12 inline taxonomy
  table; ready to paste with minor formatting (3 rows + Implications paragraph).
- `scripts/run_eval.py` — log file schema reference; `make_results.py` must parse
  the same JSON format (keys: `tier`, `defense`, `model`, `paired_asr`,
  `retrieval_rate`, `fpr`).
- `scripts/run_eval_matrix.py` — **(NEW)** schema reference for 45-cell matrix
  outputs.
- `demo.ipynb` — existing Jupyter notebook from Phase 2.1; can be extended to
  host interactive figure generation alongside `make_figures.py` if preferred.
- `data/test_queries.json` — query set used in all evals; useful for failure case
  analysis (EVAL-04) to identify which query types correlate with defense failures.

### Established Patterns

- All output logs in `logs/` with descriptive names (convention from Phase 2.3).
- `--delay 3` flag used for all cloud model inference (gpt-oss:20b-cloud,
  gemma4:31b-cloud).
- `dataclasses.replace(cfg, collection=...)` for collection switching in eval runs.
- Arms race table rows follow the structure in 03.2-CONTEXT D-17:
  `[No Defense, DEF-02, BERT alone, Fused (v4), Fused (tuned), ATK-08 vs Fused,
  ATK-09 vs Fused]`. (Note: "Causal Attribution" row removed from this list per
  Round-2 reclassification; LOO results live only in §5 negative-result section.)

### Integration Points

- New `scripts/make_results.py` and `scripts/make_figures.py` (or combined script).
- New `docs/phase3_results.md` — Markdown source of the Phase 3 writeup section
  (D-10).
- New `figures/` directory — output directory for all 5 generated PNG figures.
- New `logs/loo_negative_result_analysis.md` — D-16 standalone analysis doc.

</code_context>

<specifics>
## Specific Requirements

- **SC-1 (ROADMAP, amended 2026-04-28):** Three results tables, drawn from existing artifacts:
  **(a) Phase 2.3 cross-LLM undefended baseline** — 4 LLMs (llama3.2:3b, mistral:7b,
      gpt-oss:20b-cloud, gpt-oss:120b-cloud) on Tier 1/2 attacks
      (`logs/eval_harness_undefended_*.json`). The llama3.2:3b row references BOTH
      `logs/attack_baseline.json` (Phase 2.2 frozen 10-query reference) AND
      `logs/eval_harness_undefended_llama.json` (Phase 2.3 100-query canonical).
  **(b) Phase 3.3 EVAL-V2-01 cross-model defense matrix** — 3 LLMs (llama3.2:3b,
      mistral:7b, gemma4:31b-cloud) × 3 defenses (no_defense, fused, def02) ×
      5 attack tiers (T1, T1b, T2, T3, T4) = 45 cells in `logs/eval_matrix/`,
      aggregated in `logs/eval_matrix/_summary.json`.
  **(c) Phase 3.1 single-LLM defense ablation** — llama3.2:3b × 7 defense modes
      × 4 tiers in `logs/ablation_table.json`.
  The matrix in (b) uses `def02` instead of a runtime `causal` defense by deliberate
  design — Phase 3.2-03 measured LOO ROC AUC at 0.372 (llama) / 0.410 (mistral),
  both below random; **Framing B (commit c4aff8b 2026-04-28)** reclassifies DEF-05
  as a failed defense hypothesis and routes it through §5 as a negative-result
  contribution. Wiring LOO inline as a runtime filter would only confirm the
  published negative result. `def02` is the more informative third comparison point.
  See `.planning/phases/03.3-quick-evaluation-additions/03.3-07-SUMMARY.md`
  "On the third defense column" for the full framing.
- **SC-4 (ROADMAP):** The utility-security tradeoff subsection explicitly shows: fused
  defense reduces poisoned retrieval rate from 88% → 50%, at a cost of 76% clean-query
  FPR. These numbers come from `logs/ablation_table.json` (retrieval_rate + fpr columns).
  D-04 panel (b) tests cross-LLM generalization of this curve.
- **SC-5 (ROADMAP) — three required limitations:**
  (a) T3/T4 baseline puzzle: Tier 3 and Tier 4 show 0% paired ASR on llama3.2:3b
      undefended — not a defense win, requires explanation (Tier 4 requires
      co-retrieval of 3 fragments which rarely aligns with the 50 paired query
      topics; Tier 3 LLM-generated payloads may not have matched the substring
      detector for the test queries).
  (b) DEF-02 counter-productive finding: system-prompt hardening increased paired
      ASR (T1: 2%→8%, T2: 12%→38%) due to priming mechanism (per
      `def02_priming_analysis.md`).
  (c) Fundamental limits of per-chunk defenses vs. cross-chunk-aware approaches.
      **Wording must cite the mechanism (chunk redundancy across retrieved set,
      LOO inverted distinguishability) WITHOUT naming DEF-05** per Round-2 framing
      decision. DEF-05 is named in §5 results.
- **DEF-02 priming finding treatment:** This is a **novel finding**, not just a
  limitation. §8(a) presents it as a positive research contribution — a counter-
  intuitive result with practical implications for RAG defenders deploying system-
  prompt-only hardening on small instruction-following models.
- **NEW (2026-04-28) — Hero findings:**
  - gemma4:31b-cloud 0%-everywhere is a **citable cross-architecture
    generalization result**, not just matrix data. §8(e) + §2 sentence per D-15a.
  - Tier 1b homoglyph DEF-02 bypass is a **citable substring-defense weakness
    result** with direct XSS Unicode-bypass precedent. §8(f) per D-15b.
- **NEW (2026-04-28) — DEF-05 negative-result contribution:** The failed defense
  hypothesis is a **first-class result** of Phase 3.4, not just a limitations
  bullet. §5 carries the full failed-hypothesis arc; `loo_negative_result_analysis.md`
  (D-16) carries the rigorous treatment; §13 Future Work expands the "improved
  attribution beyond top-k LOO" follow-up.

</specifics>

<deferred>
## Deferred Ideas

The two items removed from this list (now PROMOTED to Future Work bullets in §13
per D-09 / D-15 follow-ups):

- ~~Cross-model DEF-02 priming test (mistral + gpt-oss:20b-cloud)~~ — partially
  superseded by Tier 1b finding; the priming question is now scoped narrower.
- ~~ATK-02 extended to Tier 2~~ — kept as deferred (still backlog).

Items still deferred (not in §13 Future Work, kept on backlog):

- **Minimax-m2.5:cloud hard-target test** — Future Work footnote in §13 already covers.
- **EVAL-07 human stealthiness study** — Future Work footnote in §13 already covers.
- **Signal 5+** (semantic similarity between query and injection instruction) — §13.
- **Gradient Boosting meta-classifier** — §13.
- **ATK-02 extended to Tier 2** — backlog only; no §13 reference.
- **Improved attribution beyond top-k LOO** — **NEW (D-16 follow-up)** — §13
  references this as a direction motivated by the DEF-05 negative result.
- **Scale-boundary study** — **NEW (D-15a follow-up)** — §13 Future Work bullet.
- **Unicode-aware DEF-02 variant** — **NEW (D-15b follow-up)** — §13 Future Work bullet.

</deferred>

---

*Phase: 03.4-full-evaluation-and-final-report*
*Round 1 context: 2026-04-24*
*Round 2 update: 2026-04-28*
