---
phase: 07-honest-fpr-metrics-gpt-oss-extension
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - docs/phase5_honest_fpr.md
autonomous: true
requirements:
  - P7-DOC-UNTOUCHED
  - P7-DOC-ADDENDUM
  - P7-DOC-TABLE-10ROW
  - P7-DOC-PHASE3-UNTOUCHED

must_haves:
  truths:
    - "docs/phase5_honest_fpr.md contains exactly one '## Phase 7 addendum: gpt-oss extension (2026-05-04)' heading"
    - "The original Phase 5 prose (everything above the new addendum heading) is bit-for-bit identical to git HEAD"
    - "The addendum's M1/M2/M3 table has exactly 10 data rows: 6 llama (DEF-02, BERT alone, Perplexity, Imperative, Fingerprint, Fused) + 2 gpt-oss:20b-cloud ({Fused, DEF-02}) + 2 gpt-oss:120b-cloud ({Fused, DEF-02})"
    - "The 6 llama rows in the addendum table are copied verbatim from the existing Phase 5 §4 results table — same numbers, same labels"
    - "The 4 gpt-oss rows are populated from logs/ablation_table_gptoss_v7.json (the file produced by Plan 04)"
    - "The addendum contains 1-2 paragraphs of substantive cross-LLM analysis (not just a numbers dump) per D-07"
    - "The addendum contains a methodology note inheriting Phase 5 D-03/D-05/D-06/D-10/D-12 conventions"
    - "The addendum links to docs/results/honest_fpr_gptoss_v7.md (the auto-companion)"
    - "docs/phase3_results.md is byte-identical to git HEAD (T-7-02 — submitted Phase 3.4 writeup not touched)"
    - "Plan 01 stubs P7-DOC-UNTOUCHED, P7-DOC-ADDENDUM, P7-DOC-TABLE-10ROW, P7-DOC-PHASE3-UNTOUCHED all flip from SKIP to PASS"
  artifacts:
    - path: "docs/phase5_honest_fpr.md"
      provides: "Phase 5 writeup with appended Phase 7 addendum (10-row cross-LLM honest-FPR table + analysis)"
      contains: "## Phase 7 addendum: gpt-oss extension"
  key_links:
    - from: "docs/phase5_honest_fpr.md addendum"
      to: "logs/ablation_table_gptoss_v7.json"
      via: "Numbers in the 4 gpt-oss table rows are copy-pasted from this file"
      pattern: "per_chunk_fpr"
    - from: "docs/phase5_honest_fpr.md addendum"
      to: "docs/results/honest_fpr_gptoss_v7.md"
      via: "Pointer paragraph: 'For the machine-readable companion see ...'"
      pattern: "honest_fpr_gptoss_v7"
---

<objective>
Append a `## Phase 7 addendum: gpt-oss extension (2026-05-04)` section to `docs/phase5_honest_fpr.md` (in-place edit, append-only — original prose stays bit-for-bit). The addendum contains: a 1-paragraph framing, the 10-row M1/M2/M3 table covering all 3 RAG targets (llama 6 rows + 2 × gpt-oss 4 rows), 1-2 paragraphs of cross-LLM analysis, a methodology note, and a pointer to the auto-companion at `docs/results/honest_fpr_gptoss_v7.md`.

Per D-07/D-08: NO standalone Phase 7 doc is created. Per inheritance constraint: `docs/phase3_results.md` (the submitted Phase 3.4 writeup) is NOT touched.

Purpose: Land the human-readable Phase 7 deliverable. This is the artifact the user reads to assess whether the gpt-oss numbers generalize the Phase 5 llama findings.

Output: One in-place edit to `docs/phase5_honest_fpr.md` (~80-150 lines appended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-CONTEXT.md
@.planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-PATTERNS.md
@.planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-VALIDATION.md
@.planning/phases/05-honest-fpr-metrics-per-chunk-answer-preserved-and-llm-as-jud/05-CONTEXT.md
@.planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-04-SUMMARY.md

@docs/phase5_honest_fpr.md
@logs/ablation_table_gptoss_v7.json
@logs/ablation_table.json
@docs/results/honest_fpr_gptoss_v7.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Phase 7 addendum to docs/phase5_honest_fpr.md (10-row table + 1-2 paragraph analysis + methodology note)</name>
  <files>docs/phase5_honest_fpr.md</files>
  <read_first>
    - docs/phase5_honest_fpr.md (FULL FILE — you must reproduce numbers from §4 verbatim into the new 10-row addendum table; you must also see the existing structure to know exactly where to append)
    - logs/ablation_table.json (read the 6 llama-row entries: DEF-02 / bert_only / perplexity_only / imperative_only / fingerprint_only / fused — extract per_chunk_fpr, answer_preserved_fpr, judge_fpr fields verbatim)
    - logs/ablation_table_gptoss_v7.json (read the 4 gpt-oss-row entries — extract the same 3 fields per row)
    - .planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-CONTEXT.md (D-07 — addendum spec; D-08 — no standalone doc)
    - .planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-PATTERNS.md §"docs/phase5_honest_fpr.md (MODIFY — append addendum in-place)" (lines 397-437 — section structure + table template)
    - tests/test_make_results_v7.py (TestPhase5ProseUntouched, TestAddendumPresent, TestAddendumTable10Rows, TestPhase34NotEdited — these are the contract tests)
  </read_first>
  <action>
    Edit `docs/phase5_honest_fpr.md` by **appending only** — do not modify any byte above the new section. Use the Edit tool with `old_string` exactly equal to the current END of the file (the last few characters of the References section), and `new_string` equal to that same suffix followed by the new addendum. This guarantees zero modification above the addendum.

    Structure of the addendum (per D-07 / 07-PATTERNS.md lines 414-437):

    ```markdown


    ## Phase 7 addendum: gpt-oss extension (2026-05-04)

    Phase 5 measured the three honest FPR metrics (M1 per-chunk, M2 answer-preserved, M3 judge-scored) on a single RAG target — `llama3.2:3b`. This addendum extends those metrics to the two cloud RAG targets used elsewhere in the project, `gpt-oss:20b-cloud` and `gpt-oss:120b-cloud`, on the `{fused, def02}` defense cells produced by Phase 6. Scope is intentionally narrow: 4 cells × 50 clean queries = 200 LLM-as-judge calls, computed against the same eval set, with the same judge model (`gpt-oss:20b-cloud`), and inheriting all of Phase 5's metric definitions, prompt template, and edge-case handling without redefinition (D-11). Other models (`mistral:7b`, `gemma4:31b-cloud`) are out of scope here — Phase 6 produced only the two gpt-oss × {fused, def02} cells we extend.

    ### Cross-LLM honest FPR table

    | Model              | Defense       | Per-chunk FPR (M1) | Answer-preserved FPR (M2) | Judge FPR (M3) |
    |--------------------|---------------|--------------------|----------------------------|----------------|
    | llama3.2:3b        | DEF-02        | <copy from §4 row "DEF-02">  | ... | ... |
    | llama3.2:3b        | BERT alone    | <copy from §4 row "BERT alone (Signal 1)"> | ... | ... |
    | llama3.2:3b        | Perplexity    | <copy from §4 row "Perplexity alone (Signal 2)"> | ... | ... |
    | llama3.2:3b        | Imperative    | <copy from §4 row "Imperative ratio alone (Signal 3)"> | ... | ... |
    | llama3.2:3b        | Fingerprint   | <copy from §4 row "Fingerprint alone (Signal 4)"> | ... | ... |
    | llama3.2:3b        | Fused         | <copy from §4 row "Fused (DEF-04)"> | ... | ... |
    | gpt-oss:20b-cloud  | Fused         | <copy from logs/ablation_table_gptoss_v7.json gptoss20b_cloud__fused.per_chunk_fpr> | ... | ... |
    | gpt-oss:20b-cloud  | DEF-02        | <copy from gptoss20b_cloud__def02> | ... | ... |
    | gpt-oss:120b-cloud | Fused         | <copy from gptoss120b_cloud__fused> | ... | ... |
    | gpt-oss:120b-cloud | DEF-02        | <copy from gptoss120b_cloud__def02> | ... | ... |

    ### Cross-LLM analysis

    <Paragraph 1: 4-6 sentences. Compare M1 (per-chunk FPR) across the three RAG targets. Does fused defense's per-chunk FPR look similar across model scales (3B → 20B → 120B), or does it shift? If similar: that's evidence the per-chunk filter is target-agnostic — it filters chunks regardless of which downstream LLM consumes them, which is the expected design property since the filter is upstream of generation. If it shifts noticeably: investigate (could indicate the v6 cells used different retrieval seeds or that the defense's chunk-removal counts were tracked inconsistently).>

    <Paragraph 2: 4-6 sentences. Compare M2 and M3 across model scales. The interesting cross-LLM question is whether the larger gpt-oss:120b "routes around" removed chunks more robustly than smaller models — i.e., does answer-preserved FPR (M2) drop with scale? If M3 (judge FPR) is consistently lower for 120b than 20b for the same defense, that is evidence that frontier-scale models can compensate for missing context, weakening the user-visible utility cost of an aggressive per-chunk filter on those targets. State the finding directly even if it is null/inconclusive (small N=50 → wide CIs; the absence of a clear trend is itself a finding worth recording for future Phase 8 work). For DEF-02 specifically: Phase 5 found DEF-02 was counter-productive on llama3.2:3b (priming the model to surface attacker text); does the gpt-oss DEF-02 row replicate or refute that pattern at scale? Cite the specific number from the table.>

    ### Methodology note

    All Phase 5 conventions inherit verbatim (D-11):
    - **Eval set**: same 50 clean queries (indices 50–99 of `data/test_queries.json`).
    - **Judge model**: `gpt-oss:20b-cloud` with temperature 0, no seed (Phase 5 D-03).
    - **A/B prompt with order randomization**: same seed (`--seed 42`) and same `JUDGE_USER_TEMPLATE` (Phase 5 D-10).
    - **Single-seed convention**: one judge call per (cell, query) pair; no per-call resampling. The standing caveat from Phase 5 §1 applies — wider confidence intervals than a multi-seed run would produce, accepted as the cost of a one-pass design (Phase 5 D-05).
    - **TIE / refusal / parse-failure** collapse to PRESERVED after a single retry (Phase 5 D-12).
    - **M2 denominator** is 50 (the literal ROADMAP wording, Phase 5 D-07).
    - **No additional ground-truth signal**: M2's "degraded" verdict comes from the same A/B judge as M3, not from a separate ROUGE / exact-match baseline (Phase 5 D-06).
    - **Cloud delay**: `--delay 3` between calls (Phase 2.4 lineage, inherited via D-12).

    For the machine-readable companion see `docs/results/honest_fpr_gptoss_v7.md`.
    ```

    Implementation steps:

    1. **Read** `logs/ablation_table.json` and extract the 6 llama M1/M2/M3 numbers. The relevant keys (per Phase 5 D-02) are: `def02_only`, `bert_only`, `perplexity_only`, `imperative_only`, `fingerprint_only`, `fused`. (If actual key names differ, use the keys present in the file — Phase 5 D-02 entries.) For each, extract `per_chunk_fpr`, `answer_preserved_fpr`, `judge_fpr` and round to 3 decimal places (matching docs/phase5_honest_fpr.md §4 formatting — verify by reading the existing §4 table).

    2. **Read** `logs/ablation_table_gptoss_v7.json` and extract the 4 gpt-oss M1/M2/M3 numbers from keys `gptoss20b_cloud__fused`, `gptoss20b_cloud__def02`, `gptoss120b_cloud__fused`, `gptoss120b_cloud__def02`. Same 3 fields, same rounding.

    3. **Use the Edit tool** to append the addendum. The `old_string` should be the last ~50 characters of the current References section (verify by reading the end of `docs/phase5_honest_fpr.md`); the `new_string` is that same suffix followed by `\n\n` + the full addendum block.

    4. **Write the analysis paragraphs with substance**, not boilerplate. The reader (the user, a PhD student) wants to know: do the gpt-oss numbers replicate or refute the Phase 5 llama narrative? Anchor each claim to a specific number from the table. If a comparison is null/inconclusive, say so plainly — that is itself a finding.

    5. **Validate immediately**:
    ```bash
    # Addendum heading present exactly once
    grep -c "^## Phase 7 addendum: gpt-oss extension" docs/phase5_honest_fpr.md
    # → should output 1

    # Phase 5 prose untouched (above addendum heading)
    git diff docs/phase5_honest_fpr.md | grep -E "^-" | grep -vE "^---" | wc -l
    # → should output 0 (zero deletions; pure additive change)

    # 10-row table count
    python -c "import re; t=open('docs/phase5_honest_fpr.md',encoding='utf-8').read(); m=re.search(r'## Phase 7 addendum.*?(?=^## |\\Z)', t, re.S|re.M); rows=[l for l in m.group().splitlines() if l.startswith('|') and '---' not in l and ('Defense' in l or 'Model' in l) == False]; print(f'data rows: {len(rows)}'); assert len(rows) == 10, len(rows)"

    # docs/phase3_results.md untouched
    git diff --exit-code docs/phase3_results.md
    # → should exit 0
    ```
  </action>
  <verify>
    <automated>pytest tests/test_make_results_v7.py::TestAddendumPresent tests/test_make_results_v7.py::TestAddendumTable10Rows tests/test_make_results_v7.py::TestPhase5ProseUntouched tests/test_make_results_v7.py::TestPhase34NotEdited -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 7 addendum: gpt-oss extension" docs/phase5_honest_fpr.md` outputs 1 (exactly one addendum heading).
    - `git diff docs/phase5_honest_fpr.md | grep -E "^-" | grep -vE "^---" | wc -l` outputs 0 (no deletions; pure append).
    - `python -c "import re; t=open('docs/phase5_honest_fpr.md',encoding='utf-8').read(); m=re.search(r'## Phase 7 addendum.*?(?=^## |\\Z)', t, re.S|re.M); rows=[l for l in m.group().splitlines() if l.startswith('|') and '---' not in l and 'Defense' not in l and 'Model' not in l]; assert len(rows) == 10, len(rows)"` exits 0.
    - `git diff --exit-code docs/phase3_results.md` exits 0 (Phase 3.4 submitted writeup untouched — T-7-02 mitigation).
    - `grep -c "honest_fpr_gptoss_v7" docs/phase5_honest_fpr.md` outputs at least 1 (pointer to auto-companion exists).
    - `grep -c "D-11" docs/phase5_honest_fpr.md` outputs at least 1 (methodology note references inheritance).
    - `grep -cE "gpt-oss:20b-cloud|gpt-oss:120b-cloud" docs/phase5_honest_fpr.md` outputs at least 4 (both models referenced multiple times — at least 2 in the table heading rows).
    - The 4 gpt-oss numbers in the addendum table match `logs/ablation_table_gptoss_v7.json` to 3 decimal places (manual spot-check during commit; Plan 06 audits this systematically).
    - `pytest tests/test_make_results_v7.py::TestAddendumPresent tests/test_make_results_v7.py::TestAddendumTable10Rows tests/test_make_results_v7.py::TestPhase5ProseUntouched tests/test_make_results_v7.py::TestPhase34NotEdited -x` exits 0 (all 4 doc-related stubs flip to PASS).
  </acceptance_criteria>
  <done>Addendum appended to docs/phase5_honest_fpr.md with 10-row table, substantive cross-LLM analysis, methodology note, auto-companion pointer; original prose bit-for-bit preserved; docs/phase3_results.md untouched; 4 doc-related test stubs flip from SKIP to PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit tool ↔ docs/phase5_honest_fpr.md | Edits to a Phase 5 deliverable — must be append-only to honor the inheritance constraint. |
| Source numbers ↔ ablation logs | The 10 table rows are sourced from `logs/ablation_table.json` (6 rows) and `logs/ablation_table_gptoss_v7.json` (4 rows) — manual transcription is the failure surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | T (tampering) | Edit tool could modify Phase 5 prose above the addendum, or could mutate `docs/phase3_results.md`. | mitigate | Acceptance criteria assert `git diff` shows 0 deletions on `docs/phase5_honest_fpr.md` AND `git diff --exit-code docs/phase3_results.md` exits 0. The Edit-tool `old_string` is scoped to the END of the file (last ~50 chars of References) so any accidental mid-file edit would fail to match and the tool would refuse. |
| T-7-05 | (new — transcription error) | Manual copying of 30 numbers (10 rows × 3 metrics) from JSON files to Markdown table is error-prone. | mitigate | Plan 06 verification audits the addendum numbers programmatically by re-parsing `logs/ablation_table.json` + `logs/ablation_table_gptoss_v7.json` and confirming each table cell matches the JSON to 3 decimal places. The auto-companion at `docs/results/honest_fpr_gptoss_v7.md` provides a 4-row machine-generated cross-check. |
</threat_model>

<verification>
After the task completes:

1. `grep -c "^## Phase 7 addendum: gpt-oss extension" docs/phase5_honest_fpr.md` outputs 1.
2. `git diff docs/phase5_honest_fpr.md` shows ONLY additions (zero deletions).
3. The addendum's table has exactly 10 data rows (regex parser test).
4. `git diff --exit-code docs/phase3_results.md` exits 0 (Phase 3.4 submitted writeup untouched).
5. `pytest tests/test_make_results_v7.py -x` reports the 4 doc tests as PASS (previously SKIP).
6. Numbers in addendum table match the source JSONs (manual spot-check, fully audited in Plan 06).
</verification>

<success_criteria>
- `docs/phase5_honest_fpr.md` has the new addendum appended; original Phase 5 prose unchanged.
- 10-row table with 6 llama + 4 gpt-oss rows present.
- 1-2 paragraphs of substantive cross-LLM analysis (not boilerplate).
- Methodology note inherits Phase 5 D-03/D-05/D-06/D-07/D-10/D-12.
- Pointer to `docs/results/honest_fpr_gptoss_v7.md` present.
- `docs/phase3_results.md` untouched (T-7-02).
- All 4 doc-related stubs from Plan 01 flip from SKIP to PASS.
- No standalone Phase 7 doc created (D-08 honored).
</success_criteria>

<output>
After completion, create `.planning/phases/07-honest-fpr-metrics-gpt-oss-extension/07-05-SUMMARY.md` with:
- Word count of the appended addendum.
- The 10 table rows (re-rendered as markdown for the summary so reviewer can audit numbers vs. source JSONs).
- `git diff --stat docs/phase5_honest_fpr.md` (should show 0 deletions, ~80-150 insertions).
- Confirmation that `docs/phase3_results.md` is untouched.
- Pass/SKIP table for the 4 doc-related tests in `tests/test_make_results_v7.py`.
</output>
</content>
</invoke>