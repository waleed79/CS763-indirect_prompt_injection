---
phase: 02.1
slug: rag-pipeline-foundation
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-11
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest >=8 (installed in `rag-security` conda env — Wave 1) |
| **Config file** | `pytest.ini` (Wave 1 — Plan 01 Task 2) |
| **Quick run command** | `pytest tests/ -x --timeout=30` |
| **Full suite command** | `pytest tests/ -v --timeout=120` |
| **Estimated runtime** | ~60 seconds full suite (dominated by one end-to-end Ollama generation test) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/ -x --timeout=30`
- **After every plan wave:** Run `pytest tests/ -v --timeout=120`
- **Before `/gsd:verify-work`:** Full suite must be green AND `demo.ipynb` runs cleanly top-to-bottom
- **Max feedback latency:** 30 seconds (quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| P01-T1 | 01 | 1 | RAG-05 | env check | `conda run -n rag-security python -c "import torch, transformers, sentence_transformers, chromadb, ollama, datasets, pytest; print('OK')"` | requirements.txt | pending |
| P01-T2 | 01 | 1 | RAG-05 | collection | `conda run -n rag-security python -m pytest tests/ --collect-only -q` | pytest.ini, tests/*.py | pending |
| P01-T3 | 01 | 1 | RAG-05 | script run | `conda run -n rag-security python scripts/check_tokenizer_ratio.py` | scripts/check_tokenizer_ratio.py | pending |
| P02-T1 | 02 | 2 | RAG-04, RAG-05 | unit | `conda run -n rag-security python -c "from rag.config import load_config; c = load_config(); assert c.seed == 42; print('OK')"` | config.toml, rag/config.py | pending |
| P02-T2 | 02 | 2 | RAG-02 | unit | `conda run -n rag-security python -m pytest tests/test_chunking.py tests/test_corpus.py -v --timeout=120` | rag/corpus.py, data/nq_500.jsonl | pending |
| P03-T1 | 03 | 3 | RAG-01, RAG-03 | unit | `conda run -n rag-security python -m pytest tests/test_logger.py -v --timeout=30` | rag/retriever.py, rag/logger.py | pending |
| P03-T2 | 03 | 3 | RAG-01, RAG-03 | integration | `conda run -n rag-security python -m pytest tests/ -v --timeout=600` | rag/generator.py, rag/pipeline.py | pending |
| P04-T1 | 04 | 4 | RAG-01, RAG-05 | notebook | `conda run -n rag-security python -c "import nbformat; nb = nbformat.read('demo.ipynb', as_version=4); assert len(nb.cells) >= 9; print('OK')"` | demo.ipynb | pending |
| P04-T2 | 04 | 4 | RAG-01 | human | Human runs demo.ipynb top-to-bottom and inspects output | demo.ipynb | pending |

*Status: pending · green · red · flaky*

---

## Wave 0 Requirements

Greenfield phase — all test scaffolding is built in Wave 1 (Plan 01) before feature tasks begin.

- [ ] Install `pytest>=8` and `pytest-timeout` into the `rag-security` conda env
- [ ] `pytest.ini` — minimal pytest config with `testpaths = ["tests"]` and timeout plugin enabled
- [ ] `tests/conftest.py` — shared fixtures: seeded `Config`, temporary `.chroma` dir, temporary log path
- [ ] `tests/test_chunking.py` — stub tests for chunker (boundary cases: empty, short passage, overlap correctness) -> RAG-02
- [ ] `tests/test_corpus.py` — stub tests for NQ loader (count, uniqueness, seeded determinism) -> RAG-02
- [ ] `tests/test_logger.py` — stub tests for `RetrievalLogger` (round-trip, append mode, JSONL validity) -> RAG-03
- [ ] `tests/test_pipeline_e2e.py` — stub integration test: real Ollama, real ChromaDB on temp dir, one query -> RAG-01
- [ ] `tests/test_reproducibility.py` — stub integration test: same seed -> identical retrieved IDs -> RAG-05

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `demo.ipynb` produces visually sensible output for a sample NQ query | RAG-01 | Grader-facing artifact; qualitative "does the LLM answer reference retrieved context" judgment has no clean automated test at this stage | Open `demo.ipynb`, Kernel -> Restart & Run All, inspect the final cell: (a) retrieved chunks printed with scores, (b) LLM answer mentions content from retrieved chunks |
| LLM answer uses retrieved context (not purely from model weights) | RAG-01 | Automated grounding check is out of scope for Phase 2.1 (belongs in Phase 2.3 evaluation harness) | Visual inspection only — confirm a reasonable overlap between retrieved chunk text and answer |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (7 test files + pytest.ini + conftest.py)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (quick run)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
