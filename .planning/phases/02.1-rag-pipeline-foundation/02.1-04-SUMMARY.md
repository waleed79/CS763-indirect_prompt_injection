# Plan 02.1-04 Summary: Demo Notebook

**Status:** COMPLETED (pending human Ollama verification)  
**Completed:** 2026-04-12  
**Phase:** 02.1-rag-pipeline-foundation

---

## What Was Done

### Task 1 — `demo.ipynb`

Created a 10-cell Jupyter notebook (1 markdown + 9 code) as the grader-facing entry point for Phase 2.1.

**Cell sequence:**
| Cell | Purpose |
|------|---------|
| 0 (markdown) | Title + overview of demo flow |
| 1 | Ollama health check via `Generator.is_reachable()` + `assert_model_available()` |
| 2 | Load config from `config.toml` via `load_config()`, print all params |
| 3 | Load 500 NQ passages, show sample, compute chunking stats (total chunks, avg words/chunk) |
| 4 | Init `RAGPipeline(config).build()` — loads corpus, embeds, indexes into ChromaDB |
| 5 | Run sample query: "what is the boiling point of water" |
| 6 | Display top-5 retrieved chunks with rank, chunk_id, cosine similarity score |
| 7 | Display LLM answer |
| 8 | Inspect `logs/retrieval.jsonl` — show last entry timestamp, query, result count |
| 9 | Cleanup via `pipeline.close()` |

**Adapts to the actual interface:**
- Uses `RAGPipeline(config).build()` (two-phase init, not auto-build)
- Ollama check uses `Generator.is_reachable()` + `assert_model_available()` (not module-level ping)
- Collection count accessed via `pipeline._retriever.count`

## Acceptance Criteria Verification

| Criterion | Status |
|-----------|--------|
| `demo.ipynb` exists at repo root | PASS |
| Notebook has 10 cells (>= 9 required) | PASS |
| `RAGPipeline` imported and used | PASS |
| `load_config` used | PASS |
| Ollama health check cell present | PASS |
| Cosine similarity scores displayed | PASS |
| Valid notebook (nbformat.read succeeds) | PASS |

## Checkpoint: Human Verification Required

**Plan 04 is `autonomous: false`** — the following steps require a human to verify:

1. Ensure Ollama is running: `ollama serve` (separate terminal)
2. Ensure model is pulled: `ollama pull llama3.2:3b`
3. Run the full test suite:
   ```
   /opt/anaconda3/envs/rag-security/bin/python -m pytest tests/ -v --timeout=600
   ```
   Expected: all unit tests pass (77+); integration tests pass with Ollama running
4. Open the notebook:
   ```
   /opt/anaconda3/envs/rag-security/bin/jupyter notebook demo.ipynb
   ```
5. Run all cells top-to-bottom (Kernel > Restart & Run All)
6. Verify output:
   - Cell 1: "llama3.2:3b is available."
   - Cell 3: "Loaded 500 unique passages" + sample passage
   - Cell 4: "Pipeline initialized. Collection has N chunks."
   - Cell 6: 5 chunks with cosine similarity scores between 0 and 1
   - Cell 7: LLM answer referencing retrieved context
   - Cell 8: at least 1 log entry with timestamp and query
7. Check `logs/retrieval.jsonl` exists with valid JSONL

**Type "approved" to mark this checkpoint complete.**
