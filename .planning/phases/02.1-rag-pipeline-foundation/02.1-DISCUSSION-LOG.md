# Phase 2.1: RAG Pipeline Foundation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-11
**Phase:** 2.1-rag-pipeline-foundation
**Areas discussed:** Corpus Dataset, Project Structure, Corpus Chunking, Retrieval Logging

---

## Corpus Dataset

| Option | Description | Selected |
|--------|-------------|----------|
| Natural Questions (Wikipedia) | Google NQ — Wikipedia passages + real user questions. Matches PoisonedRAG setup. | ✓ |
| SQuAD (Wikipedia) | Pre-extracted Wikipedia passages. Simpler but less realistic. | |
| Custom domain (CS/security papers) | arXiv papers. Thematically coherent but extra ETL work. | |
| You decide | Claude picks based on prior work. | |

**User's choice:** Natural Questions (Wikipedia)

---

## Corpus Size

| Option | Description | Selected |
|--------|-------------|----------|
| 500 passages | Meets RAG-02 required threshold. Fast to index. | ✓ |
| 1000–2000 passages | More realistic retrieval conditions. Slower setup. | |
| Full NQ corpus (100K+) | Most realistic but significant indexing time/disk. | |

**User's choice:** 500 passages

---

## Project Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Python modules | Modular rag/ package. Clean imports for Phase 2.2/3.1. | |
| Jupyter notebooks + modules | Notebooks for demos; modules for core logic. | ✓ |
| Notebooks only | Easy to demo but hard to import/extend. | |

**User's choice:** Jupyter notebooks + modules

---

## Entry Point

| Option | Description | Selected |
|--------|-------------|----------|
| demo.ipynb notebook | Notebook walks full pipeline. Graders run cell-by-cell. | ✓ |
| CLI script (run_pipeline.py) | Command-line for automated experiments. | |
| Both | Notebook for demos, CLI for experiments. | |

**User's choice:** demo.ipynb notebook

---

## Corpus Chunking

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed-size 256-token chunks | Deterministic, matches attack paper convention, 32-token overlap. | ✓ |
| Paragraph-level chunks | Semantically coherent, variable size. | |
| Whole-passage (no splitting) | Simplest — NQ passages are already short. | |

**User's choice:** Fixed-size 256-token chunks

---

## Retrieval Logging Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Verbose (full content + metadata) | Query + doc IDs + scores + full chunk text + timestamp. | ✓ |
| Minimal (IDs + scores only) | Smaller files, re-retrieve content later if needed. | |
| Minimal now, extend later | Start lean, upgrade in Phase 2.2. | |

**User's choice:** Verbose (full content + metadata)

---

## Retrieval Log Storage

| Option | Description | Selected |
|--------|-------------|----------|
| JSON Lines file (.jsonl) | One JSON per line, append mode, parsed with pandas. | ✓ |
| SQLite database | SQL-queryable but more setup. | |
| Print to stdout only | Zero overhead, no persistent structure. | |

**User's choice:** JSON Lines file (.jsonl)

---

## Claude's Discretion

- Top-k default value
- Config file format (JSON vs YAML)
- ChromaDB collection naming convention
- NQ split/subset selection

---

## Deferred Ideas

- CLI entry point — deferred to Phase 2.2
- 1000+ passage scaling — optional, deferred
- Configurable retrieval parameters (RAG-04) — optional, deferred
- Cross-model experiments — deferred to Phase 3.2
