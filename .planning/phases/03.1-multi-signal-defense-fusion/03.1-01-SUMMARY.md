---
phase: 03.1-multi-signal-defense-fusion
plan: "01"
subsystem: testing
tags: [pytest, distilbert, defense, tdd, wave0, stubs]

requires:
  - phase: 02.4-advanced-attack-tiers
    provides: attack tier definitions (T1-T4), corpus_poisoned.jsonl, nq_poisoned_v4 collection

provides:
  - tests/test_defense.py with 10 stub test items (9 skip + 1 red) covering all 4 defense signals
  - TestGenerateSystemPrompt class (2 red stubs) in tests/test_generator.py
  - models/bert_classifier/.gitkeep directory marker
  - DistilBERT weights cached in ~/.cache/huggingface/hub/models--distilbert-base-uncased
  - scripts/_download_distilbert.py pre-flight helper

affects:
  - 03.1-02 (Wave 1 defense implementation — all stubs go green)
  - 03.1-03 (DEF-02 system-prompt implementation — TestGenerateSystemPrompt goes green)

tech-stack:
  added:
    - distilbert-base-uncased (DistilBertForSequenceClassification, DistilBertTokenizerFast from transformers)
  patterns:
    - Wave 0 TDD pre-flight: write stubs first, use module-level try/except ImportError so collection never fails
    - pytest.skip() inside test body for stubs pending implementation
    - TestDef02Prompt test body does direct import (no skip) — fails red until Wave 1 Plan 03 adds constant

key-files:
  created:
    - tests/test_defense.py
    - models/bert_classifier/.gitkeep
    - scripts/_download_distilbert.py
  modified:
    - tests/test_generator.py

key-decisions:
  - "Module-level try/except ImportError for rag.defense import so pytest collection exits 0 even before defense module exists"
  - "TestDef02Prompt has no pytest.skip() — directly imports DEF_02_SYSTEM_PROMPT to produce red FAILED (not skip) until Wave 1 adds the constant"
  - "DistilBERT download uses an external script (_download_distilbert.py) rather than conda run -c inline due to Windows conda limitation with newlines in -c arguments"

patterns-established:
  - "Wave 0 stub pattern: try/except ImportError at module level, pytest.skip() in test body"

requirements-completed: [DEF-01, DEF-02, DEF-03, DEF-04]

duration: 6min
completed: 2026-04-23
---

# Phase 03.1 Plan 01: Multi-Signal Defense Fusion — Wave 0 Pre-Flight Summary

**Wave 0 TDD pre-flight: 10 defense test stubs created (9 skip/1 red), DistilBERT cached locally, models/ directory initialized**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-23T18:40:38Z
- **Completed:** 2026-04-23T18:46:57Z
- **Tasks:** 3 of 3
- **Files modified:** 4

## Accomplishments

- Created `tests/test_defense.py` with 10 test items covering DEF-01 (BERT signal), DEF-02 (system-prompt constant), DEF-03 (defense_fn contract), and DEF-04 (perplexity, imperative ratio, retrieval fingerprint, meta-classifier)
- Extended `tests/test_generator.py` with `TestGenerateSystemPrompt` (2 red stubs that will go green when Wave 1 Plan 03 adds `DEF_02_SYSTEM_PROMPT` and `system_prompt` kwarg to `generate()`)
- Downloaded and cached DistilBERT (`distilbert-base-uncased`, `DistilBertForSequenceClassification` with `num_labels=2`) — unblocks `scripts/train_defense.py` in Wave 2
- Created `models/bert_classifier/.gitkeep` as directory marker for fine-tuned model artifacts

## Task Commits

1. **Task 1: Write tests/test_defense.py with 9 stubs** — `f100344` (test)
2. **Task 2: Extend tests/test_generator.py with TestGenerateSystemPrompt** — `7fb9c82` (test)
3. **Task 3: Create models/ directory structure and download DistilBERT** — `f62fc93` (chore)

## Files Created/Modified

- `tests/test_defense.py` — 10 stub test items for all 4 defense signals + contract; module-level try/except guards collection
- `tests/test_generator.py` — appended `TestGenerateSystemPrompt` class with 2 red stubs
- `models/bert_classifier/.gitkeep` — directory marker; fine-tuned model weights saved here after Wave 2 training
- `scripts/_download_distilbert.py` — one-time pre-flight script; downloads DistilBERT to HuggingFace cache

## Decisions Made

- Module-level `try/except ImportError` for `rag.defense` import ensures pytest collection exits 0 even before `rag/defense.py` is created. All 9 defense-dependent tests call `pytest.skip()` in their body — correct Wave 0 TDD state.
- `TestDef02Prompt.test_def02_system_prompt_constant_exists` does NOT skip — it directly imports `DEF_02_SYSTEM_PROMPT` and will produce `FAILED` (red) until Wave 1 Plan 03 adds the constant. This is by design: one test that actively drives the Wave 1 implementation.
- DistilBERT download uses an external script rather than `conda run -c` with inline Python due to a Windows conda limitation: conda 23.7.4 does not support multiline strings in `-c` arguments. The helper script `scripts/_download_distilbert.py` is an equivalent and reusable artifact.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used external script for DistilBERT download instead of inline conda -c**
- **Found during:** Task 3 (DistilBERT download)
- **Issue:** `conda run -n rag-security python -c "..."` with multiline string failed with `AssertionError: Support for scripts where arguments contain newlines not implemented` on Windows conda 23.7.4
- **Fix:** Created `scripts/_download_distilbert.py` containing the download code; invoked with `conda run -n rag-security python scripts/_download_distilbert.py`
- **Files modified:** `scripts/_download_distilbert.py` (new file)
- **Verification:** Second invocation completed without network download (cache hit confirmed)
- **Committed in:** `f62fc93` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 3 — blocking)
**Impact on plan:** Minimal — download succeeded identically via external script. Adds a useful reusable artifact.

## Issues Encountered

None beyond the conda multiline argument limitation (documented as deviation above).

## Known Stubs

The following stubs are intentional Wave 0 artifacts — they will be wired in subsequent Wave 1 plans:

| Stub | File | Status |
|------|------|--------|
| `TestBertSignal` (2 tests) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestPerplexitySignal` (1 test) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestImperativeSignal` (2 tests) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestRetrievalSignal` (1 test) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestMetaClassifier` (1 test) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestFusedDefenseContract` (2 tests) | `tests/test_defense.py` | Skips until `rag/defense.py` created (Plan 02) |
| `TestDef02Prompt` (1 test) | `tests/test_defense.py` | Fails red until `DEF_02_SYSTEM_PROMPT` in `rag/generator.py` (Plan 03) |
| `TestGenerateSystemPrompt` (2 tests) | `tests/test_generator.py` | Fails red until Wave 1 Plan 03 adds constant + kwarg |

These stubs are the Wave 0 purpose — not wiring gaps to fix. Each will go green when the corresponding Wave 1 plan implements the targeted interface.

## Next Phase Readiness

- Wave 0 pre-flight complete — Wave 1 (Plan 02) can begin implementing `rag/defense.py`
- DistilBERT cached locally — `scripts/train_defense.py` (Wave 2) can start training immediately
- `models/bert_classifier/` directory ready to receive fine-tuned model artifacts
- All 9 `test_defense.py` rag.defense stubs will flip from skip → green as Plan 02 implements `FusedDefense`
- `TestGenerateSystemPrompt` and `TestDef02Prompt` will flip from fail → green as Plan 03 adds `DEF_02_SYSTEM_PROMPT`

---

*Phase: 03.1-multi-signal-defense-fusion*
*Completed: 2026-04-23*
