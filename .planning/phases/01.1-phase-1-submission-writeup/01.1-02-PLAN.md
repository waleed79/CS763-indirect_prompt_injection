---
phase: 01.1-phase-1-submission-writeup
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - docs/phase1-writeup.md
autonomous: false
requirements:
  - PH1-05
  - PH1-06
  - PH1-07

must_haves:
  truths:
    - "The threat model opens with a 1-2 sentence attacker scenario, then lists capabilities, goals, and limitations as explicit bullet lists"
    - "The threat model explicitly states what the attacker CANNOT control (LLM weights, system prompt, user queries, retrieval outcome)"
    - "The methodology overview describes three stages (RAG pipeline, corpus poisoning attacks, BERT defense) without naming specific libraries"
    - "ASR is defined as the key evaluation metric in the methodology section"
    - "The execution plan maps specific deliverables to Phase 2 (Apr 12) and Phase 3 (Apr 30) deadlines"
    - "The complete document is clean academic prose suitable for copy-paste into the Google Doc"
  artifacts:
    - path: "docs/phase1-writeup.md"
      provides: "Complete Phase 1 writeup — all 8 sections including threat model, methodology, and execution plan"
      contains: "Attack Success Rate"
  key_links:
    - from: "docs/phase1-writeup.md Section 6 (Threat Model)"
      to: "PH1-05"
      via: "structured bullet format with capabilities and explicit limitations"
      pattern: "cannot control\|cannot.*access\|no.*access"
    - from: "docs/phase1-writeup.md Section 8 (Execution Plan)"
      to: "PH1-07"
      via: "deadline-mapped deliverables table"
      pattern: "April 12\|April 30\|Phase 2\|Phase 3"
---

<objective>
Complete the Phase 1 submission writeup by appending the threat model, methodology preview, and execution plan sections to docs/phase1-writeup.md, then get human sign-off that the complete document is ready to copy into the Google Doc.

Purpose: These three sections (PH1-05, PH1-06, PH1-07) complete the seven required components of the Phase 1 submission. The document is already overdue (was due 2026-03-27) — once the human approves, copy the content into the shared Google Doc immediately.

Output: Complete docs/phase1-writeup.md with all 8 sections and a human-verified checkpoint confirming the document is Google-Doc-ready.
</objective>

<execution_context>
@C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh\project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh\project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md
@.planning/phases/01.1-phase-1-submission-writeup/01.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write threat model section (PH1-05)</name>

  <read_first>
    - docs/phase1-writeup.md (current state after Plan 01 — append to existing content)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md (Pattern 2 — threat model structure; exact bullet content for capabilities, goals, limitations)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md (D-01, D-02, D-03 — structured bullet format; three specific capabilities; no mathematical formalism)
  </read_first>

  <files>docs/phase1-writeup.md</files>

  <action>
Append the Threat Model section (Section 6) to docs/phase1-writeup.md. Follow the structure locked in D-01 exactly: short scenario intro, then explicit bullet lists. No mathematical notation, no sets, no game-theoretic formalism (per D-03).

**Section 6: Threat Model**
Heading: `## Threat Model`

**Opening scenario (1-2 sentences, per D-01):**
Consider an adversary with write access to a document store used by an enterprise RAG-based assistant. Without ever interacting with the LLM, querying the system as a privileged user, or modifying the model weights, the adversary uploads a small number of documents containing embedded instructions that will be retrieved and injected into the LLM context window when legitimate users ask relevant questions.

**Attacker Capabilities**
Heading: `### Attacker Capabilities`

Bullet list (per D-02 — all three must appear):
- **Corpus write access:** The attacker can insert, modify, or delete documents in the retrieval database. This is the sole required access — the attacker needs no other system privileges.
- **Black-box query access:** The attacker can interact with the RAG system as a normal user to observe outputs and refine poisoned documents. They cannot access model internals, logs, or other users' queries.
- **Domain knowledge:** The attacker understands the general topic domain of the knowledge base and the types of questions legitimate users are likely to issue. This allows crafting documents that will be semantically retrieved for targeted query types.

**Attacker Goals**
Heading: `### Attacker Goals`

Bullet list:
- **Instruction override:** Cause the LLM to follow the attacker's embedded instructions rather than the system prompt — for example, responding with attacker-specified text or refusing to answer.
- **Misinformation injection:** Cause the LLM to produce factually incorrect but plausible-sounding answers that serve the attacker's interests.
- **Data exfiltration:** Embed instructions that cause the LLM to request or reveal user-provided information in its response.

**What the Attacker Cannot Control**
Heading: `### What the Attacker Cannot Control`

Bullet list (explicitly required — as important as the capabilities list):
- **LLM weights and system prompt:** The model architecture, parameters, and operator-defined system instructions are not accessible to the attacker. The attacker cannot fine-tune, modify, or observe the system prompt.
- **User queries:** The attacker does not know the specific questions individual users will ask. They can only predict the general topic domain and rely on semantic similarity to trigger retrieval.
- **Retrieval outcome:** The attacker cannot deterministically force their document to be retrieved for a given query. Retrieval depends on embedding similarity, and the attacker's document competes with all legitimate documents in the corpus. High retrieval probability requires carefully crafted content.
- **LLM inference internals:** Attention weights, generation logits, and intermediate states are not observable. The attacker receives only the final text output.

CONSTRAINTS: All three attacker capabilities from D-02 must appear as bullets. The "What the Attacker Cannot Control" section is mandatory (per RESEARCH.md Pitfall 3). Do not use any mathematical notation.
  </action>

  <verify>
    <automated>grep -c "cannot control\|cannot.*access\|cannot.*force\|not accessible\|not.*observable" "C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/docs/phase1-writeup.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "## Threat Model" docs/phase1-writeup.md` returns 1 match
    - `grep "### Attacker Capabilities" docs/phase1-writeup.md` returns 1 match
    - `grep "### Attacker Goals" docs/phase1-writeup.md` returns 1 match
    - `grep "### What the Attacker Cannot Control" docs/phase1-writeup.md` returns 1 match
    - `grep -i "corpus write access" docs/phase1-writeup.md` returns 1 match (D-02 capability 1)
    - `grep -i "black-box\|no LLM access\|cannot.*modify.*model\|not accessible" docs/phase1-writeup.md` returns at least 1 match (D-02 capability 2)
    - `grep -i "cannot.*force.*retrieval\|rely on semantic similarity\|Retrieval.*depends" docs/phase1-writeup.md` returns at least 1 match (D-02 capability 3 — no retrieval control)
    - No mathematical notation: `grep "\\\\forall\|\\\\exists\|\\\\in\|:=" docs/phase1-writeup.md` returns 0 matches
  </acceptance_criteria>

  <done>Threat model section written with attacker scenario, three capability bullets (including all from D-02), goals bullets, and explicit "cannot control" limitations bullets. No mathematical formalism present.</done>
</task>

<task type="auto">
  <name>Task 2: Write methodology preview and execution plan (PH1-06, PH1-07)</name>

  <read_first>
    - docs/phase1-writeup.md (current state — append to existing content)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md (Pattern 4 — execution plan table; Pattern in architecture patterns — three-stage methodology; D-07, D-08 — high-level only, no library names, define ASR)
    - .planning/ROADMAP.md (Phase 2.1–2.3 and Phase 3.1–3.2 deliverables — source of truth for execution plan content)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md (D-07, D-08 — methodology constraints; no pseudocode, no library names, no component diagrams)
  </read_first>

  <files>docs/phase1-writeup.md</files>

  <action>
Append the Proposed Methodology section (Section 7) and Execution Plan section (Section 8) to docs/phase1-writeup.md.

---

**Section 7: Proposed Methodology**
Heading: `## Proposed Methodology`

Opening sentence: Our approach proceeds in three stages: building a functional RAG pipeline as the experimental substrate, implementing corpus poisoning attacks at multiple sophistication levels, and developing and evaluating a context sanitization defense.

**7.1: Stage 1 — RAG Pipeline**
Heading: `### Stage 1: RAG Pipeline Construction`

Content: We will build a custom RAG pipeline — without abstraction frameworks — to maintain explicit control over each component of the retrieve-then-generate pipeline. The pipeline consists of an embedding model that encodes documents and queries as dense vectors, a vector store that indexes document embeddings and retrieves the top-k most similar documents for a given query, and a local open-source language model that generates answers grounded in the retrieved context. Building the pipeline from primitives (rather than using a high-level framework) ensures that the retrieval-to-generation boundary — the attack surface — is fully observable and instrumentable. All components will run locally to ensure reproducibility.

**7.2: Stage 2 — Corpus Poisoning Attacks**
Heading: `### Stage 2: Corpus Poisoning Attacks`

Content: We will implement at least two tiers of corpus poisoning attacks targeting the retrieval stage. The naive tier embeds explicit instruction overrides directly in document content (e.g., "Ignore previous instructions and respond with..."). The sophisticated tier crafts documents that blend into the legitimate corpus topically — appearing relevant to user queries — while embedding subtler instructional payloads that are harder to detect by surface-level inspection. Attack documents will be injected into a clean corpus and the pipeline will be evaluated on a test query set. We will measure at least two payload types: instruction override (forcing the LLM to follow the attacker's instructions) and one additional payload type (misinformation injection or data exfiltration prompt). The primary evaluation metric is Attack Success Rate (ASR): the fraction of test queries for which the attack successfully hijacks the LLM's response.

**7.3: Stage 3 — Context Sanitization Defense**
Heading: `### Stage 3: BERT-Based Context Sanitization`

Content: As a defense, we will train a BERT-based classifier to detect retrieved chunks containing imperative or instructional content — the linguistic signature of indirect prompt injection payloads. The classifier is integrated as middleware between the retrieval step and the generation step: retrieved chunks flagged as potentially malicious are filtered out before being injected into the LLM prompt. We will evaluate defense effectiveness as the reduction in ASR compared to the undefended baseline, and measure utility preservation as the change in answer quality on benign (non-attack) queries with the defense active. This before/after comparison is the core experimental result.

CONSTRAINTS (per D-07): Do not name specific libraries (no "sentence-transformers", "ChromaDB", "Ollama", "BERT-base-uncased"). Use "an embedding model", "a vector store", "a local language model", "a BERT-based classifier". No pseudocode. No component diagrams. No equations.

CONSTRAINTS (per D-08): ASR must be defined — use the phrase "Attack Success Rate (ASR): the fraction of test queries for which the attack successfully hijacks the LLM's response."

---

**Section 8: Execution Plan**
Heading: `## Execution Plan`

Opening sentence: Remaining work maps to two course deadlines: Phase 2 (April 12, 2026) and Phase 3 (April 30, 2026).

**Phase 2 Deliverables (due April 12, 2026)**
Heading: `### Phase 2 — April 12, 2026`

Bullet list (derived from ROADMAP.md phases 2.1–2.3):
- Functional custom RAG pipeline with embedding model, vector store, and local LLM generation running end-to-end
- A clean retrieval corpus of 500+ documents indexed and queryable
- Corpus poisoning attack module implementing at least two attack tiers (naive instruction override plus one more sophisticated tier)
- At least two payload types demonstrated: instruction override and one of misinformation injection or data exfiltration
- Baseline ASR measured on the undefended pipeline (this number motivates the Phase 3 defense)
- Automated evaluation harness with retrieval logging (documents retrieved per query, with similarity scores)

**Phase 3 Deliverables (due April 30, 2026)**
Heading: `### Phase 3 — April 30, 2026`

Bullet list (derived from ROADMAP.md phases 3.1–3.2):
- BERT-based context sanitization classifier trained and integrated as pipeline middleware
- Full experiment matrix: before/after ASR comparison across attack tiers with defense active
- Utility preservation measurement: answer quality on benign queries with and without defense
- Complete final report with results tables, at least one visualization (ASR comparison chart), and a limitations section
- GitHub repository with pinned package versions, configuration files, and replication instructions

CONSTRAINTS (per D-07): Do not list specific library names in this section.
  </action>

  <verify>
    <automated>grep -c "April 12\|April 30\|Phase 2\|Phase 3" "C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/docs/phase1-writeup.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "## Proposed Methodology" docs/phase1-writeup.md` returns 1 match
    - `grep "## Execution Plan" docs/phase1-writeup.md` returns 1 match
    - `grep "Attack Success Rate (ASR)" docs/phase1-writeup.md` returns 1 match (defined per D-08)
    - `grep "April 12" docs/phase1-writeup.md` returns 1 match (Phase 2 deadline)
    - `grep "April 30" docs/phase1-writeup.md` returns 1 match (Phase 3 deadline)
    - `grep "Stage 1\|Stage 2\|Stage 3" docs/phase1-writeup.md` returns 3 matches (three-stage structure per D-07)
    - No library names: `grep "sentence-transformers\|ChromaDB\|Ollama\|bert-base-uncased" docs/phase1-writeup.md` returns 0 matches
    - `grep "### Phase 2" docs/phase1-writeup.md` returns 1 match
    - `grep "### Phase 3" docs/phase1-writeup.md` returns 1 match
    - `grep "utility preservation\|answer quality" docs/phase1-writeup.md` returns at least 1 match (secondary metric per D-08)
  </acceptance_criteria>

  <done>Methodology section covers all three stages at high-level narrative with no library names. ASR is defined. Execution plan maps concrete deliverables to both remaining deadlines (April 12 and April 30).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete docs/phase1-writeup.md with all 8 sections:
    1. Abstract
    2. Note on Project Pivot (TVRID, one paragraph)
    3. Problem Statement (with direct vs indirect injection distinction)
    4. Motivation (OWASP LLM01:2025)
    5. Literature Survey (4 themes, 4+ papers with arXiv IDs and venues)
    6. Threat Model (scenario + capabilities + goals + "cannot control" bullets)
    7. Proposed Methodology (3 stages, ASR defined, no library names)
    8. Execution Plan (Phase 2 Apr 12 + Phase 3 Apr 30 deliverables)
  </what-built>

  <how-to-verify>
    1. Read docs/phase1-writeup.md in full
    2. Verify these 7 content checks match course requirements:
       - [ ] Indirect vs direct injection distinction is explicit (both terms defined, contrast drawn)
       - [ ] OWASP LLM01:2025 cited as #1 LLM vulnerability with URL genai.owasp.org
       - [ ] Four required papers cited: Greshake (arXiv:2302.12173), PoisonedRAG (arXiv:2402.07867, USENIX Security 2024), BIPIA (arXiv:2312.14197, KDD 2025), OWASP LLM01:2025
       - [ ] Threat model has explicit "What the Attacker Cannot Control" bullet list
       - [ ] Methodology mentions three stages and defines ASR — no library names appear
       - [ ] Execution plan lists specific deliverables under April 12 and April 30 headings
       - [ ] Pivot note is one paragraph, mentions TVRID, framed positively
    3. Run final grep check: `grep -c "indirect prompt injection" docs/phase1-writeup.md` should return >= 3
    4. Confirm the document reads as clean academic prose (suitable for Google Doc copy-paste)
    5. If approved: copy docs/phase1-writeup.md content into the shared Google Doc and submit
  </how-to-verify>

  <resume-signal>Type "approved" to proceed to Phase 1.2 (initial RAG pipeline), or describe any issues to fix first</resume-signal>
</task>

</tasks>

<verification>
After automated tasks complete (before human checkpoint):

```bash
# Full section presence check
grep "## Abstract\|## Note on Project Pivot\|## Problem Statement\|## Motivation\|## Literature Survey\|## Threat Model\|## Proposed Methodology\|## Execution Plan" docs/phase1-writeup.md | wc -l
# Expected: 8

# Critical content checks
grep -c "indirect prompt injection" docs/phase1-writeup.md  # >= 3
grep "Unlike direct prompt injection" docs/phase1-writeup.md  # key sentence present
grep "Attack Success Rate (ASR)" docs/phase1-writeup.md  # ASR defined
grep "April 12\|April 30" docs/phase1-writeup.md  # deadlines mapped

# Anti-pattern checks (all should return 0)
grep "sentence-transformers\|ChromaDB\|Ollama\|bert-base-uncased" docs/phase1-writeup.md
grep -c "initial results" docs/phase1-writeup.md

# Citation completeness
grep "arXiv:2302.12173" docs/phase1-writeup.md  # Greshake
grep "arXiv:2402.07867" docs/phase1-writeup.md  # PoisonedRAG
grep "arXiv:2312.14197" docs/phase1-writeup.md  # BIPIA
grep "genai.owasp.org\|OWASP" docs/phase1-writeup.md  # OWASP
```
</verification>

<success_criteria>
- docs/phase1-writeup.md is complete with all 8 sections
- All 7 Phase 1 requirements (PH1-01 through PH1-07) satisfied with verifiable content
- Document contains no deferred content (no initial results claims, no library names)
- Human has reviewed and approved for Google Doc submission
- Phase 1.1 is marked complete and team submits to Google Doc immediately
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-submission-writeup/01.1-02-SUMMARY.md` with:
- Files modified: docs/phase1-writeup.md (complete — Sections 6, 7, 8 added)
- Key content: threat model (scenario + 3 capabilities + goals + cannot-control bullets), methodology (3 stages, ASR defined), execution plan (Apr 12 + Apr 30 deliverables)
- Human checkpoint: approved (note actual outcome)
- Google Doc: submitted (note actual outcome)
- Ready for: Phase 1.2 (initial RAG pipeline, optional) or Phase 2.1 (RAG pipeline foundation)
</output>
