---
phase: 01.1-phase-1-submission-writeup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase1-writeup.md
autonomous: true
requirements:
  - PH1-01
  - PH1-02
  - PH1-03
  - PH1-04

must_haves:
  truths:
    - "The document contains a one-paragraph TVRID pivot note framed positively before the problem statement"
    - "The document defines indirect prompt injection and explicitly distinguishes it from direct prompt injection"
    - "The document explains why RAG security matters citing OWASP LLM01:2025 as the #1 LLM vulnerability"
    - "The literature survey is organized thematically (not as annotated bibliography) and covers all four required papers"
    - "Each cited paper is discussed in relation to this project's specific contribution"
  artifacts:
    - path: "docs/phase1-writeup.md"
      provides: "Phase 1 writeup draft — Sections 1 through 5 (pivot, problem statement, motivation, literature survey)"
      contains: "indirect prompt injection"
  key_links:
    - from: "docs/phase1-writeup.md Section 3"
      to: "PH1-01"
      via: "explicit definition of indirect vs direct injection"
      pattern: "direct.*injection|indirect.*injection"
    - from: "docs/phase1-writeup.md Section 5"
      to: "PH1-04"
      via: "thematic literature survey with 4+ papers cited"
      pattern: "Greshake|PoisonedRAG|BIPIA|OWASP"
---

<objective>
Create the docs/ directory and write the first half of the Phase 1 submission writeup: the file scaffold, TVRID pivot note, problem statement (with the critical indirect vs. direct injection distinction), motivation section, and thematic literature survey.

Purpose: Produce clean academic prose for the CS 763 Google Doc submission (already overdue — submit immediately after Plan 02 completes). These four sections establish the conceptual foundation the threat model and methodology sections (Plan 02) depend on.

Output: docs/phase1-writeup.md containing Sections 1-5 of the writeup (abstract through literature survey).
</objective>

<execution_context>
@C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs scaffold and write abstract + pivot note (PH1-03)</name>

  <read_first>
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md (decisions D-10, D-11 — format and pivot constraints)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md (Pattern 5 — pivot note content spec)
    - .planning/PROJECT.md (pivot rationale: TVRID → RAG injection)
  </read_first>

  <files>docs/phase1-writeup.md</files>

  <action>
Create the docs/ directory and docs/phase1-writeup.md. Write the document header, abstract, and pivot note as follows.

**Document header (metadata comment at top of file):**
```
<!-- Phase 1 Submission — CS 763 Computer Security, UW-Madison Spring 2026 -->
<!-- Team: Muhammad Musa & Waleed Arshad -->
<!-- Submission target: shared Google Doc (Prof. Somesh Jha / TA Saikumar Yadugiri) -->
<!-- Local draft — copy-paste into Google Doc for submission -->
```

**Section 1: Abstract (2-3 sentences)**
Heading: `# Indirect Prompt Injection in Retrieval-Augmented Generation Systems`
Subheading: `## Abstract`

Content: Retrieval-Augmented Generation (RAG) has become the dominant paradigm for grounding large language model (LLM) outputs in external knowledge, and is widely deployed in enterprise systems. This project investigates indirect prompt injection in RAG — an attack where an adversary who can write documents to the retrieval corpus can hijack LLM responses for any user who triggers retrieval of those documents. We build a complete attack-and-defense pipeline and measure Attack Success Rate (ASR) before and after applying a BERT-based context sanitization defense.

**Section 2: Project Pivot Note (PH1-03 — ONE paragraph, per D-11)**
Heading: `## Note on Project Pivot`

Content: This project is a pivot from our original pre-proposal topic, which addressed cross-modal RGB-Depth person re-identification for ICPR 2026 (TVRID). After initial scoping, the team decided to pivot to RAG prompt injection for three reasons: the topic is more directly relevant to our research interests in LLM security and safety; it is better suited to the course timeline (a complete attack + defense contribution is achievable in one month with the available tooling); and it addresses an active, industry-recognized threat (OWASP classifies prompt injection as the #1 LLM vulnerability). The RAG injection problem has an established literature base — including Greshake et al. (2023) and Zou et al. (2024) — which enables us to position our contribution clearly relative to prior work.

CONSTRAINTS (per D-11): This section must be exactly one paragraph. Do not add sub-bullets or split into multiple paragraphs. Frame positively (what was gained), not defensively (what was abandoned).
  </action>

  <verify>
    <automated>grep -i "TVRID\|pivot" "C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/docs/phase1-writeup.md" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - docs/phase1-writeup.md exists
    - `grep -i "TVRID" docs/phase1-writeup.md` returns at least 1 match
    - `grep -i "pivot" docs/phase1-writeup.md` returns at least 1 match
    - The pivot note section contains exactly one paragraph (no blank-line-separated paragraph breaks within the section body)
    - `grep "## Note on Project Pivot" docs/phase1-writeup.md` returns 1 match
    - `grep "## Abstract" docs/phase1-writeup.md` returns 1 match
  </acceptance_criteria>

  <done>docs/phase1-writeup.md created with abstract and one-paragraph pivot note. File exists and grep confirms both "TVRID" and "pivot" appear in the document.</done>
</task>

<task type="auto">
  <name>Task 2: Write problem statement with direct vs indirect injection distinction (PH1-01)</name>

  <read_first>
    - docs/phase1-writeup.md (current state — append to existing content)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md (Pattern 1 — distinguishing direct vs indirect injection; key sentence pattern to use)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md (D-01, D-03 — no heavy formalism)
  </read_first>

  <files>docs/phase1-writeup.md</files>

  <action>
Append the Problem Statement section (Section 3) to docs/phase1-writeup.md. This section has three subsections.

**Section 3: Problem Statement**
Heading: `## Problem Statement`

**3.1: What is Retrieval-Augmented Generation?**
Heading: `### What is Retrieval-Augmented Generation?`

Content: Retrieval-Augmented Generation (RAG) augments a large language model with an external knowledge base. When a user submits a query, the system first retrieves the most semantically similar documents from the knowledge base (using an embedding model and vector similarity search), then injects those documents as context into the LLM's prompt alongside the user's question. The LLM generates a response grounded in the retrieved content. RAG was designed to reduce hallucination and enable domain-specific knowledge without fine-tuning — but this architecture was not designed with adversarial inputs in mind. The retrieval step, specifically the injection of third-party document content into the LLM context window, is the attack surface this project studies.

**3.2: The New Attack Surface RAG Introduces**
Heading: `### The Attack Surface RAG Introduces`

Content: Standard LLM deployments take user input, apply a system prompt, and produce output. The attack surface is the user-to-model boundary. RAG extends this: a third-party corpus now contributes content to every LLM context window. If an adversary can write documents to that corpus, they can inject content that the model will treat as authoritative retrieved knowledge. The adversary does not need to interact with the user, the LLM, or the system prompt. A single poisoned document, once indexed, can affect every user who issues a query that triggers its retrieval.

**3.3: Direct vs. Indirect Prompt Injection — The Key Distinction**
Heading: `### Direct vs. Indirect Prompt Injection`

Content: *Direct prompt injection* occurs when an attacker controls the user's input and embeds malicious instructions in the query itself. The attacker IS the user. Defenses include input validation, rate limiting, and system prompt hardening applied at the user-model boundary.

*Indirect prompt injection* is fundamentally different. The adversary has no access to the user interface. Instead, they poison the knowledge base (retrieval corpus). When a legitimate user submits a benign query, the RAG system retrieves attacker-controlled content containing embedded instructions. Those instructions are injected into the LLM context window alongside the legitimate query, and the model — unable to distinguish retrieved knowledge from retrieved instructions — follows the attacker's commands instead of the system prompt.

Key sentence (include verbatim): "Unlike direct prompt injection — where the attacker controls the user's input — indirect prompt injection exploits the retrieval boundary: an adversary who can write a single document to the knowledge base can hijack the LLM's response for any user who triggers retrieval of that document."

CONSTRAINTS: Both the terms "direct prompt injection" and "indirect prompt injection" must appear and be defined. Do not conflate them. The phrase "indirect prompt injection" must appear at least three times in this section.
  </action>

  <verify>
    <automated>grep -c "indirect prompt injection\|indirect.*injection" "C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/docs/phase1-writeup.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -i "direct prompt injection" docs/phase1-writeup.md` returns at least 1 match (for the direct injection definition)
    - `grep -i "indirect prompt injection" docs/phase1-writeup.md` returns at least 3 matches
    - `grep "### Direct vs. Indirect Prompt Injection" docs/phase1-writeup.md` returns 1 match
    - `grep "Unlike direct prompt injection" docs/phase1-writeup.md` returns 1 match (key verbatim sentence)
    - `grep "### What is Retrieval-Augmented Generation" docs/phase1-writeup.md` returns 1 match
    - `grep "### The Attack Surface RAG Introduces" docs/phase1-writeup.md` returns 1 match
  </acceptance_criteria>

  <done>Problem statement section written with all three subsections. The critical direct/indirect injection distinction is explicitly drawn with a verbatim key sentence. Both terms appear and are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Write motivation section and thematic literature survey (PH1-02, PH1-04)</name>

  <read_first>
    - docs/phase1-writeup.md (current state — append to existing content)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-RESEARCH.md (Pattern 3 — thematic literature survey spec; full citation table with arXiv IDs and venues; anti-patterns to avoid)
    - .planning/phases/01.1-phase-1-submission-writeup/01.1-CONTEXT.md (D-04, D-05, D-06 — thematic synthesis, required papers, each paper tied to project contribution)
  </read_first>

  <files>docs/phase1-writeup.md</files>

  <action>
Append the Motivation section (Section 4) and Literature Survey section (Section 5) to docs/phase1-writeup.md.

---

**Section 4: Motivation**
Heading: `## Motivation`

Content: Prompt injection is classified as LLM01:2025 — the #1 vulnerability in the OWASP Top 10 for LLM Applications (2025 edition), marking the second consecutive edition in which it holds the top position [OWASP 2025]. The OWASP definition explicitly covers indirect injection via third-party content, including retrieval-augmented generation pipelines. Beyond classification, RAG is not a niche research prototype: enterprise adoption of RAG-based assistants has grown rapidly across industries including healthcare, legal, financial services, and software development. Systems such as Microsoft Copilot, enterprise chatbots, and customer-facing knowledge assistants are all RAG-based architectures. Each of these systems shares the same vulnerability: any party who can contribute documents to the knowledge base can, in principle, inject attacker-controlled instructions into the LLM context window of any legitimate user. The scale of deployment makes this a timely and consequential security research problem.

---

**Section 5: Literature Survey**
Heading: `## Literature Survey`

Opening sentence: Prior work on indirect prompt injection and RAG security falls into four natural themes: RAG background and motivation, attack research, defense and detection, and evaluation frameworks.

**5.1: Theme 1 — RAG Background and Motivation**
Heading: `### Theme 1: RAG Background and Motivation`

Content: Lewis et al. (2020) introduced Retrieval-Augmented Generation as a method for augmenting pretrained language models with non-parametric knowledge retrieved from an external document store, demonstrating that retrieval-augmented models outperform parametric-only models on knowledge-intensive NLP tasks [Lewis et al., NeurIPS 2020]. This work established the architectural pattern — encode documents as dense vectors, retrieve by similarity, inject as context — that underlies every modern enterprise RAG system. Critically, Lewis et al. designed RAG for accuracy and factual grounding, not adversarial robustness. The attack surface studied in this project — the retrieval-context injection boundary — is precisely what their architecture introduced.

**5.2: Theme 2 — Attack Research**
Heading: `### Theme 2: Attack Research`

Content: Greshake et al. (2023) established the foundational taxonomy of indirect prompt injection, demonstrating that LLM-integrated applications (browser plugins, email summarizers, travel planners) can be compromised by embedding instructions in third-party content the LLM processes [Greshake et al., IEEE S&P Workshop SecTL, arXiv:2302.12173]. Their work covered a broad class of applications where the LLM processes external web content. This project narrows the focus to RAG-specific corpus poisoning, where the injection vector is a retrieval database rather than live web content — a more controlled and measurable attack scenario.

Zou et al. (2024) demonstrated corpus poisoning attacks specifically targeting RAG systems in PoisonedRAG, showing that injecting as few as five poisoned documents into a one-million-document corpus can achieve over 90% attack success rate [Zou et al., USENIX Security 2024, arXiv:2402.07867]. PoisonedRAG is the closest comparable work to this project. A key gap in PoisonedRAG is that it characterizes the attack but does not develop or evaluate a defense. This project extends their attack framework with a defense evaluation component.

Shafran et al. (2025) demonstrated a different attack variant — denial-of-service via "blocker" documents that prevent relevant results from reaching the LLM — published at USENIX Security 2025 [Shafran et al., arXiv:2406.05870]. The breadth of RAG attack variants (injection, poisoning, jamming) underscores that the retrieval boundary is a rich and underexplored attack surface. This project focuses on the injection/poisoning variant rather than denial-of-service.

**5.3: Theme 3 — Defense and Detection**
Heading: `### Theme 3: Defense and Detection`

Content: Yi et al. (2023) introduced BIPIA (Benchmarking and Defending Against Indirect Prompt Injection Attacks), which proposed four black-box prompt-learning defenses and one white-box fine-tuning defense, evaluated across email QA, web QA, and summarization tasks [Yi et al., KDD 2025, arXiv:2312.14197]. BIPIA's defenses operate at the prompt level — they modify how the LLM is instructed to treat external content. This project proposes a complementary approach: a BERT-based context sanitization classifier that operates at the retrieved-chunk level, filtering potentially malicious content before it reaches the LLM prompt. This architectural difference — pre-prompt filtering versus prompt-level instruction — is the primary distinction between this work and BIPIA's defense proposals.

**5.4: Theme 4 — Evaluation Frameworks and Industry Context**
Heading: `### Theme 4: Evaluation Frameworks and Industry Context`

Content: The OWASP Top 10 for LLM Applications 2025 (LLM01:2025) classifies prompt injection — including indirect injection via retrieved content — as the top LLM vulnerability for the second consecutive edition [OWASP Foundation, genai.owasp.org]. The OWASP framework provides an industry-recognized risk taxonomy that grounds this project's threat model in real-world deployment concerns. This project's evaluation metric (Attack Success Rate) aligns with the impact framing in LLM01:2025: the measure of severity is how reliably the attack subverts the system's intended behavior.

CONSTRAINTS:
- All four required papers must appear: Greshake et al. (cite arXiv:2302.12173 and IEEE S&P Workshop venue), PoisonedRAG/Zou et al. (cite arXiv:2402.07867 and USENIX Security 2024), BIPIA/Yi et al. (cite arXiv:2312.14197 and KDD 2025), OWASP LLM01:2025 (cite genai.owasp.org).
- Each paper must be discussed in relation to what this project does differently or builds on (per D-06). Do not write standalone summaries.
- Survey is thematic (four named themes), not a paper-by-paper list (per D-04).
- Do not add any claim of initial results.
  </action>

  <verify>
    <automated>grep -c "Greshake\|PoisonedRAG\|BIPIA\|OWASP" "C:/Users/muham/OneDrive - UW-Madison/Spring_26/CS763_Somesh/project/docs/phase1-writeup.md"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Greshake\|PoisonedRAG\|BIPIA\|OWASP" docs/phase1-writeup.md` returns 4 or more (all four required papers present)
    - `grep "arXiv:2302.12173" docs/phase1-writeup.md` returns 1 match (Greshake citation with arXiv ID)
    - `grep "arXiv:2402.07867" docs/phase1-writeup.md` returns 1 match (PoisonedRAG citation with arXiv ID)
    - `grep "arXiv:2312.14197" docs/phase1-writeup.md` returns 1 match (BIPIA citation with arXiv ID)
    - `grep "genai.owasp.org\|OWASP" docs/phase1-writeup.md` returns at least 2 matches
    - `grep "## Literature Survey" docs/phase1-writeup.md` returns 1 match
    - `grep "## Motivation" docs/phase1-writeup.md` returns 1 match
    - `grep "Theme 1\|Theme 2\|Theme 3\|Theme 4" docs/phase1-writeup.md` returns 4 matches (four themes present)
    - `grep "USENIX Security 2024" docs/phase1-writeup.md` returns 1 match (PoisonedRAG venue)
    - `grep "KDD 2025" docs/phase1-writeup.md` returns 1 match (BIPIA venue)
  </acceptance_criteria>

  <done>Motivation section cites OWASP LLM01:2025 as the #1 LLM vulnerability. Literature survey contains four named themes with all four required papers cited with arXiv IDs, publication venues, and explicit project-relative integration sentences.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run these checks:

```bash
# Check file exists
ls docs/phase1-writeup.md

# Confirm critical sections present
grep "## Abstract" docs/phase1-writeup.md
grep "## Note on Project Pivot" docs/phase1-writeup.md
grep "## Problem Statement" docs/phase1-writeup.md
grep "## Motivation" docs/phase1-writeup.md
grep "## Literature Survey" docs/phase1-writeup.md

# Confirm key content
grep -c "indirect prompt injection" docs/phase1-writeup.md   # >= 3
grep -c "Greshake\|PoisonedRAG\|BIPIA\|OWASP" docs/phase1-writeup.md  # >= 4
grep "TVRID" docs/phase1-writeup.md
grep "Unlike direct prompt injection" docs/phase1-writeup.md
```
</verification>

<success_criteria>
- docs/phase1-writeup.md exists with 5 major sections (Abstract, Pivot Note, Problem Statement, Motivation, Literature Survey)
- "indirect prompt injection" appears at least 3 times
- "direct prompt injection" appears and is defined
- All four required papers cited with arXiv IDs and publication venues
- Each paper tied to the project's specific contribution (not standalone summaries)
- Literature survey is thematically organized (4 named themes)
- TVRID pivot appears as one positive paragraph
- No claims of initial results anywhere in the document
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-submission-writeup/01.1-01-SUMMARY.md` with:
- Files created: docs/phase1-writeup.md (Sections 1-5)
- Key content: abstract, pivot note, problem statement with direct/indirect distinction, motivation (OWASP), thematic lit survey (4 papers, 4 themes)
- Patterns established: academic prose style, citation format (Author et al., Venue Year, arXiv:XXXXXXX)
- Ready for: Plan 02 (threat model + methodology + execution plan, same file)
</output>
