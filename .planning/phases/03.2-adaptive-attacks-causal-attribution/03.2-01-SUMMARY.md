---
phase: 03.2-adaptive-attacks-causal-attribution
plan: "01"
subsystem: testing
tags: [pytest, tdd, red-state, adaptive-attacks, loo, seed-variance]

# Dependency graph
requires:
  - phase: 03.1-multi-signal-defense-fusion
    provides: rag/defense.py with IMPERATIVE_VERBS regex; parse_judge_output in run_judge.py
  - phase: 02.4-advanced-attack-tiers
    provides: generate_poisoned_corpus.py with TIER1_KEYWORDS; run_judge.py judge infrastructure

provides:
  - "TestAdaptiveCorpus stub class in test_corpus.py (3 tests, skip on current codebase)"
  - "TestLooExcludeFn stub class in test_loo.py (2 tests, skip — run_loo.py not yet created)"
  - "TestLooJudge class in test_loo.py (4 tests, green — parse_judge_output exists)"
  - "TestSeedVariance class in test_train.py (2 tests, green — stdlib and absence guard)"

affects:
  - 03.2-02 (adaptive corpus + train_defense --seed implementation — these stubs go green)
  - 03.2-03 (run_loo.py LOO attribution — TestLooExcludeFn goes green)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "importlib.util.spec_from_file_location pattern for loading scripts/ without package install"
    - "Module-level try/except guard with boolean flag + pytest.skip() in test body for wave 0 stubs"
    - "Regex-based argparse flag absence check (add_argument.*--seed) instead of bare string search"

key-files:
  created:
    - tests/test_loo.py
    - tests/test_train.py
  modified:
    - tests/test_corpus.py

key-decisions:
  - "importlib.spec_from_file_location used to load scripts/ modules without sys.path mutation — avoids contaminating test environment"
  - "test_train_defense_has_no_seed_flag_yet uses regex add_argument.*--seed not bare string — '--seed' already appears in train_defense.py docstring on line 95"
  - "TestLooJudge tests are green immediately (parse_judge_output from Phase 2.4 already satisfies contract)"

patterns-established:
  - "Wave 0 stub pattern: importlib guard + boolean flag + pytest.skip() in each test body"
  - "Absence guard for CLI flags uses regex on add_argument call, not bare string search"

requirements-completed: [ATK-08, ATK-09, DEF-05, EVAL-05]

# Metrics
duration: 6min
completed: 2026-04-24
---

# Phase 03.2 Plan 01: Wave 0 Test Stubs Summary

**Three new test classes establishing RED/SKIP state for adaptive attacks, LOO attribution, and multi-seed variance — all skipping or green on current codebase as designed**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-24T17:55:25Z
- **Completed:** 2026-04-24T18:01:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- TestAdaptiveCorpus (3 tests) appended to tests/test_corpus.py; all skip because NOVEL_ANCHOR_TOKENS and build_adaptive_passages() absent — correct RED state for plan 03.2-02
- TestLooExcludeFn (2 tests) + TestLooJudge (4 tests) created in tests/test_loo.py; ExcludeFn tests skip (run_loo.py missing), Judge tests are all green (parse_judge_output already satisfies the contract)
- TestSeedVariance (2 tests) created in tests/test_train.py; both green — stdlib shuffle variance confirmed, argparse --seed absence confirmed with regex guard
- Full test suite runs clean: 121 passed, 5 skipped, 0 failed

## Task Commits

Each task was committed atomically:

1. **Task 1: Add TestAdaptiveCorpus to tests/test_corpus.py** - `207d96c` (test)
2. **Task 2: Create tests/test_loo.py and tests/test_train.py stubs** - `fde63e8` (test)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `tests/test_corpus.py` — TestAdaptiveCorpus class appended (3 stub tests for ATK-08/09 validation using importlib guard)
- `tests/test_loo.py` — New file: TestLooExcludeFn (skip, run_loo.py absent) + TestLooJudge (green, parse_judge_output satisfies contract)
- `tests/test_train.py` — New file: TestSeedVariance with stdlib shuffle test and argparse flag absence guard

## Decisions Made

- `importlib.util.spec_from_file_location` used to load scripts/ modules without modifying sys.path globally — keeps test isolation clean
- `test_train_defense_has_no_seed_flag_yet` uses `re.search(r'add_argument\s*\([^)]*["\']--seed["\']', src)` instead of `"--seed" not in src` — because `--seed` appears as plain text in a docstring on line 95 of train_defense.py (Phase 3.2 EVAL-05 multi-seed aggregation documentation added during Phase 3.1-07 code review fixes)
- TestLooJudge kept as immediately-green tests (not skip-guard) because parse_judge_output already satisfies the full contract including edge cases

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test_train_defense_has_no_seed_flag_yet false failure**
- **Found during:** Task 2 (create tests/test_train.py)
- **Issue:** Plan's absence guard `assert "--seed" not in src` failed because `--seed` appears verbatim in train_defense.py docstring (line 95: "Phase 3.2 EVAL-05 multi-seed aggregation (--seed flag, WR-04 fix)") — test would always fail even before argparse flag is added
- **Fix:** Replaced bare string check with regex `re.search(r'add_argument\s*\([^)]*["\']--seed["\']', src)` that matches only actual argparse argument registration
- **Files modified:** tests/test_train.py
- **Verification:** `pytest tests/test_train.py::TestSeedVariance::test_train_defense_has_no_seed_flag_yet` exits 0; will correctly go RED once `add_argument("--seed", ...)` is added in plan 03.2-02
- **Committed in:** fde63e8 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug in test guard logic)
**Impact on plan:** Fix necessary for correct RED/GREEN state tracking. No scope change.

## Issues Encountered

None beyond the auto-fixed devation above.

## Known Stubs

- `tests/test_corpus.py::TestAdaptiveCorpus` — all 3 tests skip; awaits plan 03.2-02 `ADAPTIVE_ID_START`, `NOVEL_ANCHOR_TOKENS`, `build_adaptive_passages()`
- `tests/test_loo.py::TestLooExcludeFn` — both tests skip; awaits plan 03.2-03 `scripts/run_loo.py::make_exclude_fn`
- `tests/test_train.py::TestSeedVariance::test_train_defense_has_no_seed_flag_yet` — currently green; will go RED once plan 03.2-02 adds `add_argument("--seed", ...)` (delete stub at that point)

## Next Phase Readiness

- Plan 03.2-02 can proceed: adaptive corpus generation (ATK-08/09) + train_defense --seed flag
- Plan 03.2-03 can proceed: scripts/run_loo.py with make_exclude_fn for DEF-05 LOO attribution
- All stubs correctly in RED/SKIP state — no accidental early greening observed
- No blockers

## Self-Check: PASSED

- tests/test_corpus.py contains "TestAdaptiveCorpus": FOUND
- tests/test_loo.py contains "TestLooExcludeFn": FOUND
- tests/test_loo.py contains "TestLooJudge": FOUND
- tests/test_train.py contains "TestSeedVariance": FOUND
- Commit 207d96c exists: FOUND
- Commit fde63e8 exists: FOUND
- Full suite result: 121 passed, 5 skipped, 0 failed

---
*Phase: 03.2-adaptive-attacks-causal-attribution*
*Completed: 2026-04-24*
