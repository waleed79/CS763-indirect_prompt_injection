# Only excluding files that exceed GitHub's 100 MB per-file hard limit
# or that are multi-GB regenerable training artifacts.
# Everything else (planning, claude state, chroma DB, pptx, etc.) is committed.

# HuggingFace Trainer intermediate checkpoints (large; regenerable)
models/bert_classifier/checkpoint-*/
models/bert_s*/checkpoint-*/

# EVAL-05 multi-seed BERT final models (~2.5 GB each; regenerable from
# `python scripts/train_defense.py --seed N --bert-output models/bert_sN`)
models/bert_s1/
models/bert_s2/
models/bert_s3/

# Phase 3.1 BERT classifier final model (256 MB; exceeds GitHub 100 MB limit)
# Regenerate with: python scripts/train_defense.py
models/bert_classifier/model.safetensors
